# GPU / Performance feature matrix fragment.

[[feature]]
id = "X3-GPU-001"
name = "GPU finalized-TPS proof"
subsystem = "gpu_performance"
source = "research"
paths = ["GPU_VALIDATOR_HONEST_AUDIT.md"]
implemented = 10
tested = 5
mainnet_ready = 5
priority = "P0"
confidence = "high"
launch_scope = "research"
claim_risk = false
blockers = ["No physical GPU benchmark showing finalized-TPS improvement"]
evidence = ["GPU_VALIDATOR_HONEST_AUDIT.md", "note: Honest audit explicitly forbids GPU acceleration claim without real device"]

[[feature]]
id = "X3-GPU-002"
name = "Production runtime weights"
subsystem = "gpu_performance"
source = "master"
paths = ["runtime/src/lib.rs"]
implemented = 55
tested = 40
mainnet_ready = 40
priority = "P0"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Older audits found WeightInfo=() in several places"]
evidence = ["runtime/src/lib.rs", "note: Benchmark infrastructure exists but weight coverage is incomplete"]

[[feature]]
id = "X3-GPU-003"
name = "FRAME benchmarking"
subsystem = "gpu_performance"
source = "master"
paths = [".github/workflows/frame-benchmarking.yml"]
implemented = 70
tested = 60
mainnet_ready = 55
priority = "P0"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Generated production weights incomplete in older audits"]
evidence = [".github/workflows/frame-benchmarking.yml", "note: Frame benchmarking workflow exists and runs on active PR heads"]

[[feature]]
id = "X3-GPU-004"
name = "GPU/CPU parity checking"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 80
tested = 70
mainnet_ready = 55
priority = "P0"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Needs real hardware equivalence evidence"]
evidence = ["crates/x3-accel/src/lib.rs", "note: ParityMismatch and CPU recomputation architecture exists"]

[[feature]]
id = "X3-GPU-005"
name = "CPU consensus-truth backend"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 95
tested = 85
mainnet_ready = 80
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Needs continuous parity tests against future GPU kernels"]
evidence = ["crates/x3-accel/src/lib.rs", "note: CpuBackend is canonical implementation"]
test_evidence = ["note: audited test/CI evidence score=85; exact named tests should be added before score increase"]

[[feature]]
id = "X3-GPU-006"
name = "Fail-closed unavailable accelerator backend"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 95
tested = 85
mainnet_ready = 82
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Maintain strict mode coverage"]
evidence = ["crates/x3-accel/src/lib.rs", "note: BackendUnavailable/KernelUnavailable errors; no fake success"]
test_evidence = ["note: audited test/CI evidence score=85; exact named tests should be added before score increase"]

[[feature]]
id = "X3-GPU-007"
name = "GPU Keccak256 batching"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 20
tested = 10
mainnet_ready = 10
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["No verified accelerator kernel in current backend"]
evidence = ["crates/x3-accel/src/lib.rs", "note: WGPU backend currently KernelUnavailable"]

[[feature]]
id = "X3-GPU-008"
name = "GPU ed25519 verification"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 20
tested = 10
mainnet_ready = 10
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["No verified accelerator kernel"]
evidence = ["crates/x3-accel/src/lib.rs", "note: WGPU backend currently KernelUnavailable"]

[[feature]]
id = "X3-GPU-009"
name = "GPU secp256k1 verification"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 20
tested = 10
mainnet_ready = 10
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["WGPU backend reports KernelUnavailable; old GPU crate simulates CPU"]
evidence = ["crates/x3-accel/src/lib.rs", "note: Honest audit says no production GPU secp256k1 kernel"]

[[feature]]
id = "X3-GPU-010"
name = "Single-authority finalized TPS baseline"
subsystem = "gpu_performance"
source = "master"
paths = ["GPU_VALIDATOR_HONEST_AUDIT.md"]
implemented = 90
tested = 90
mainnet_ready = 35
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Single-authority result cannot stand in for production network"]
evidence = ["GPU_VALIDATOR_HONEST_AUDIT.md", "note: Measured ~51–54 sustained finalized TPS with zero failures"]
test_evidence = ["note: audited test/CI evidence score=90; exact named tests should be added before score increase"]

[[feature]]
id = "X3-GPU-011"
name = "WGPU accelerator backend"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel-wgpu"]
implemented = 70
tested = 55
mainnet_ready = 40
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Needs real physical GPU CI/run evidence"]
evidence = ["crates/x3-accel-wgpu", "note: Honest audit confirms real WGSL/wgpu device code compiles"]

[[feature]]
id = "X3-GPU-012"
name = "WGPU SHA-256 compute kernel"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel-wgpu"]
implemented = 75
tested = 55
mainnet_ready = 42
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["No production validator GPU benchmark yet"]
evidence = ["crates/x3-accel-wgpu", "note: Real SHA-256 compute path called out by honest audit"]

[[feature]]
id = "X3-GPU-013"
name = "GPU divergence quarantine / fallback"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-gpu-validator-swarm"]
implemented = 75
tested = 65
mainnet_ready = 50
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = [
  "**CLOSED 2026-09-25 — `UnifiedProof::validate()` called any non-empty bytes an attestation.** `proof_integration.rs` asserted `is_valid` for proofs whose attestation signature was `vec![1, 2, 3, 4]` and `vec![9, 9, 9]`, while the crate's own verifier — `ProofAggregator::signature_from_bytes`, then `verify_attestations` against a registered public key — refuses anything that is not 65 bytes (`r || s || recovery`). A caller who only saw `ProofValidationResult::is_valid` was told a placeholder was evidence. The check now requires `crypto::SIGNATURE_LENGTH`, one named constant shared with the verifier so the two cannot drift, and it is measured load-bearing: reverting it to `is_empty()` turns exactly `validate_refuses_a_signature_that_is_not_the_shape_the_verifier_reads` red (110 passed, 1 failed, file restored).",
  "An empty attestation set stays a *warning*, on purpose, because the aggregator's flow is submit-first: `test_submit_proof` submits an unattested proof and expects it collected, and only `add_attestation` past `finality_threshold` moves the entry. `an_unattested_proof_is_collected_but_never_finalized` pins the other half — silence never becomes finality.",
  "Still open: `GpuReceiptValidator::verify_signature` is a real Ed25519 verification over the receipt hash that **nothing calls** — it is defined and re-exported from `lib.rs`, and no path in this repository invokes it, so receipt-level signature checking is dead code rather than a control. It also uses a second signing convention (`pubkey || sig`, 96 bytes) where the aggregator uses `r || s || recovery` (65), and the two have never been reconciled.",
  "Needs real GPU failure injection",
]
test_evidence = [
  "note: the two regression tests are crates/x3-gpu-validator-swarm/src/unified_proof.rs (validate refuses a 4-byte signature, naming both lengths) and src/proof_aggregator.rs (an unattested proof is collected, never finalized), on 2026-09-25",
  "note: the crate is gated by `test x3-gpu-validator-swarm` (added 2026-09-25 — it had no gate): 111 lib tests plus its integration suites, all green",
]
evidence = ["crates/x3-gpu-validator-swarm", "note: Honest audit describes divergence detection, replay, quarantine, CPU fallback", "note: 2026-09-25 - the structural validator accepted placeholder signatures as attestations and the crate's tests ran nowhere"]

[[feature]]
id = "X3-GPU-014"
name = "x3-accel backend abstraction"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 85
tested = 75
mainnet_ready = 60
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Real accelerator coverage is incomplete"]
evidence = ["crates/x3-accel/src/lib.rs", "note: AccelBackend trait with CPU truth and fail-closed unavailable backend"]

[[feature]]
id = "X3-GPU-015"
name = "Benchmark regression CI gate"
subsystem = "gpu_performance"
source = "master"
paths = [".github/workflows/benchmark-regression.yml"]
implemented = 85
tested = 75
mainnet_ready = 72
priority = "P1"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["Need benchmark corpus tied to launch SLOs"]
evidence = [".github/workflows/benchmark-regression.yml", "note: Exact-head benchmark regression workflow is active"]

[[feature]]
id = "X3-GPU-016"
name = "GPU Blake2b batching"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 20
tested = 10
mainnet_ready = 10
priority = "P2"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["No verified accelerator kernel"]
evidence = ["crates/x3-accel/src/lib.rs", "note: WGPU backend currently KernelUnavailable"]

[[feature]]
id = "X3-GPU-017"
name = "GPU Merkle-root acceleration"
subsystem = "gpu_performance"
source = "master"
paths = ["crates/x3-accel/src/lib.rs"]
implemented = 20
tested = 10
mainnet_ready = 10
priority = "P2"
confidence = "high"
launch_scope = "guarded"
claim_risk = false
blockers = ["No verified accelerator kernel"]
evidence = ["crates/x3-accel/src/lib.rs", "note: WGPU backend currently KernelUnavailable"]

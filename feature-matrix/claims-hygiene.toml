# Claims / Hygiene feature matrix fragment.

[[feature]]
id = "X3-CLAIM-001"
name = "Million-TPS GPU claims"
subsystem = "claims_hygiene"
source = "claim_risk"
paths = ["GPU_VALIDATOR_HONEST_AUDIT.md", "docs/testnet-config/RELEASE-NOTES.md"]
implemented = 55
tested = 25
mainnet_ready = 35
priority = "P0"
confidence = "high"
launch_scope = "research"
claim_risk = true
blockers = [
  "The current-performance wording is gone: `docs/testnet-config/RELEASE-NOTES.md` announced a released `solana-gpu-validator-v1.0.tar.gz` with \"Achieved: 2.75M TPS in lab, 1-5M TPS on testnet\" and a \"Guarantee: Minimum 100k TPS\", none of which exists in this repository (no `.cu`/`.ptx` file, no tarball, no TPS benchmark of any kind). It now states what is real, marks the figures as targets, and names the two orphan result files (`day10-validation-results.json`, `day10-hotfix-results.json`) that nothing here produces — TICKET-099",
  "What remains is not wording: no GPU benchmark can run on this host (no compute device) and no GPU artifact is published, so any acceleration claim is still unverifiable here",
]
evidence = ["GPU_VALIDATOR_HONEST_AUDIT.md", "docs/testnet-config/RELEASE-NOTES.md", ".ai/reports/gpu-claims-hygiene-20260922.md", "note: Honest GPU audit says no real GPU finalized-TPS benchmark; the release note that contradicted it has been rewritten and the orphan result artifacts are ticketed"]

[[feature]]
id = "X3-CLAIM-002"
name = "MEV-proof marketing claim"
subsystem = "claims_hygiene"
source = "claim_risk"
paths = ["CURRENT_MAINNET_STATUS.md"]
implemented = 20
tested = 10
mainnet_ready = 10
priority = "P0"
confidence = "high"
launch_scope = "research"
claim_risk = true
blockers = ["Rename to MEV-resistant architecture under development"]
evidence = ["CURRENT_MAINNET_STATUS.md", "note: No complete fair-ordering/encrypted mempool proof"]

[[feature]]
id = "X3-CLAIM-003"
name = "Cross-chain complete claim"
subsystem = "claims_hygiene"
source = "claim_risk"
paths = ["reports/atomic_crossvm_completion_audit.md"]
implemented = 45
tested = 30
mainnet_ready = 30
priority = "P0"
confidence = "high"
launch_scope = "research"
claim_risk = true
blockers = [
  "Distinguish internal cross-VM from external-chain settlement",
  "**2026-09-26, corrected — the scoreboard fed the claim.** `AdapterScoreboard` reports each adapter's self-declared `readiness_score()` verbatim, and fourteen adapters declared `event_proof_extraction`, `finality_proof`, `rpc_indexer_support` and `proof_ledger_integration` true while building their lock, claim and refund proofs from `mock_tx_id(..)`, a \"Simulated block number\" and literal \"mock proof\" payloads, with `finality_status` returning a block hash derived from the number 42. All fourteen are corrected now: every adapter whose `finality_status` returns a constant proof declares `finality_proof: false`, the five that declared `proof_ledger_integration` without a ledger write declare it false, and the tests that pinned the inflated totals moved with them (move_vm and bitcoin 80 -> 60, cosmwasm 80/100/80 -> 60/80/60, wasm_l1 90 -> 70, and the rest 70 -> 60). `security/adapter-readiness-claims-baseline.txt` is empty, so `scripts/ci/check-adapter-readiness-claims.py` is a plain gate now rather than a ratchet; `crates/x3-atomic-swap/tests/adapter_readiness_truth.rs` pins the EVM and SVM cases and the integration suite asserts the honest overall rather than `>= 70`. Measured load-bearing: re-declaring `finality_proof: true` on the EVM adapter fails both the checker and the test.",
]
test_evidence = [
  "note: `test x3-atomic-swap adapter readiness` runs crates/x3-atomic-swap/tests/adapter_readiness_truth.rs; the `adapter readiness claims` gate runs the checker, whose baseline is empty as of 2026-09-26",
  "note: the fourteen adapters' own `test_readiness_score` tests were updated with the declarations (they pinned the inflated scores and the `missing_items()` counts), and `test_e2e_multi_vm_atomic_swap_lifecycle` now asserts the scoreboard reports `finality_proof` missing instead of `overall_score >= 70`",
]
evidence = ["reports/atomic_crossvm_completion_audit.md", "note: Audit identifies simulated adapters/placeholder proof verification in older path", "note: 2026-09-26 - the audit's finding was reproduced in code: the adapters' self-declared readiness was the score, and it claimed evidence they fabricate"]

# Cross-VM / Atomic feature matrix fragment.

[[feature]]
id = "X3-XVM-001"
name = "Settlement engine"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-settlement-engine/src/lib.rs"]
implemented = 75
tested = 55
mainnet_ready = 60
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs end-to-end external finality proof"]
evidence = ["pallets/x3-settlement-engine/src/lib.rs", "note: Real settlement pallet with timeout/atomic integration"]

[[feature]]
id = "X3-XVM-002"
name = "Pending-supply accounting"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 85
tested = 86
mainnet_ready = 68
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "**Two thirds closed 2026-09-25 — the ledger and the router are both proven, the external bridges are not.** The ledger's transitions are asserted for all four domain ids including the external one (`pallets/x3-supply-ledger/src/tests_conservation.rs`). The router's own suite, which wires the registry, the ledger and the router into one runtime, already proved the reconciliation and now runs in a gate: `test_x3_native_evm_svm_roundtrip_preserves_supply` (pending 50 while the leg is in flight, 0 once it completes, canonical ceiling unchanged, invariant checked at every point) and `test_failed_destination_credit_refunds_pending_supply` (an expired leg returns its pending supply to the source). `test x3-cross-vm-router` was added because those 81 tests ran in no gate list either. What the row still lacks is the half it named: each bridge's own observe-and-record path feeding those transitions with evidence from a real external chain, and the router's accounting reconciled under concurrent multi-validator traffic rather than in one externalities.",
]
required_tests = [
  "test_x3_native_evm_svm_roundtrip_preserves_supply",
  "test_failed_destination_credit_refunds_pending_supply",
]
test_evidence = [
  "note: both are in pallets/x3-cross-vm-router/src/tests.rs and run under the `test x3-cross-vm-router` gate added 2026-09-25 (2026-09-25)",
]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: Router invariant suite covers pending supply behavior"]

[[feature]]
id = "X3-XVM-003"
name = "Canonical supply preservation across routes"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 88
tested = 84
mainnet_ready = 72
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "**Re-derived 2026-09-25.** The claim this row makes — canonical supply is preserved across every route — is asserted by the router suite that wires the registry, ledger and router into one runtime: `test_x3_native_evm_svm_roundtrip_preserves_supply` walks the native -> EVM -> SVM round trip and checks the invariant and the canonical ceiling at each leg, and `test_all_six_internal_routes_succeed` drives all six internal route pairs with the ledger behind them. Both were ungated until 2026-09-25, when `test x3-cross-vm-router` was added to the fast set. Still open: the external (non-X3VM) route pairs are refused at genesis by design and their real-chain counterparts are untested here, and nothing reconciles the router under concurrent multi-validator traffic.",
]
required_tests = [
  "test_x3_native_evm_svm_roundtrip_preserves_supply",
  "test_all_six_internal_routes_succeed",
]
test_evidence = [
  "note: both are in pallets/x3-cross-vm-router/src/tests.rs and run under the `test x3-cross-vm-router` gate added 2026-09-25",
]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: required_tests includes canonical supply never breaks"]

[[feature]]
id = "X3-XVM-004"
name = "SHA-256 HTLC hash compatibility across VMs"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-settlement-engine/src/lib.rs"]
implemented = 90
tested = 65
mainnet_ready = 72
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs cross-language fixture proof for every adapter"]
evidence = ["pallets/x3-settlement-engine/src/lib.rs", "note: Code explicitly standardizes SHA-256 for EVM/SVM/X3 HTLC secrets"]

[[feature]]
id = "X3-XVM-005"
name = "EVM → SVM route"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs", "programs/svm/x3_atomic_swap/src/lib.rs", "scripts/cross-domain-svm-gate.sh"]
implemented = 90
tested = 88
mainnet_ready = 78
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "The SVM leg is proven against a real local validator and the deployed program, but nothing has been deployed to devnet or mainnet",
  "A production relayer cannot submit the proof yet: pallets/x3-settlement-engine requires the intent party (who == intent.maker || who == intent.taker, NotAuthorized otherwise), so the submitting authority is an open decision",
  "No external audit of the SVM program or of this route",
]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "programs/svm/x3_atomic_swap/src/lib.rs", "scripts/cross-domain-svm-gate.sh", "note: network-level external execution proof added 2026-09-22 — solana-test-validator + cargo build-sbf + the deployed SBF program, in dev and in strict posture (X3_STRICT_CROSS_DOMAIN_PROOFS=1, which refuses to start unless the chain reports the strict value)"]
test_evidence = ["node/tests/x3vm_svm_live.rs", "scripts/cross-domain-svm-gate.sh", "programs/svm/x3_atomic_swap/test-live-lifecycle.sh", "note: real_x3vm_svm_lock_claim_atomic_lifecycle and real_x3vm_svm_timeout_refund_atomic_lifecycle run against the booted validator; test-live-lifecycle.sh is 15 assertions, 0 failed on 2026-09-22, reading PDA state over raw JSON-RPC — wrong-preimage claim, double-claim, refund-before-timeout and refund-by-wrong-authority all rejected, claim and refund move the locked lamports"]

[[feature]]
id = "X3-XVM-006"
name = "Expired transfer refund"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 88
tested = 80
mainnet_ready = 78
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs multi-validator timeout/recovery proof"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: required_tests includes test_expired_transfer_refunds_to_source"]
required_tests = [
  "expired_transfer_refunds_source",
  "test_expired_transfer_refunds_to_source",
  "edge_expired_transfer_rejected",
  "test_cannot_cancel_before_expiry",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: expired_transfer_refunds_source, test_expired_transfer_refunds_to_source, edge_expired_transfer_rejected and test_cannot_cancel_before_expiry (a refund before expiry is refused); run by the `test x3-cross-vm-router` gate",
]

[[feature]]
id = "X3-XVM-007"
name = "External bridge circuit breaker"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 88
tested = 70
mainnet_ready = 78
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs live fault-trigger test"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: Audit notes bridge gate can flip false on invariant break"]

[[feature]]
id = "X3-XVM-008"
name = "Failed destination credit refund"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 88
tested = 80
mainnet_ready = 78
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs persistence/crash injection around refund path"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: required_tests includes failed_destination_credit_refunds_pending_supply"]
required_tests = [
  "test_failed_destination_credit_refunds_pending_supply",
  "failed_second_leg_rolls_back_first_leg",
  "ixl_abort_after_lock_restores_ledger",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: test_failed_destination_credit_refunds_pending_supply, failed_second_leg_rolls_back_first_leg and ixl_abort_after_lock_restores_ledger, which assert the pending supply returns to the source rather than being created or stranded; run by the `test x3-cross-vm-router` gate",
]

[[feature]]
id = "X3-XVM-009"
name = "SVM → EVM route"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs", "programs/svm/x3_atomic_swap/src/lib.rs", "scripts/cross-domain-svm-gate.sh"]
implemented = 90
tested = 88
mainnet_ready = 78
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "The SVM leg is proven against a real local validator and the deployed program, but nothing has been deployed to devnet or mainnet",
  "A production relayer cannot submit the proof yet: pallets/x3-settlement-engine requires the intent party (who == intent.maker || who == intent.taker, NotAuthorized otherwise), so the submitting authority is an open decision",
  "No external audit of the SVM program or of this route",
]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "programs/svm/x3_atomic_swap/src/lib.rs", "scripts/cross-domain-svm-gate.sh", "note: network-level external execution proof added 2026-09-22 — the same validator-backed lifecycle that covers EVM → SVM, run in dev and strict posture; the SVM program is the one the gate builds with cargo build-sbf and deploys"]
test_evidence = ["node/tests/x3vm_svm_live.rs", "scripts/cross-domain-svm-gate.sh", "programs/svm/x3_atomic_swap/test-live-lifecycle.sh", "note: the reverse direction of the same validator-backed lifecycle (15 assertions, 0 failed on 2026-09-22)"]

[[feature]]
id = "X3-XVM-010"
name = "Cross-VM message replay rejection"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 90
tested = 85
mainnet_ready = 82
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs distributed replay injection test"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: required_tests includes duplicate_message_replay_rejected"]
required_tests = [
  "test_duplicate_message_replay_rejected",
  "replay_message_rejected_no_state_change",
  "duplicate_message_id_rejected",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: test_duplicate_message_replay_rejected, replay_message_rejected_no_state_change and duplicate_message_id_rejected (the last two assert the ledger is untouched, not merely that an error was returned); run by the `test x3-cross-vm-router` gate",
]

[[feature]]
id = "X3-XVM-011"
name = "Cross-VM nonce replay rejection"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 90
tested = 85
mainnet_ready = 82
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs sustained adversarial replay testing across validators"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: required_tests includes duplicate nonce/message replay rejection"]
required_tests = [
  "test_duplicate_nonce_rejected",
  "six_internal_routes_strict_invariants_and_replay_guards",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: test_duplicate_nonce_rejected, plus six_internal_routes_strict_invariants_and_replay_guards, which re-runs every internal route and requires the replay guards to hold on all six; run by the `test x3-cross-vm-router` gate",
]

[[feature]]
id = "X3-XVM-012"
name = "EVM → X3 route"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 92
tested = 82
mainnet_ready = 82
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["External boundary proof remains separate"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: Native internal routing and supply tests"]
required_tests = [
  "six_internal_routes_strict_invariants_and_replay_guards",
  "test_all_six_internal_routes_succeed",
  "vm_adapter_six_routes_preserve_supply_and_clear_pending",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: six_internal_routes_strict_invariants_and_replay_guards drives DomainId::X3Evm -> DomainId::X3Native with ledger invarants checked on both sides, with test_all_six_internal_routes_succeed and vm_adapter_six_routes_preserve_supply_and_clear_pending covering the same route; run by the `test x3-cross-vm-router` gate",
]

[[feature]]
id = "X3-XVM-013"
name = "SVM → X3 route"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 92
tested = 82
mainnet_ready = 82
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["External boundary proof remains separate"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: Native internal routing and supply tests"]
required_tests = [
  "six_internal_routes_strict_invariants_and_replay_guards",
  "test_all_six_internal_routes_succeed",
  "xvm_router_svm_to_evm_full_round_trip",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: six_internal_routes_strict_invariants_and_replay_guards drives DomainId::X3Svm -> DomainId::X3Native with ledger invarants checked on both sides, with test_all_six_internal_routes_succeed and xvm_router_svm_to_evm_full_round_trip covering the same route family; run by the `test x3-cross-vm-router` gate",
]

[[feature]]
id = "X3-XVM-014"
name = "X3 → EVM route"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 92
tested = 82
mainnet_ready = 82
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["External boundary proof remains separate"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: Native internal routing and supply tests"]
test_evidence = [
  "note: named tests, measured 2026-09-26 — node/tests/x3vm_evm_live.rs runs real_x3vm_evm_lock_claim_atomic_lifecycle, real_x3vm_evm_timeout_refund_atomic_lifecycle, real_evm_header_attestation_populates_the_verifiers_anchor, real_evm_receipt_proof_is_accepted_against_the_attested_header, real_evm_a_claim_with_the_wrong_secret_is_refused, real_evm_a_second_claim_on_the_same_lock_is_refused and real_evm_an_early_refund_is_refused_and_succeeds_once_expired against anvil with a deployed AtlasHTLC and a real local X3 node; scripts/cross-domain-evm-gate.sh runs all seven (gate `cross-domain EVM`, PASS 224s) and the same gate with X3_STRICT_CROSS_DOMAIN_PROOFS=1 (PASS 334s)",
  "note: the contract-level half is X3-contracts/evm/test-live-lifecycle.sh (gate `EVM contract lifecycle`, 11 passed / 0 failed — double-claim rejected, premature refund rejected, wrong-authority refund rejected); it calls the contract directly, while the node-level tests above are the same refusals through the executor the product path uses",
]

[[feature]]
id = "X3-XVM-015"
name = "X3 → SVM route"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs", "programs/svm/x3_atomic_swap/src/lib.rs", "scripts/cross-domain-svm-gate.sh"]
implemented = 92
tested = 90
mainnet_ready = 82
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "The X3-side lock and the SVM-side lock are both exercised end to end against a real validator, but nothing has been deployed to devnet or mainnet",
  "A production relayer cannot submit the settlement proof yet: the settlement engine requires the intent party, so the submitting authority is an open decision",
  "No external audit",
]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "programs/svm/x3_atomic_swap/src/lib.rs", "scripts/cross-domain-svm-gate.sh", "note: the external boundary this row called separate is now exercised — X3VM lock + SVM HTLC lock/claim/refund against solana-test-validator with the deployed program, in dev and strict posture (2026-09-22)"]
test_evidence = ["node/tests/x3vm_svm_live.rs", "scripts/cross-domain-svm-gate.sh", "scripts/local-ci.sh", "note: gates cross-domain-svm and cross-domain-svm-(strict-posture) both PASS on 2026-09-22"]

[[feature]]
id = "X3-XVM-016"
name = "Cross-VM atomic router"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 92
tested = 85
mainnet_ready = 88
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["External bridge path intentionally disabled at genesis"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: FEATURE_REGISTRY atomic_router = 88%; eight required tests listed"]
required_tests = [
  "six_internal_routes_strict_invariants_and_replay_guards",
  "test_all_six_internal_routes_succeed",
  "vm_adapter_six_routes_preserve_supply_and_clear_pending",
  "xvm_router_state_machine_legal_transitions_only",
  "compiled_x3_lang_gateway_path_routes_and_rejects_direct_unsigned",
  "unsigned_origin_cannot_use_x3_lang_router_entrypoints",
  "signed_user_cannot_spoof_vm_origin",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: six_internal_routes_strict_invariants_and_replay_guards, test_all_six_internal_routes_succeed, vm_adapter_six_routes_preserve_supply_and_clear_pending, xvm_router_state_machine_legal_transitions_only, and the origin guards compiled_x3_lang_gateway_path_routes_and_rejects_direct_unsigned / unsigned_origin_cannot_use_x3_lang_router_entrypoints / signed_user_cannot_spoof_vm_origin; run by the `test x3-cross-vm-router` gate",
]
registry_feature = "atomic_router"

[[feature]]
id = "X3-XVM-017"
name = "External bridge pause-at-genesis"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-cross-vm-router/src/lib.rs"]
implemented = 95
tested = 90
mainnet_ready = 90
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Operational governance activation process still required"]
evidence = ["pallets/x3-cross-vm-router/src/lib.rs", "note: ExternalBridgesEnabled defaults false and is dispatch-gated"]
required_tests = [
  "external_bridges_are_paused_at_genesis",
  "external_bridges_disabled_at_genesis",
  "test_external_route_rejected_in_mvp",
  "register_external_root_rejected_at_genesis",
  "enabling_external_bridges_requires_documented_audit_gate",
]
test_evidence = [
  "note: named tests, measured 2026-09-26 — pallets/x3-cross-vm-router/src/tests.rs: external_bridges_are_paused_at_genesis, external_bridges_disabled_at_genesis, test_external_route_rejected_in_mvp, register_external_root_rejected_at_genesis and enabling_external_bridges_requires_documented_audit_gate; run by the `test x3-cross-vm-router` gate",
]

[[feature]]
id = "X3-XVM-018"
name = "Adaptor-signature settlement primitive"
subsystem = "cross_vm_atomic"
source = "master"
paths = ["pallets/x3-settlement-engine/src/lib.rs"]
implemented = 60
tested = 35
mainnet_ready = 35
priority = "P1"
confidence = "medium"
launch_scope = "core"
claim_risk = false
blockers = ["Needs cryptographic audit and production-path proof"]
evidence = ["pallets/x3-settlement-engine/src/lib.rs", "note: Settlement code contains adaptor-signature secret extraction logic"]

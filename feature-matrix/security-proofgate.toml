# Security / ProofGate feature matrix fragment.

[[feature]]
# Provenance corrected 2026-09-22: PR #166 merged 2026-09-17; recorded as master provenance on 2026-09-22
id = "X3-SEC-001"
name = "Reproducible srtool runtime build"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/production-gate.yml"]
implemented = 85
tested = 70
mainnet_ready = 65
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "Measured locally, 2026-09-22: `./scripts/update-runtime-hashes.sh` builds the runtime twice from scratch in the pinned srtool image and refuses to write unless the two builds agree byte for byte. Run three times in one session (`93fe86c9bd`, `e74bb199f9`, `269d611d96`, `93d97edd34`, `2a4296b630`), agreeing every time. What is still missing is reproduction by a party that is not this machine, and an end-to-end `make mainnet-check` on a release candidate",
  "Hosted `production-gate` workflow has run five times, all `workflow_dispatch`, all cancelled and never green (2026-09-19: 43m50s, 56m57s, 1h29m37s, 1h34m0s, 1h33m11s); the gate of record for this repository is local (`scripts/local-ci.sh`)",
]
evidence = ["scripts/run-srtool.sh", "scripts/update-runtime-hashes.sh", "docs/reports/runtime-wasm-hashes.json", ".github/workflows/production-gate.yml", "note: two independent srtool builds per attestation, compared field by field, with the record carrying the runtime version and metadata; PR #166 merged 2026-09-17; recorded as master provenance on 2026-09-22"]

[[feature]]
# Provenance corrected 2026-09-22: PR #166 merged 2026-09-17; recorded as master provenance on 2026-09-22
id = "X3-SEC-002"
name = "Runtime artifact hash evidence"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/production-gate.yml"]
implemented = 85
tested = 70
mainnet_ready = 65
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "`docs/reports/runtime-wasm-hashes.json` records the revision, image digest, compact/compressed sizes, set_code, authorize_upgrade, IPFS CID and BLAKE2-256, plus the runtime version and metadata the artifact reports; `scripts/check-runtime-hash-freshness.py` fails a change that touches the runtime's dependency graph without moving the record, and it is wired into `local-ci` (it fired on this session's runtime changes and passed after each re-attestation)",
  "Still missing: the release gate's own rebuild-and-compare (stage 6b) has not been run end to end on a release candidate, and no second party has reproduced the artifact",
  "Hosted `production-gate` workflow has run five times, all `workflow_dispatch`, all cancelled and never green (2026-09-19: 43m50s, 56m57s, 1h29m37s, 1h34m0s, 1h33m11s); the gate of record for this repository is local (`scripts/local-ci.sh`)",
]
evidence = ["docs/reports/runtime-wasm-hashes.json", "scripts/check-runtime-hash-freshness.py", "scripts/update-runtime-hashes.sh", ".github/workflows/production-gate.yml", "note: branch records commit, BLAKE2-256, SHA-256, WASM size/path; PR #166 merged 2026-09-17; recorded as master provenance on 2026-09-22"]

[[feature]]
id = "X3-SEC-003"
name = "Mainnet release audit script"
subsystem = "security_proofgate"
source = "master"
paths = ["scripts/mainnet_release_gate.py"]
implemented = 82
tested = 70
mainnet_ready = 65
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Must remain aligned with actual production build artifacts"]
evidence = ["scripts/mainnet_release_gate.py", "note: Checks docs, builds, specs, critical tests, srtool prerequisites, secrets"]

[[feature]]
# Provenance corrected 2026-09-22: PR #166 merged 2026-09-17; recorded as master provenance on 2026-09-22
id = "X3-SEC-004"
name = "Production gate"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/production-gate.yml"]
implemented = 88
tested = 75
mainnet_ready = 62
priority = "P0"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = [
  "The blocker this row carried — \"srtool hardening is on #166 branch, not master yet\" — is stale: #166 merged 2026-09-17 and the reproducible-build path is on master (`scripts/run-srtool.sh`, `scripts/update-runtime-hashes.sh`)",
  "What is actually missing: the production gate has never completed a full run. Hosted `production-gate` has five runs, all cancelled, longest 1h34m, none green; `scripts/mainnet_release_gate.py` exists (31 KB) and has not been run end to end on a release candidate either. The local gate set that *does* run is `scripts/local-ci.sh`, green 30/30 on `bae40d46c8` on 2026-09-22 — but it is not this gate",
]
evidence = ["scripts/mainnet_release_gate.py", "scripts/local-ci.sh", ".github/workflows/production-gate.yml", "note: runs guard/tests/release build/audit/node build/mainnet check; PR #166 merged 2026-09-17; recorded as master provenance on 2026-09-22"]

[[feature]]
id = "X3-SEC-005"
name = "Formal verification workflow"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/formal-verification.yml"]
implemented = 65
tested = 50
mainnet_ready = 45
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Selected proof coverage only; K/toolchain reliability historically required repair"]
evidence = [".github/workflows/formal-verification.yml", "note: Formal verification CI exists"]

[[feature]]
id = "X3-SEC-006"
name = "CodeQL analysis"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/codeql.yml"]
implemented = 75
tested = 65
mainnet_ready = 60
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Not all languages/modes may be covered"]
evidence = [".github/workflows/codeql.yml", "note: CI recovery retained supported CodeQL modes"]

[[feature]]
id = "X3-SEC-007"
name = "ProofForge gates"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/proof-gates.yml"]
implemented = 78
tested = 65
mainnet_ready = 60
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Coverage does not equal whole-protocol formal proof"]
evidence = [".github/workflows/proof-gates.yml", "note: Automated proof verification workflow exists"]

[[feature]]
id = "X3-SEC-008"
name = "Test-cheat / placeholder detection"
subsystem = "security_proofgate"
source = "master"
paths = ["scripts/x3-detect-test-cheats.sh"]
implemented = 75
tested = 60
mainnet_ready = 60
priority = "P1"
confidence = "medium"
launch_scope = "core"
claim_risk = false
blockers = ["Needs broad language coverage and CI enforcement"]
evidence = ["scripts/x3-detect-test-cheats.sh", "note: GPU audit references anti-cheat proof tooling"]

[[feature]]
id = "X3-SEC-009"
name = "Feature registry truth source"
subsystem = "security_proofgate"
source = "master"
paths = ["FEATURE_REGISTRY.toml"]
implemented = 85
tested = 70
mainnet_ready = 65
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Still incomplete as full inventory; several subsystems untracked"]
evidence = ["FEATURE_REGISTRY.toml", "note: Registry corrected fictional rows and scores"]

[[feature]]
id = "X3-SEC-010"
name = "Forbidden-secret scanning"
subsystem = "security_proofgate"
source = "master"
paths = ["scripts/mainnet_release_gate.py"]
implemented = 80
tested = 65
mainnet_ready = 65
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Regex scanning is supplementary, not a full secret-management system"]
evidence = ["scripts/mainnet_release_gate.py", "note: Mainnet gate scans private keys/mnemonics/AWS-like keys"]

[[feature]]
id = "X3-SEC-011"
name = "PR supervisor gate"
subsystem = "security_proofgate"
source = "master"
paths = ["scripts_infrastructure/pr_supervisor.py", "tests/test_pr_supervisor.py"]
implemented = 80
tested = 70
mainnet_ready = 65
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs continued exact-head enforcement"]
# The hosted workflow this row used to name does not exist: automatic hosted CI was
# removed from this repository, and the supervisor is the script below (with its
# own test). Naming a workflow that is not in `.github/workflows` was an evidence
# path that could not back the claim.
evidence = ["scripts_infrastructure/pr_supervisor.py", "tests/test_pr_supervisor.py", "note: Recent CI recovery added real PR integrity checks; enforcement is the script plus its test, run where the queue is operated"]

[[feature]]
id = "X3-SEC-012"
name = "Readiness consistency checker"
subsystem = "security_proofgate"
source = "master"
paths = ["scripts/check-readiness-consistency.sh"]
implemented = 85
tested = 75
mainnet_ready = 70
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Needs continuous maintenance as feature registry evolves"]
evidence = ["scripts/check-readiness-consistency.sh", "note: Repo uses it to reject fictional registry paths/tests"]

[[feature]]
id = "X3-SEC-013"
name = "Semgrep scanning"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/semgrep.yml"]
implemented = 85
tested = 78
mainnet_ready = 72
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Ruleset coverage needs periodic review"]
evidence = [".github/workflows/semgrep.yml", "note: Exact-head Semgrep checks active"]

[[feature]]
id = "X3-SEC-014"
name = "Trivy scanning"
subsystem = "security_proofgate"
source = "master"
paths = [".github/workflows/trivy.yml"]
implemented = 85
tested = 78
mainnet_ready = 72
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Keep filesystem scope current"]
evidence = [".github/workflows/trivy.yml", "note: Exact-head Trivy checks active"]

[[feature]]
id = "X3-SEC-015"
name = "Rust Clippy hard-fail gate"
subsystem = "security_proofgate"
source = "master"
paths = ["scripts/local-ci.sh", "Makefile"]
implemented = 88
tested = 80
mainnet_ready = 75
priority = "P1"
confidence = "high"
launch_scope = "core"
claim_risk = false
blockers = ["Keep -D warnings and avoid coverage weakening"]
# The hosted `rust-clippy.yml` this row used to name does not exist. Clippy is a
# hard fail in `scripts/local-ci.sh` (gates `clippy workspace`,
# `clippy runtime rc1`, `clippy node rc1`, each `-D warnings`) and in the
# `x3-lang` workspace target in the Makefile.
evidence = ["scripts/local-ci.sh", "Makefile", "note: three workspace clippy gates with -D warnings, plus the x3-lang workspace target; verified PASS on 2026-09-22 (clippy workspace 774s, clippy runtime rc1 803s, clippy node rc1 1009s)"]
test_evidence = ["scripts/local-ci.sh", "note: the gates themselves are the evidence; each runs cargo clippy with -D warnings over a workspace and fails the run on any warning"]

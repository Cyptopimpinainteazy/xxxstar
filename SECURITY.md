# Security Policy

- Never commit secrets, private keys, mnemonics, or RPC tokens.
- Critical path changes require tests and invariant coverage.
- Security findings must be tracked and resolved before mainnet promotion.

Run `make audit` for baseline automated checks.
# Shared UI: ChainStatusBanner

### Added
- `ChainStatusBanner` component — a colorful, animated banner with emoji and gradient text for showing chain status in wallets and explorer apps.

### Notes
- Hook into the shared component by importing from `@x3-chain/shared/components`.
- The wallet and explorer pages were wired to show the banner. Additional apps can import and use it.

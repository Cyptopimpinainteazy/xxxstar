# Monitoring

The dashboard exposes live metrics at `/metrics.json`:

- `svm_tps`, `evm_tps`
- `atomic_success_rate`, `atomic_rollbacks`
- `pending_swaps`
- `gpu_health`
- `svm_rpc_latency_ms`, `evm_rpc_latency_ms`

Use the UI in `dashboard/` for live updates.

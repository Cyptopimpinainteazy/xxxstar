// Test: cross vm - atomic operation stub
agent TestDual {
    context { chains: [evm, svm] }
    strategy bridge_test() {
        atomic fn commit_pair() {
            evm::swap(...);
            svm::swap(...);
        }
    }
}

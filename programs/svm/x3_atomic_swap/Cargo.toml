[package]
name = "x3-atomic-swap-solana"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "Solana BPF program for X3 Atomic Swap HTLC"
publish = false

[lib]
name = "x3_atomic_swap"
crate-type = ["lib", "cdylib"]

[dependencies]
solana-program = "=3.0.0"

[dev-dependencies]
solana-program-test = "=3.0.0"
solana-sdk = "=3.0.0"

[features]
no-entrypoint = []
test-sbf = []

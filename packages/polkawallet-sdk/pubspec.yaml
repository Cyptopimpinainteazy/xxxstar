name: polkawallet_plugin_atlas
description: Polkawallet plugin for X3 Chain x3chain - x3vm, atomic trades, cross-chain settlement, .x3 domains
version: 0.1.0
homepage: https://github.com/x3-chain/polkawallet-plugin-x3

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  polkawallet_sdk: ^0.6.0
  polkawallet_ui: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.7.0

flutter:
  assets:
    - lib/js_service_atlas/

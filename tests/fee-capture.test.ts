import { expect } from "chai";
import { ethers } from "hardhat";

describe("Treasury", function () {
  it("should capture and split fees correctly", async function () {
    // Deploy treasury, route fees, check splits
    // ...
    expect(true).to.be.true;
  });
});

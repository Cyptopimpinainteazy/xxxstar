# Files the provider-secret guard may ignore, one path per line.
#
# Nothing may be added here without saying *what* the value is and why it is not a secret. A real
# key belongs in the environment; a fixture that merely looks like one belongs here.
#
# `0x1234567890abcdef…` repeated — the end-to-end tests' signing key for a local chain that does not
# exist (`ws://localhost:9944`, chain id 9999). It is a placeholder: no such account is funded
# anywhere, and the tests never reach a network.
tests/e2e/utils/mod.rs
tests_phase4/e2e/utils/mod.rs

# The generated chain list is a public registry (2,505 entries) copied verbatim, and it carries a
# third-party Alchemy *demo* key published by that registry — not a credential of this project, and
# not ours to remove without regenerating the file from its source. If the generator is ever taught
# to strip keyed URLs, delete these two lines with it.
packages/blockchain-connector/src/chains/generated/chains.json
packages/blockchain-connector/src/chains/generated/index.ts

# A crawler's recorded state and a built bundle. Both contain other people's endpoints, captured or
# compiled rather than configured: the crawler stored what it found, and `dist/` is output. They are
# allowed here because they are not credentials of this project, and TICKET-103 is to stop tracking
# build output and crawler state at all, which removes the values as a side effect.
infra-structure/services/rpc-crawler/crawler_state.json
packages/blockchain-connector/dist/chains/generated/index.js

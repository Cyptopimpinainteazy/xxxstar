[package]
name = "pallet-x3-sentinel"
version = "0.1.0"
description = "On-chain Sentinel guard — governance freeze/review gate over the Universal Asset Kernel token-factory mint authority"
authors = ["X3 Chain Core Engineering <engineering@x3-chain.io>"]
edition = "2021"
license = "Apache-2.0"
publish = false

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[lib]
name = "pallet_x3_sentinel"
crate-type = ["rlib"]

[dependencies]
frame-support = { workspace = true, default-features = false }
frame-system = { workspace = true, default-features = false }
parity-scale-codec = { workspace = true, default-features = false, features = ["derive", "max-encoded-len"] }
scale-info = { workspace = true, default-features = false, features = ["derive"] }
sp-core = { workspace = true, default-features = false }
sp-io = { workspace = true, default-features = false }
sp-runtime = { workspace = true, default-features = false }
sp-std = { workspace = true, default-features = false }

x3-asset-kernel-types = { path = "../../crates/x3-asset-kernel-types", default-features = false }

[dev-dependencies]
sp-core = { workspace = true, default-features = false, features = ["std"] }
sp-io = { workspace = true, default-features = false, features = ["std"] }
sp-runtime = { workspace = true, default-features = false, features = ["std"] }

[features]
default = ["std"]
std = [
    "frame-support/std",
    "frame-system/std",
    "parity-scale-codec/std",
    "scale-info/std",
    "sp-core/std",
    "sp-io/std",
    "sp-runtime/std",
    "sp-std/std",
    "x3-asset-kernel-types/std",
]
try-runtime = [
    "frame-support/try-runtime",
    "frame-system/try-runtime",
]
runtime-benchmarks = [
    "frame-support/runtime-benchmarks",
    "frame-system/runtime-benchmarks",
]

[package]
name = "pallet-x3-cross-vm-router-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

# Exclude from root workspace to allow nightly fuzz builds
[workspace]

[dependencies]
libfuzzer-sys = "0.4"
parity-scale-codec = { version = "3", features = ["derive"] }
sp-core = { default-features = false, git = "https://github.com/paritytech/polkadot-sdk", tag = "polkadot-stable2603" }

[dependencies.pallet-x3-cross-vm-router]
path = ".."

[[bin]]
name = "router_transfer_validation"
path = "fuzz_targets/router_transfer_validation.rs"
test = false
doc = false
bench = false

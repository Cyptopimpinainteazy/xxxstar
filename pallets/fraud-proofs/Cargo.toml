[package]
name = "pallet-fraud-proofs"
version = "0.1.0"
authors = ["X3 Chain Core Team <core@x3-chain.io>"]
description = "Stub — fraud-proof FRAME pallet is implemented inline in x3-chain-runtime to avoid circular deps."
edition = "2021"
license = "Apache-2.0"
publish = false

# NOTE: The actual pallet logic lives in runtime/src/fraud_proofs/pallet.rs
# and is compiled as part of x3-chain-runtime.
# This stub keeps the workspace member entry valid for tooling.


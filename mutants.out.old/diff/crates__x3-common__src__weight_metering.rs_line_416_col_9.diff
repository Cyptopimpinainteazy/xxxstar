--- crates/x3-common/src/weight_metering.rs
+++ replace WeightMeter::reset with ()
@@ -408,18 +408,17 @@
 
     /// Get the config
     pub fn config(&self) -> &WeightConfig {
         &self.config
     }
 
     /// Reset the meter
     pub fn reset(&mut self) {
-        self.compute.reset();
-        self.gas.reset();
+        () /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// Operation type for weight metering
 #[derive(Debug, Clone, Copy, PartialEq, Eq)]
 pub enum Operation {
     /// Signing operation
     Sign(KeyType),

--- crates/x3-common/src/lib.rs
+++ delete match arm "for" in Keyword::parse
@@ -83,17 +83,17 @@
         match src {
             "fn" => Some(Self::Fn),
             "let" => Some(Self::Let),
             "mut" => Some(Self::Mut),
             "if" => Some(Self::If),
             "else" => Some(Self::Else),
             "while" => Some(Self::While),
             "loop" => Some(Self::Loop),
-            "for" => Some(Self::For),
+             /* ~ changed by cargo-mutants ~ */
             "return" => Some(Self::Return),
             "break" => Some(Self::Break),
             "continue" => Some(Self::Continue),
             "struct" => Some(Self::Struct),
             "enum" => Some(Self::Enum),
             "match" => Some(Self::Match),
             "true" => Some(Self::True),
             "false" => Some(Self::False),

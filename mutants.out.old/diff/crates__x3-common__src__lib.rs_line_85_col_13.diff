--- crates/x3-common/src/lib.rs
+++ delete match arm "let" in Keyword::parse
@@ -77,17 +77,17 @@
     Const,
     In,
 }
 
 impl Keyword {
     pub fn parse(src: &str) -> Option<Self> {
         match src {
             "fn" => Some(Self::Fn),
-            "let" => Some(Self::Let),
+             /* ~ changed by cargo-mutants ~ */
             "mut" => Some(Self::Mut),
             "if" => Some(Self::If),
             "else" => Some(Self::Else),
             "while" => Some(Self::While),
             "loop" => Some(Self::Loop),
             "for" => Some(Self::For),
             "return" => Some(Self::Return),
             "break" => Some(Self::Break),

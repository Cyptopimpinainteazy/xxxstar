--- crates/x3-common/src/weight_metering.rs
+++ replace ComputeMeter::remaining -> u64 with 1
@@ -253,17 +253,17 @@
             return Err(WeightError::BudgetExceeded(total, budget));
         }
         self.consumed = total;
         Ok(())
     }
 
     /// Get remaining units
     pub fn remaining(&self) -> u64 {
-        self.limit.saturating_sub(self.consumed)
+        1 /* ~ changed by cargo-mutants ~ */
     }
 
     /// Get consumed units
     pub fn consumed(&self) -> u64 {
         self.consumed
     }
 
     /// Get the limit

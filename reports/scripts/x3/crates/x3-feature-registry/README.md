# X3 Feature Registry

This directory is a placeholder for feature registry tooling used by the X3 readiness engine.

It should eventually include:
- feature registry parsers
- proof metadata generators
- report bindings for readiness scoring

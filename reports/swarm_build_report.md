# X3 Swarm Build Report

Generated by scripts/swarm/yolo_build_swarm.sh

Required components:
- crates/x3-swarm-core
- crates/x3-swarm-core/services/x3-swarm-api
- crates/x3-swarm-core/services/x3-swarm-worker
- scripts/swarm
- scripts/gpu
- data/agent-memory
- apps/tauri-os/src/apps/SwarmCommand
- reports/swarm_task_queue.json
- reports/swarm_build_report.md

Hard safety:
- Mainnet edits blocked
- BTC mainnet blocked
- External bridge mainnet blocked
- Keys and .env blocked
- Runtime/pallet/bridge edits require approval

---

## Honest status (09-03 lojak recon build pass)

Mapping the spec against the repo showed the swarm build is **already largely implemented** (not greenfield): x3-swarm-core (`agent/approval/guard/permissions/policy/authority/scheduler/scoreboard/memory/audit/events/misconduct/spawn/genesis/task/report`, `tests/guard_tests.rs`), x3-swarm-api (axum), x3-swarm-worker, swarm+GPU scripts, SwarmCommand Tauri app, task queue. This pass did NOT overwrite existing code (no git history; irreversibility).

### Components verified present (on disk)
| Component | Present |
|---|---|
| x3-swarm-core crate | yes |
| x3-swarm-api / x3-swarm-worker bins | yes |
| scripts/swarm (approve_task/health/scan/self_test/task_queue/up/down/report/yolo) | yes |
| scripts/gpu (inventory/workers/soak) | yes |
| apps/tauri-os/.../SwarmCommand | yes |
| reports/swarm_task_queue.json | yes |
| data/agent-memory | NOT PRESENT on disk (dir created only at runtime by yolo script) |

### BLOCKER (root-workspace wiring) — recorded 09-03
Attempted to wire `crates/x3-swarm-core` + its two `services/*` into the root
`Cargo.toml` `members` so the orchestrator's `cargo test -p x3-swarm-core`
(node from repo root) actually resolves. `cargo metadata --no-deps` passed, but a
full `cargo test -p x3-swarm-core` from root triggered a complete workspace
resolve that surfaced a **PRE-EXISTING, unrelated** dependency break in `node/`:
`sc-consensus-grandpa` is locked to `0.40.0` but the git source
`paritytech/polkadot-sdk?branch=stable2512` only offers `0.40.1`. This is NOT
caused by the swarm work and is not a swarm-crate problem.

Recovery: reverted root `Cargo.toml` byte-identical to the pre-change snapshot
(see `.pre-edit-snapshot/Cargo.toml.before-swarm-wire`). No lasting change.

Status of swarm onboarding into the root workspace: **BLOCKED on an upstream
node/polkadot-sdk resolution issue** — fixing it requires editing `node/`
and/or lockfile/git re-pinning, which is approval-gated territory per the spec.

## Resolution: standalone workspace (09-03)
Made `crates/x3-swarm-core` its own standalone cargo workspace (empty `[workspace]`
with members = the two `services/*`; `resolver = "2"`) so the swarm crates build and
test independently WITHOUT touching the parent substrate workspace graph. This follows
cargo's own suggested fix and avoids the node/`sc-consensus-grandpa` root-resolve blocker.
Per-file snapshot: `.pre-edit-snapshot/x3-swarm-core-Cargo.toml.before`.
Running `cargo test -p x3-swarm-core` from the crate dir to record pass/fail.

### VERIFIED RESULTS (09-03, standalone workspace fix)
- `cargo test -p x3-swarm-core` (from `crates/x3-swarm-core`): **54 unit tests + 15
  integration tests passed, 0 failed (exit 0)** — covers guard (blocks env/keys/mainnet;
  approval for runtime/pallets/bridge/btc_gateway), genesis, misconduct, policy
  (governance/human/security review), spawn.
- `cargo build -p x3-swarm-api -p x3-swarm-worker`: **both compile (exit 0)**.
- API smoke run: x3-swarm-api served `/health` => `{"status":"ok","mode":"GUARDED_TESTNET",
  "agents_enabled":true,"kill_switch":false}` — correctly reports guarded testnet mode,
  NOT mainnet. Killed after probe; port 8787 clear.
- Root `Cargo.toml` / `Cargo.lock`: UNCHANGED (byte-identical to snapshots).
- No `.env`, keys, or blocked material read or edited.


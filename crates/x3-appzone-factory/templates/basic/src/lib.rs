//! Minimal app zone produced by the `basic` template.
//!
//! The factory copies this template into a new directory and then initialises
//! and deploys it; keep the four files `validate_app_zone` requires
//! (`Cargo.toml`, `src/lib.rs`, `app-config.toml`, `pallets.toml`) in sync with
//! `AppZoneFactory::validate_app_zone`.

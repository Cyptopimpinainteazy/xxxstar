// 10: Forward references (function calling function defined later)
fn main() {
    let result = helper(10);
    return result;
}

fn helper(n) {
    if (n < 2) {
        return 1;
    }
    return n * helper(n - 1);
}

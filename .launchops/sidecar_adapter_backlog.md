# Generated Sidecar Adapter Backlog

This file is generated from rpc_consumer_contracts.json. Do not hand-edit it.

Generated at: 2026-05-30T11:04:51.375842667+00:00

| Route | Method | Reason | Ownership Note | Notes |
| --- | --- | --- | --- | --- |
| `explorer` | `wallet_backupWallet` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_createWallet` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_getBalance` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_getNetworks` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_getTransactions` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_getWalletStatus` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_importWallet` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_listWallets` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_setNetwork` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_signTransaction` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |
| `explorer` | `wallet_submitTransaction` | No route-specific frontend-safe contract exists yet, so keep this behind sidecar ownership until the consumer surface freezes. | - | - |

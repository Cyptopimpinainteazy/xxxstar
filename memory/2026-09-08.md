# Session Memory — 2026-09-08

Pre-compaction durable capture for the X3 Atomic Star repo. <!-- project: path:memory/2026-09-08.md -->

## Authoritative scope & status docs (read these before acting)
- `LAUNCH_SCOPE.md` v1.1 (2026-06-10) = THE single authoritative scope doc. Supersedes README, `CURRENT_MAINNET_STATUS.md`, old `MAINNET_RC1_SCOPE.md`.
- `docs/current/READINESS_GRAPH.md` — 3-milestone completion graph (committed `c87f25a3`).
- `docs/current/MAINNET_GAMEPLAN.md` — week-by-week path to mainnet (committed `5d411dc0`).
- `docs/current/MASTER_CHECKLIST_STATUS.md` — external v1.0.0 checklist mapped to real repo layout, live-verified statuses (committed `c81363e5`/`5ae43f96`).
- `grants/GRANTS_DATABASE.md` — grant/sponsor/in-kind prospect DB, ~80 rows across C1–C12 categories, all URLs/eligibility/awards live-verified by 3 research agents (committed; last header update 2026-09-05).
- `TREASURY_POLICY.md` — governs EVM treasury + credential/key rotation policy.

## Milestone completion (honest, live-verified)
- **M1 Internal Staged Testnet (RC-1): ~85–90%.** Gaps: FRAME benchmarks only 5 of 42 x3-pallets (`x3-atomic-kernel`, `x3-inventory`, `x3-settlement-engine`, `x3-slash`, `cross-chain-validator`); 37 pallets need `benchmarks!`; signed release+SBOM not built; no `proptest!` state-machine tests for the 6-route atomic-swap matrix; multi-host mesh proof is LOOPBACK-ONLY (`.testnet-audit/run1`), needs 3+ physical hosts.
- **M2 Bridge Testnet: ~55–60%.** Architecture crates exist (`x3-crosschain-gateway`, `x3-finality-oracle`, `x3-relayer`, `x3-gateway-indexer`) but chain-specific finality proofs (ETH/SOL), production bridge adapters, and deployment are unbuilt. External audit NOT engaged — the hard blocker.
- **M3 Public Mainnet: ~35–40%.** No permissionless staking/tokenomics (deferred per LAUNCH_SCOPE), no multisig validator admission, no genesis ceremony, no external audit, no public soak.

## Verified DONE (claims that external deep-research reports wrongly said were missing — reports were stale)
- `x3-packet-standard` crate EXISTS (3 refs in router).
- `x3-ixl` crate EXISTS (3 refs in router).
- CrossVM atomicity proven (commit `7ab046a1`, `62aa66ba` gas-invariant fix).
- Sentinel guard closing fictional `x3_sentinel` gap (commit `55028fff`).
- 169 EVM forge tests green / 12 suites / 4096-run fuzz (commit `2e6efe03`).
- No "100% production" claims remain in `CURRENT_MAINNET_STATUS.md`; MAINNET_RC1_SCOPE removed; 52 obsolete docs purged (commit `9322e41f`).
- Relayer uses REAL SCALE-encoded signed extrinsics (`crates/x3-relayer/src/submitter.rs:196,584-596,658`) — not JSON pseudo-extrinsics.
- Launchpad graduation tests added this session (21→25 tests) — commit c87f25a3.
- Cargo audit clean (`a64846c3`); `deny.toml` fixed (removed deprecated `severity-threshold`); crossbeam-epoch 0.9.18→0.9.20; `.cargo/audit.toml` ignores documented with justification; x3-vrf now `license.workspace=true`; GPL-3.0-only allowed for xcm-procedural build-dep.
- Deterministic build: `scripts/run-srtool.sh` (image pinned `paritytech/srtool:1.75.0`) + evidence in `launch-gates/evidence/substrate/`.

## OPEN security debt / blockers (must survive compaction)
- **EVM treasury Safe gate** (`MASTER_CHECKLIST_STATUS.md` §10, `TREASURY_POLICY.md` §1/§2): `X3-contracts/evm/contracts/treasury/Treasury.sol` uses single-owner OpenZeppelin `Ownable` + open `routeFee` (callable by anyone). MUST resolve (Safe owner / Safe-mirror / permissioned router) BEFORE first funded ETH/USDC route. Not a go-blocker while EVM treasury stays unfunded.
- **3 repo key rotations before ANY next git tag** (`TREASURY_POLICY.md` §3): (1) `deployment/keys/validator-0{1,2,3}-summary.txt` — AURA+GRANDPA seeds, TRACKED in git → rotate AND `git filter-branch`/BFG history-purge. (2) `sepolia-deployer-wallet.txt` plaintext key in repo root, TRACKED → rotate address + purge. (3) `deployment/chain-specs/fresh/validator-keys/validator-{1..7}.suri` 7 raw seeds TESTNET-ONLY, gitignored/untracked → rotate locally + move out of repo, no history purge needed.
- **API key rotation** — DeepSeek `sk-...` + GitHub PAT surfaced in earlier transcript; still open.
- Repro PRIME-DIRECTIVE note: repo forbids fake adapters/relayers/proofs/no-op paths/placeholder logic; run `cargo check --workspace`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `pnpm test/build`, `npm test`, `python -m pytest`, plus the grep fake-code scan before claiming completion.

## Grants pipeline — state & P0 actions (see `grants/GRANTS_DATABASE.md`)
- 3 research agents (cloud C1–C3, ecosystem C4–C7, audit-hardware C8–C12) all COMPLETED their passes on 2026-09-05; DB consolidated.
- **Live-verified corrections that changed earlier recommendations:** W3F general Grants Program DISCONTINUED (repo on GitHub remains; old URL redirects to homepage). Substrate Builder Program / Moonbeam / Astar / Polkadot Pioneers Prize all 404/defunct. Use **Polkadot OpenGov Treasury** (C4.2) instead — live, ~42 DOT deposit, referendum-approved.
- **Biggest audit-funding upside:** Arbitrum Audit Program (C5.2) — $10M/12-month pool.
- **Firm changes:** Spearbit merged into **Cantina** (cantina.xyz); Equinix Metal SUNSET 2026-06-30; Online.net → Scaleway Dedibox; OtterSec → osec.io; LexDAO dormant (use DLx Law / Anderson Kill); OVH/Oracle/IBM/Alibaba/Tencent startup-credit programs DEFUNCT (URLs 404).
- **P0 actions:** Polkadot OpenGov Treasury proposal; AWS Activate + GCP for Startups + Microsoft for Startups; Cloudflare free tier; DigitalOcean Startups. Engage Runtime Verification / Trail of Bits / Zellic / MixBytes for audit quotes. DLx Law for legal. Lock 3-host testnet: cheapest 3× Kimsufi or balanced 3× Hetzner CCX. See `grants/GRANTS_DATABASE.md` for current grant amounts and provider status (verified 2026-09-05).

## Review of session commit history (for cross-session continuity)
Session produced these root-level durable artifacts (all committed on `master`):
1. `docs/current/READINESS_GRAPH.md` + 4 launchpad graduation tests → commit `c87f25a3`
2. `docs/current/MAINNET_GAMEPLAN.md` → commit `5d411dc0`
3. `docs/current/MASTER_CHECKLIST_STATUS.md` + `grants/GRANTS_DATABASE.md` → commits `c81363e5`, `5ae43f96`
4. security hardening (audit/deny/license) → commit `a64846c3` (prior turn)
5. LP locker wiring fix context: earlier notable commit `1f27cff1` wired `token_getSupply`'s locked supply to real chain state.

## Correct repo paths (map external docs onto these)
No top-level `/runtime2 /vm /daemon /ai /sdk /cli /ui /tests/econ /ops`. Real layout: `runtime/`, `node/`, `pallets/` (42 x3-*), `crates/` (x3-vm, x3-chain-health-daemon, x3-agent, x3-court, x3-fees, x3-crosschain-gateway, x3-finality-oracle, x3-relayer, x3-svm-integration, etc.), `apps/` (tauri-os, x3-desktop, dashboard, explorer…), `X3-contracts/evm` + `X3-contracts/svm`, `tests/e2e/`. Branch is `master`, not `main`.

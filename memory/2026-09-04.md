# Memory 2026-09-04

## Trigger to remember
User's active prompt: **"fix it"** — refers to the P0 reliability finding: X3 testnet cold-start consensus convergence is INTERMITTENT in single-host local testing (forks into separate finalized sets; killing a validator partitions a 6/7 majority). "It" = make multi-validator testnet reliably converge + survive member loss. This is the open task to continue next session. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Repo state & commits this session
- Repo snapshot was UNVERSIONED; I ran `git init` + baseline commit. All work committed cleanly (baseline `091dbe3` → final `3f847f3d`); nothing left uncommitted except stray/experimental files removed. Every change reversible/auditable.
- Throwaway validator keys (SURIs/seeds) must NOT be committed. `.gitignore` covers `deployment/chain-specs/fresh/validator-keys/`, `*.suri`. Aura spec contains PUBLIC keys only.

## Proven (hard, reproducible) — do not re-derive
- **Multi-validator consensus WORKS when a connected majority forms**: 7/7 identical finalized heads; 2000/2000 system.remark txs finalized, 110.6 finalized TPS, 0 lost; canonical head identical on all 7 under sustained load.
- **Runtime GRANDPA correct when connected.** Failure is at P2P graph level under loopback/private-IP contention.

## Critical operational gotchas (the "how-to" for this node binary)
1. **Aura block authoring requires `X3_DEV_SEED=<suri>` env** at node start. File-only keystore injection (standard substrate `~/.local/share/x3/chains/<id>/keystore` files) does NOT reach the Aura worker on this binary. `maybe_insert_dev_keys`/`insert_dev_keys_with_seed` (service) is the only path. Also needed: `--unsafe-force-node-key-generation --validator --force-authoring --allow-private-ip`.
2. **Chain-spec**: build via env-gated `cargo run --release -- build-spec --chain=testnet --disable-log-color` with `X3_TESTNET_AUTHORITIES` etc. Fresh seeds, sr25519=aura authority, ed25519=grandpa authority from SAME master seed. Use PLAIN spec (NOT `--raw`) for node `--chain` — the node-side file validator rejects raw Live specs (no `runtimeGenesis.config` for its structural checks) even though authorities are in the trie. Env example: `X3_EVM_ESCROW_ADDR=0x<40hex>` `X3_SVM_ESCROW_ADDR=0x<64hex>`.
3. **subkey needed**: installed at ~/.cargo/bin; had to symlink libclang (`libclang.so`) to fix a build/run blocker. Verify `subkey inspect --scheme sr25519|ed25519`.
4. **CLI drift**: current binary rejects `--ws-port`/`--ws-external`, requires `--execution=native-else-wasm` (lowercase hyphen). Old harness `scripts/testnet/x3_testnet_up.sh` stale; fixed in place.
5. **Intra-host P2P**: dial addresses must be loopback `/ip4/127.0.0.1/tcp/P` for peers (0.0.0.0 is listen-only wildcard). No peers connect with `system_localPeerId`-discovered bootnode unless node1 settles first — boot-order race (wrap a readiness/settled wait).
6. `@polkadot/api` connects reliably when the mjs sits under `packages/ts-sdk/` (module resolution). Transactions: funding account = sr25519 of validator master seed (raw `0x<64hex>` SURI via `Keyring.addFromUri`), which genesis endows.

## P0 open problem details (what "fix it" = next session's work)
- Symptom: repeated cold-start of 7 validators sometimes converges cleanly, sometimes FORKS into ≥2 GRANDPA-finalized sets at same block height; killing one (leaf or bootnode) can partition a 6/7 majority into forking groups (seen: `{sj1,sj2,sj4}=0xb333` vs `{sj5,sj6,sj7}=0xf93d`; and solo-join run `{sj2,sj5}=0x42ed` vs rest `0xb4da`). Even the supposedly deterministic solo-lead-then-join recipe forked on a later identical run → NOT start-order-only; real P2P healing/GRANDPA-under-divergence issue on single-loopback host.
- GRANDPA itself only forbids forks if the ≥2/3 set sees ONE branch; when the peer graph partitions, subgroups can finalize different forks. Root-cause direction = resilient full mesh.
- **Proposed fix direction (NOT yet proven)**: reserved full-mesh. Constraint confirmed EMPIRICALLY: this binary REQUIRES `/p2p/<peerid>` suffix on `--reserved-nodes` (errors "Peer id is missing"). So need deterministic PeerIds from deterministic `--node-key`. To compute PeerId from an ed25519 libp2p node-key exactly (multihash of protobuf public key) is error-prone; derive via node itself.
- Alternative considered: multiple redundant bootnodes each connected — hit same race. Kademlia/public-addr DHT on pure 127.0.0.1 gives nothing to gossip.
- Honest bound: fully proving cross-host reserved-mesh resilience likely needs real networked hosts, not single loopback. Local empirical milestone = prove convergence across MANY cold-starts + survive arbitrary single-node kill on one host.

## Session artifacts (committed, reusable)
- `scripts/testnet/build-x3-testnet-spec.py` — generate spec from fresh master seeds.
- `scripts/testnet/run-fresh-validators.sh`, `run-solo-join.py` — single-bootnode bring-ups.
- `packages/ts-sdk/par-load.mjs`, `batch-remark.mjs` — finalized-TPS loaders (run with SURI/COUNT/LIMIT env, WS default ws://127.0.0.1:9950).
- Reference/ledger docs: `TESTNET_GAP_LEDGER.md` (GAP-P2P-1, GAP-CONSENSUS-REPRO-1, GAP-MESH-1 = open P0/P1s; GAP-CLI-1/Spec/Auth noted), `tests/testnet/TESTNET_VERIFICATION.md`, notes under `.testnet-audit/`.

## Environment
Runtime host x3star1, Linux 6.8. Workspace `/home/lojak/Desktop/xxxstar-main`. Dev net test loopback ports ~30533-30839 p2p / 9950-9990 rpc, logs under `/tmp/x3-*`. Binary `target/release/x3-chain-node`. slot/finality fast (~2-3s). Model deepseek-v4-flash.

## Next concrete step (recommended)
Implement deterministic-PeerId reserved full-mesh so 7 validators each `--reserved-nodes` all others, then: (a) N cold-starts all converge, (b) kill each node once → remaining ≥5 always keep ONE finalized chain. If loopback-bound, document across-host need and move to soak/stability characterization instead.

# Memory 2026-09-04 (APPEND — second session same day)

## Session #2 scope: clippy gate + JS test-fragment fixes (all committed, gates green)
A separate fix-batch session (unrelated to the P0 consensus task above). Summary: made `cargo clippy --workspace --all-targets -- -D warnings` EXIT 0 and cleared several red JS npm-test fragments named in a readiness scan. 8 logical commits; tree otherwise clean (only other active session's files untouched: GPU_VALIDATOR_HONEST_AUDIT.md, memory/*, DREAMS.md, scripts/testnet/*, .testnet-audit/*).

## Proven hard facts for THIS repo (do not re-derive)
- Full clippy gate now GREEN. Proof cmd: `cargo clippy --workspace --all-targets -- -D warnings` → exit 0, 0 errors. `cargo check --workspace` → exit 0. Only dependency-level note is upstream `uint v0.4.1` future-incompat (dependency, not our code; also present in the original scan's cargo check).
- Unit tests still pass for crates whose test code I touched: x3-kernel 210, x3-coin 31, x3-settlement-engine 114, atomic-trade-engine 48, x3-proof-carrying-agent 37, x3-foundry-core 41, parallel-proposer 23 (all `cargo test -p X --lib`). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
- JS fragments all pass now: x3-foundry-sdk build exit 0 + test 3 pass; atomic-swap-sdk test 25; x3-desktop arena vitest 27; apps/shared (2 suites/3); apps/wallet (1/9); polkawallet-plugin plugin jest 12. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Where the clippy lints lived (map for quick re-fix if CI re-enables on another toolchain)
- Test crates had ~165 `#[cfg(test)]` lints; swept via `cargo clippy --fix` then fixed residuals by hand (no test bodies weakened/dropped — e.g., property_tests execute_with blocks bound with `let _`, double-paren removed at H256::from_low_u64_be `((i+1))`, unused type alias/helper removed, `timestamp >= 0`→`!= 0` matched comment intent).
- **Library-source fixes worth remembering (they were the "do not ignore" ones):** four `Default::default()`-then-single-variant assignments → `#[derive(Default)]` + delegation via `new()` with explicit `next_id:1` (foundry-registry pools), and the x3-foundry-core error.rs test restructured to `#[test] -> Result<(), E>` with `?` instead of an `Ok(...).unwrap()`.
- **Runtime (node) crate reached only AFTER the pallet crates were fixed** (clippy aborts early → later crates only surface later). This is why each full-workspace run surfaced ONE more crate dozens of times. Fixes cascade crate-by-crate; expect to iterate if gate regressions appear.
- Frozen-API / SDK-deprecation lints handled with narrow documented `#[allow(deprecated)]`/`#[allow]` rather than behavior-changing ports: runtime `create_runtime_str!` (proc-macro grammar REQUIRES it; `.into()` breaks `#[sp_version::runtime_version]` — do NOT "fix" again), `CurrencyAdapter<Balances, DealWithFees>` in pallet_transaction_payment Config, the inline fraud-proof `Call` enum carrying large `FraudProofV1` (Boxing = breaking extrinsic encoding), and the EVM optional-feature runtime-API methods whose params are only used under `frontier` (canonical Substrate idiom: `#[allow(unused_variables)]` per method). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
- Removed the deprecated fraud-proof pallet `Config type RuntimeEvent` associated item + its impl bindings (pallet.rs + lib.rs Config impl) — SDK auto-generates it now; this is the sanctioned removal, not a hack. Also: fraud pallet has an intentional `pub mod pallet` nesting (module_inception) because lib references `crate::fraud_proofs::pallet::pallet`; keep the nesting, allow the lint.
- settlement tests.rs had a redundant nested `mod tests {}` wrapper inside a `#[cfg(test)] mod tests;` include → de-nested (large whitespace-only dedent diff) to clear `module_inception`. If re-adding includes, mount test file WITHOUT an inner `mod tests` wrapper.

## Node/JS wiring gotchas (from this session #2)
- apps/shared + apps/wallet + x3-foundry-sdk each need a self-contained ts-jest `jest.config.js` (inline tsconfig) to avoid Jest loading-cross-package issues. Shared needed `ts-jest` + `jest-environment-jsdom` dev deps added. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
- x3-desktop BlockStore cap must keep exactly MAX_RECENT_BLOCKS=30 → append-then-trim (do not drop-to-capacity, which loses one).
- atomic-swap-sdk has stale compiled `dist/**/__tests__` that vitest de-dupes on; added vitest.config.ts excluding node_modules/dist so only `src` tests run. Boundary assertions there are time-dependent; make them structural (timelock invariants), not same-wall-second `Date.now()`.
- polkawallet env.ts lacks `getEnvOptional`; declared `getEnvOptional` → `string | undefined` for X3_RPC_ENDPOINT. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
- `cargo clippy -p x3-chain-node --benches` ALONE can transiently fail with Criterion `to_async` E0599 (async feature not resolved in isolated target), but passes under the full `--all-targets` workspace invocation. Do not chase that isolated error.

## Remaining blockers recorded (unfinished follow-ups, mostly out of this session's scope)
1. `cargo test --workspace` end-to-end still has the pre-existing env-dependent e2e SIGKILL (`cross_vm_real_chain_test`) — needs idle-host re-run.
2. Frozen-API ports left as documented `#[allow]` and scheduled: CurrencyAdapter→FungibleAdapter (retype DealWithFees), Box fraud `Call` FraudProofV1 w/ migration, Criterion async feature wiring for isolated benches.
3. pnpm / python -m pytest gates not runnable on this host (tooling absent) — unchanged.
4. P0 multi-validator consensus intermittency ("fix it" task) — see top of file; reserved full-mesh with deterministic PeerIds is the proposed but not-yet-proven direction.


# Memory 2026-09-04 (APPEND — third session same day, USB-drive reconcile)

## Active user request NOW (the open task to continue): surgical USB reconcile
User found a second copy of the project on a USB drive and wants all "finished things" there merged into THIS repo. Asked + answered clarify: user chose **Surgical reconcile** — bring in only what USB has that local lacks, reviewing each differing source file to AVOID regressing our audited fixes. Do NOT wholesale-replace local with USB. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## USB drive facts (measured 2026-09-04)
- Location: `/media/lojak/USB-Drive/home/x3star/Desktop/xxxstar-main` (a deep path; USB is the same machine's earlier work, copied to a removable drive).
- It is a **separate git repo with its own 109-commit history**, last commit **2026-06-20** ("simulator + risk-engine changes"). Local = git-initialized-by-me snapshot + audited fixes (commits through `0bac495b`).
- Total 156GB (mostly node_modules, videos `*.mp4`, `.deb` protobuf installs, `protoc*zip/tar.gz`, `ollama-models`, `nohup.out`, a stray zip) — these top-level USB-only items are MACHINE ARTIFACTS, must NOT be imported.
- **Source inventories are ~identical**: USB 133 crates vs local 132 (only USB extra = `crates/x3-chain-health-daemon/`); pallets both 57 (no USB-only pallet). Same directory set (config/, docker/ exist ONLY in USB).
- **Content delta is tiny**: ~7,889 tracked source files present in BOTH are byte-identical; only **65 files differ in content**. (Plus ~23 local-only = our audit/fix additions.)

## Real, meaningful USB-only ADDITIONS (genuinely new "finished things" to merge — non-conflicting)
1. **`crates/x3-chain-health-daemon/`** — real new crate: a health moni daemon that polls chain metrics and writes health proofs (workspace member in USB root Cargo.toml line ~196). Its dependencies import `x3_atomic_swap::ledger::{...}` which EXISTS locally → should compile as a workspace member. It's aligned with the X3 "chain health monitor" system. Merge = copy crate dir + add ONE member line to root Cargo.toml (keep local's other members).
2. **`config/rpc-endpoints.toml`** and **`docker/docker-compose.yml`** (whole `config/`, `docker/` dirs USB-only) — real config/deploy artifacts; review-in.
3. Top-level docs/planning: `prd.json`, `x3_cross_vm_expansion_prompt.json`, `THE_HOLY_GRAIL_COMPLETION_CHECKLIST.md`, `legal-handoff-audit-package-2026-05-16.tar.gz` (archive — likely skip or leave unextracted).
4. **`vendov`** = typo'd copy (junk; skip).

## CRITICAL — the two trees are DELIBERATELY ENGINEERED OPPOSITE in root Cargo.toml
- LOCAL (dst) **re-enabled** `svm-integration`, `svm-counter`, `x3-crosschain-gateway` (tested 2026-09-03, builds green independently).
- USB has those 3 **commented out** (older decision) but has the health-daemon member.
- → When merging root Cargo.toml: KEEP local's 3 re-enabled members, INSERT ONLY the `x3-chain-health-daemon` member line. Do NOT take USB's Cargo.toml wholesale or you undo our re-enablement.
- Root Cargo.toml current members list ends (line ~196): `... "crates/x3-bitcoin-vault" ]` then `default-members = ["node"]`, `resolver = "2"`. Insert new member before the closing `]`.

## The 65 differing-content files (the delicate per-file adjudication set)
Both sides have content the other lacks in overlapping paths. Local is AHEAD (audited, DON'T overwrite) on at least: `apps/x3-desktop/.../BlockStore.ts` (off-by-one fix), `crates/x3-foundry-core/src/error.rs`, `pallets/x3-kernel/*` (Default impl fixes), `runtime/fraud_proofs/*`, `crates/x3-foundry-sdk/src/*` + `packages/x3-foundry-sdk/*` (TS error fixes), `packages/atomic-swap-sdk/packages/*` (racy-test fixes), `node/benches/*`, and every TESTNET_*/audit/readiness artifact. USB may be AHEAD in other paths (its June-20 simulator/risk-engine/x3-lang/EVM-contract evolution) — each of the 65 must be diffed and decided directionally. Bucket them; never blindly cp over local.

## Merge strategy guardrails (agreed with user)
- Surgical, reversible, per-file. No clobber of audited fixes. Expect to iterate crate-by-crate.
- Get a full clean diff of each of the 65 differing files (git diff --no-index USB local) before choosing direction.
- Don't import USB top-level junk. Respect .gitignore. Don't pull in 156GB.

## Next concrete steps (recommended, in order)
1. Stage 1 (safe, do it directly): copy `crates/x3-chain-health-daemon/` into local + add ONE workspace member line (keep local's 3 re-enabled); `cargo check -p x3-chain-health-daemon` to prove it compiles; commit.
2. Review `config/` + `docker/` additions; import the useful ones (rpc-endpoints.toml, docker-compose) without breaking existing infra/`Dockerfile.validator`.
3. Bucket the 65 differing files: (a) local-ahead audited → keep local (no-op), (b) USB-ahead finished feature work → merge USB content, (c) genuinely new USB-only source → copy. For each (c)/real-USB-ahead file, verify it compiles/passes before commit.
4. Report per-file disposition table to operator at the end.

## COMPLETE — USB surgical reconcile finished 2026-09-04 (all 4 stages landed + independently verified)
Stage 1: health-daemon crate merged (`14fe3a78`,`48c63c07`). Stage 2: config/rpc-endpoints.toml (`c36629a4`); docker/ LEFT untracked — repo `.gitignore` deliberately excludes docker/ ("# Docker support infra (not for validators)"), respecting author intent. Stage 3: the 65-file set adjudicated. Real USB-ahead clusters = (1) x3-lang/ compiler+VM and (2) x3-atomic-swap + x3-vm; reconciled by two focused subagents, each keeping local-only orthogonal additions + tests, both gated on build/clippy -D warnings/test EXIT 0 which I INDEPENDENTLY re-ran. x3-lang: `f55fa53a`/`f4c4ddc8`, verified `90d16bff` (self-contained separate workspace; USB finished base + local verify.rs/numeric.rs/diagnostic.rs first-class). x3-atomic-swap+x3-vm: `b1cb60ed`/`ac70bbb4`/`0df9ab5d`, verified `956f6465` (USB typed VmFamily adapter engine + wasm_l1_htlc + x3-vm symmetric unwind + 0x32/0x33 universal escrow hostcalls; kept local evm_live/btc_live; lib.rs union). EVERY other differing source file measured LOCAL-AHEAD (local unique > USB unique incl. x3-crosschain-gateway, cross-vm-bridge, service.rs, benchmarking, BlockStore.ts, foundry-sdk) → kept local, no regression. Genuine USB conflicts NOT forced (documented in TESTNET_VERIFICATION.md): state_machine_proptest.rs (violates shared intent.rs semantics), X3-contracts FoundryGovernance.sol (USB rewrite internally inconsistent; EVM-contracts reconcile is separate scope). Final region clean; no commits outstanding. Remaining standing work = the OLD pre-USB task thread (see top of this file + TESTNET_*): P0 "fix it" already resolved `8579b34d` via run-mesh.py deterministic full-mesh; open items were the post-fix real-batch (x3-desktop BlockStore already fixed in clippy session), cross-host mesh re-proof (loopback-bound only), and the readiness fixes already landed. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Re-Raised note
Memory search pausing may relate to model/index mismatch (`fts-only` vs embedding). Not re-diagnosed this session; reading files directly works fine. Remember: workspace READ-ONLY files during flush: MEMORY.md, DREAMS.md, SOUL.md, AGENTS.md, IDENTITY.md.


## Memory 2026-09-04 (APPEND — fourth session same day, USB reconcile CLOSED + status)

### USB surgical reconcile: COMPLETE (all stages done, region clean)
The active user request "surgical USB reconcile" (merge USB-drive finished things without regressing audited fixes) is fully closed. Everything genuinely-USB-finished is merged & proven green; all remaining differing files measured **local-ahead** (keep local). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

Merged & verified (each gate independently re-run by me, not just trusted from subagent reports):
- x3-lang compiler/VM reconcile — subagent commits `f55fa53a` (code) + `f4c4ddc8` (disposition); my independent reproduction commit `90d16bff`. USB finished lineage as base (restored formatter/linter/risk modules + full semantic risk/invariants/route-score/mainnet-gating engine) + kept local orthogonal passes (verify_ir/numeric/diagnostic/conformance). x3-lang is its OWN Cargo workspace; no root crate depends on it (verified self-contained → cannot regress root build). build/clippy `-D warnings`/test ALL EXIT 0. Notable honest exclusion: local `verify_ir` kept as exported-but-NOT-pipeline pass because USB's sanctioned `flagship_b52.x3` lowers a swap to `min_output=0` the structural verifier would reject (flagged as a real slippage-safety concern for later). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
- x3-atomic-swap + x3-vm reconcile — subagent commits `0df9ab5d`/`ac70bbb4`/`b1cb60ed` (+ x3-vm universal_escrow.rs addition); my reproduction commit `956f6465`. Adopted USB: typed `VmFamily {Account,Utxo,Object,Wasm,Message,Zk,Runtime,Native}` enum adapter.rs (726 vs local 345, local was pure subset), new `wasm_l1_htlc.rs` (WASM-L1 HTLC adapter ICP/MultiversX/Archway), `cross_adapter_atomicity_test` readiness field propagated across all 17 sibling htlc adapters + scoreboard (per-file differed only by that line), fixed scoreboard hardcoded 1600-denominator pct, x3-vm symmetric-compensation unwind hooks + 0x32/0x33 universal escrow hostcalls + universal_escrow.rs. KEPT local verbatim: evm_live.rs/btc_live.rs (guarded live executors); lib.rs = union so both coexist. Clippy EXIT 0; atomic-swap std test 0 failed (649 lib + 31 chaos + 44 integration + 1 atlas); x3-vm test 0 failed; fake-code scan clean. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
- Earlier merged: x3-chain-health-daemon crate (`14fe3a78`), config/rpc-endpoints.toml (`c36629a4` = latest commit there), Cargo.lock for daemon (`48c63c07`). docker/ left UNTRACKED — repo `.gitignore` deliberately excludes it ("not for validators"); respected design intent. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

Documented-but-NOT-forced genuine USB conflicts (both internally inconsistent even in USB → correct to exclude): `x3-atomic-swap/.../state_machine_proptest.rs` (fails vs shared intent.rs expired-refund semantics; adopt would force out-of-scope intent.rs change or test-fake) and `X3-contracts/.../FoundryGovernance.sol` (rewrite inconsistent with own test peer; belongs to separate EVM-contracts reconcile). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

Adjudication METHOD that worked (reuse if more USB-esque merges arise): low-fidelity `comm` on sorted lines over-reports "USBAHEAD" (counts duplicate/blank/reordered lines). Correct check = per-file `diff <usb> <local>` reading `<`=USB `>`=local, PLUS git-log of local file (if local file only ever touched by baseline snapshot commit, local is an OLD/unmodified snapshot → USB likely ahead; if local has later audit/fix commits → local ahead, keep). For every "remaining" differ after the two reconciles, local had MORE unique content → all local-ahead. service.rs local carries Attestation/VerificationRouter/production-EVM-receipt-verifier refs USB lacks. So no USB testnet feature is being missed. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

### Standing next-work options (post-reconcile, ask operator which)
1. Cross-host mesh re-proof (P0 evidence is loopback-only on one host = honest bound; run-mesh.py `scripts/testnet/run-mesh.py` + deterministic PeerIds commit `8579b34d`). Needs real networked hosts → hard blocker on this single box.
2. Real-fix batch from readiness scan — LARGELY LANDED during later clippy/JS-fix sessions (BlockStore off-by-one, foundry-sdk TS, kernel Default, jest configs all green). Re-check TESTNET_GAP_LEDGER.md for any not yet closed.
3. Memory search paused: index built for `fts-only`, expected `text-embedding-3-small`. Fix cmd given to user: `openclaw memory status --index --agent x3` then rebuild embed index. Reading files directly works fine.
4. `/tmp/x3-*` stale litter dirs noted (no running node processes — grep count 0; harmless leftover logs outside git).

### Proven correct-direction cases worth not re-deriving
Remaining differing Rust files = local-ahead (do NOT overwrite from USB): x3-atomic-kernel/benchmarking.rs (local 60-monolty), settlement-engine/benchmarking.rs, node/src/service.rs, crosschain-gateway/lib.rs (+250 local), cross-vm-bridge, x3-bridge-adapters (usbonly=41/loconly=44), northern-swarm, freeze.rs, launch-gates/* (legacy illustrative excerpts, not compiled), foundry.toml (micro), next-env.d.ts (artifact), docs/.clinerules/*.md (cosmetic). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

Note: turn-about-after-reconcile — I dispatched a SECOND reconcile subagent for atomic-swap/x3-vm (session 9863368a) after abort; its completion event re-arrived 2x; treat duplicate child announcements as one; always independently re-run gates myself before declaring done.

## Post-reconcile verification (fifth session note) — NO-REGRESSION confirmed on fresh binary
Rebuilt node from reconciled tree & re-proved reserved full-mesh (pick rationale: real-fix batch was closed; cross-host blocked on single host). `cargo build --release -p x3-chain-node` EXIT 0 (fresh binary 12:06, stale prior was 09-03). run-mesh.py cycles 8/8 cold-starts -> each ONE finalized head; kills 7/7 -> remaining 6 keep one chain. Proves x3-atomic-swap typed-VmFamily/wasm_l1_htlc + x3-vm unwind/universal-escrow merges did NOT regress consensus/finality/bring-up. Recorded TESTNET_VERIFICATION.md commit `48580996`. Tree clean; nodes stopped. Confirms no further USB work and no testnet regression from the reconcile. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
# Memory 2026-09-04 (APPEND — validator-launcher/spec mismatch verification, post-flush batch)

## Validator-launcher vs chain-spec mismatch check (subagent result, session settled)
A subagent was dispatched to check an alleged validator-launcher/spec mismatch ("spec installs five Aura authorities and does not include Dave and One"). **Premise DISPROVEN by direct decode of the on-disk file** — no fix needed. Everything below measured, treat as settled (do not re-open unless new evidence). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

- `deployment/chain-specs/x3-testnet-raw.json` installs **7 Aura + 7 Grandpa** authorities = exactly Alice, Bob, Charlie, Dave, Eve, Ferdie, One — matching the launcher's `DEV_SEEDS`. There is NO launcher/spec authority mismatch to fix.
- Conclusion/pattern to remember: when a task claims "the checked-in spec has X authorities that don't match the launcher", the FIRST step is to decode the actual spec file (`jq '.genesis.raw.top'` / direct read), not trust the stated premise. This tree's real spec is a RAW/Live spec (see top-of-file gotcha #2), so authorities live under `genesis.raw.top` trie keys, not a plain `runtimeGenesis.config` — grep-ability is limited; decode via the node or targeted key lookup.

## Batch state
- Child completion delivered for this batch; subagent settled, no further yield expected. Original request's visible final answer not required to persist past flush; this is a durable note for the record.

## Standing (unchanged) next-work options — ask operator which thread to resume
1. Cross-host mesh re-proof (P0 evidence loopback-only; run-mesh.py + deterministic PeerIds commit `8579b34d`; blocked on a single box → needs real networked hosts).
2. Readiness-scan real-fix batch — largely landed in prior clippy/JS sessions; re-check TESTNET_GAP_LEDGER.md for anything still open.
3. Memory search pause (fts-only vs text-embedding-3-small index mismatch); reading files directly works. Fix: `openclaw memory status --index --agent x3` then rebuild embed index.
4. `/tmp/x3-*` stale litter dirs (harmless, outside git; no live nodes).

## SEC-v1 FINDING (2026-09-04, resumed after compaction) — Key-material leak purged (non-destructive)
- While re-verifying the validator-launcher/spec "mismatch" premise (claimed spec installs 5 Aura authorities, excludes Dave+One), found the premise FALSE: the ACTIVE launcher run-mesh.py uses `deployment/chain-specs/fresh/x3-testnet-plain.json` (plain, real keys) + per-validator `X3_DEV_SEED` from ignored `validator-N.suri` files. On-disk spec = 7 Aura + 7 Grandpa matching the 7 DEV_SEED keys exactly. No mismatch.
- REAL finding: `suris.txt` in the same validator-keys/ dir (aggregate plaintext log of all 7 authoring seeds written by `build-x3-testnet-spec.py`) was TRACKED in git, committed in d594af8f — contradicting fresh/.gitignore "NEVER commit" (which used root-relative paths that never applied from the fresh/ location), that commit's "Key SURIs excluded" claim, and the file's own "not committed" header.
- FIX (cb1452e3): rewrote fresh/.gitignore patterns relative to fresh/ so validator-keys/ + *.suri + *.seed + .node-key-* actually match; `git rm --cached` suris.txt (on-disk copy kept, now protected, harness log still works). Ledger updated (f3f0d9bb).
- STILL OPEN: seeds remain in d594af8f on master history. Full purge = filter-repo rewrite (destructive to 32-commit audit chain), DEFERRED pending operator decision. No remotes; local audit repo.
- LESSON: gitignore path bases in nested dirs resolve relative to the .gitignore's location. Verify with `git check-ignore -v` + sweep `git ls-files | grep suri/seed/secret/node-key`, not by eyeballing the gitignore. Note sibling tracking override wins over gitignore.
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## SEC-v1 RESOLVED (2026-09-04) — full history purge executed + verified
- Operator authorized ("yes do what's best"). Ran git filter-branch --index-filter to drop validator-keys/suris.txt from all history on master (37 commits rewritten; --prune-empty).
- Purge cleanup caught a subtlety: after filter-branch the secret SURVIVED reachable via refs/original/refs/{heads/master,stash} AND a refs/codex/... checkpoint ref (filter-branch warned it wasn't rewritten). Deleted all three + orphaned stash ref, expired reflog, gc --prune=now.
- VERIFIED triple-clean: no suris.txt reachable from any ref; secret blob dab0e4e3 physically absent (cat-file -e fails); deep batch-all-objects grep for seed hex 0xe8e93d... found nothing. 37 commits intact; all key files present at HEAD; working tree clean; ledger updated (a44fd79a).
- NOTE re-write: pre-rewrite hashes (d594af8f, cb1452e3, f3f0d9bb, 981ea5f6, 3f847f3d,...) NO LONGER EXIST. Post-rewrite equivalents: 207fffeb (gitignore fix), a063faeb (ledger SEC-v1), 1f980377/43e96630 (memory+dreams gitignore), 20459eaa (GPU/launcher deliverables), 43e96630 = current tip... tip actually a44fd79a now.
- LESSON (SEC purge): filter-branch alone is NOT enough — secret survives in refs/original/*, codex checkpoint refs (non-committish, skipped), and reflog. Must delete ALL pinning refs + expire reflog + gc --prune=now + verify with git cat-file --batch-all-objects --batch-check.
- Pre-purge full-history bundle written then DELETED (only other secret copy; gone — no leak remains anywhere).
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Op session (2026-09-04 14:03) — restored unexplained working-tree deletions
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->
Operator said "do what's best" after I flagged 52 files deleted from the working tree (unstaged, still in committed HEAD). No recorded intent/commit explained the removals, and README.md:149 links to one of them (MAINNET_RC1_SCOPE.md); several were reference/runbook/report docs. Best-effort non-destructive call: restored all 52 from HEAD via `git checkout -- $(git diff --name-only --diff-filter=D)`. Working tree now clean (git status = 0). HEAD intact 7c582d8a. If a future session intends those doc deletions, re-do them deliberately WITH a matching README/link update + commit message — don't remove docs that authoritative files still link to.


# Memory 2026-09-04 (APPEND — full doc-vs-code audit, chaptered book + olddocs, ~13:40–14:04 MDT)

## Active user request (this session): exhaustive documentation audit → one chaptered book
User request: audit EVERY doc against real code, flag incomplete, list undone features, DEDUPE, compile into ONE chaptered book; destination = a NEW folder `Desktop/xxxstar` (NOT the repo `xxxstar-main`, which is the SOURCE). User then said "put all read and used docs that we won't need anymore in a file called olddocs" → chose folder option: `/home/lojak/Desktop/xxxstar/olddocs/`. User picked (clarify): Everything/maximize coverage, exhaustive spec-book, MOVE obsolete docs to olddocs. Then "lets keep going" = continue same thread. USERDEFAULTESTINATION = Desktop/xxxstar (sibling of repo), NOT inside repo. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Deliverables produced (4 Sep 2026, DONE in /home/lojak/Desktop/xxxstar)
- `book.md` — single chaptered book (7 ch: methodology → doc inventory/flags → per-domain doc-vs-code verdict tables → flagged-incomplete → dedup Register of 45+ Undone Features U01–U45 → retired-docs index → next actions + verify commands). Compose rules: REAL/PARTIAL/STALE/FICTIONAL/UNVERIFIABLE.
- `evidence/CHILD_C1..C5_*.md` + `_CONTRACT.md` — five parallel read-only subagent audits (C1 core-chain/recovered-to-file, C2 gateway/bridges/EVM/SVM/BTC/Solidity, C3 x3-lang compiler+VM+formal proofs, C4 swarm/agents/readiness/tooling/reports corpus, C5 testnet/consensus/P2P/security). NOTE: C1 finished its investigation but never flushed its file (session marked complete mid-write); parent reconstructed it into evidence/CHILD_C1_core.md from C1's own summary + a fresh direct re-verify. Pattern: for collector children that don't announce completion, always check evidence dir; if agent said "writing file now" but no file, recover from progress summary + re-verify rather than re-spawn.
- `olddocs/` — moved 52 obsolete docs OUT of repo (git shows deletions, reversible via olddocs/README.md which maps origin). Categories preserved: `_root/` (superseded MAINNET_*/TESTNET_* stubs, snapshots, grok-chat1.md 8.8MB dump), `reports/` (placeholder never-regenerated stubs + old-path dump), `relayer_phase13/` (~24 PHASE_13E/F + MAINNET_* "GO FOR LAUNCH" overclaim docs from crates/x3-relayer), `runbooks_deployment/` (TIER5 "✅ PRODUCTION READY / FULL YOLO / APPROVED SELF-SIGNED" overclaims), `docs_archive/`. KEPT in repo (live authority): LAUNCH_SCOPE.md, FEATURE_REGISTRY.toml, RELEASE_GATES.md, TESTNET_GAP_LEDGER.md, TESTNET_VERIFICATION.md, GPU_VALIDATOR_HONEST_AUDIT.md, FAILURES_AND_TODOS.md. RECOMMENDED-not-auto-moved: root SECURITY.md (thin) + docs/security.md (large dup) — canonical = docs/security/SECURITY.md; non-Phase13 operational relayer guides.
- `corpus/` — scratch inventory helper files (crates_x3.txt real 102-crate list, root_md_dates.tsv, pallets manifests).

## Doc-corpus scope reality (13.6k .md) — don't re-count every time
Repo-wide `find -name '*.md'` ~13,600 files but ~13k are non-claim vendored/scraped prose: apps(~9,092), packages(~2,735), vendored forge-std(~711), .toolchain(~153), docs archive/openspec/patches. The CLAIM-BEARING corpus (what asserts "feature X is built") is: root *.md (~55), docs/current, docs/reports/runbooks/x3_operator, reports/*.md snapshots, FEATURE_REGISTRY.toml + .blackboxrules + AUDIT_SPEC.md + AGENTS.md, TESTNET_*.md + .testnet-audit. Audit those; ignore the other 13k as claim sources.

## HEADLINE verified findings (command-backed, high-confidence — go in any future docs work)
- **Core internal cross-VM pallets REAL + wired**: x3-atomic-kernel, x3-cross-vm-router, x3-supply-ledger, x3-settlement-engine, x3-dex, x3-token-factory, x3-launchpad, x3-lp-locker, x3-wallet-pallet exist. X3CrossVmRouter present in 5 construct_runtime blocks (grep hits ~475/550/632/694/771 in runtime/src/lib.rs); the source COMMENT claiming "six explicit variants" is STALE (real = five in lib.rs + uses in fraud_proofs/pallet.rs + tests.rs). evolution-core = pallets/evolution-core + crates/x3-evolution-core + crates/x3-evolution (registry `triforge_runtime` real but prefix-inconsistent).
- **External gateway/bridges genuinely OFF for shipped build**: pallets/x3-cross-vm-router `compile_error!` forbids `external-gateway` with `mainnet-rc1` (lib.rs:61-65), router rejects non-internal route `NonInternalRouteNotSupported` (lib.rs:882-886), `ExternalBridgesEnabled` genesis-false Root-only toggle w/ ExternalBridgeAuditGate (lib.rs:765-794), absent from mainnet chain-spec. x3-bridge / x3-bitcoin-vault / x3-relayer are WORKSPACE ISLANDS (real code + tests, ZERO compiled consumers, x3-bridge lib.rs:1 `#![allow(unused,dead_code,deprecated)]`). MAINNET_RC1_SCOPE matches code.
- **FEATURE_REGISTRY.toml + CURRENT_MAINNET_STATUS name features that do NOT exist on disk** (FICTIONAL paths): `crates/x3-broadcast`, `crates/x3-grantsmith`, `crates/x3-reactor`, `pallets/x3-sentinel` all ABSENT. The real 102-crate list (`crates/x3-*`) contains NONE of these. Registry rows x3_broadcast 30% / x3_grantsmith 20% "LIVE_TESTNET" + CURRENT scoreboard "X3 Sentinel 50%" and "Reactor" are dead references; readiness engine's marketing/grant generators still hardcode placeholder strings in crates/x3-readiness lib.rs (:518-519 marketing "No automated claims...", :622 grant "not yet wired") while main.rs peers ARE registry-aware (lib.rs & main.rs = two divergent copies; FAILURES_AND_TODOS "replaced 5 placeholders" overstates — only 3 of 5 registry-driven in lib.rs).
- **NOT implemented despite docs**: real native JIT — crates/x3-vm/jit_compiler.rs `backend_available()==false`, `#[cfg(not(test))]` returns Err("not yet available"), only test-only mock; NO cranelift/llvm dep; docs header claiming Cranelift/3-5x is aspirational. x3-lang/vm/jit.rs = hit-counter skeleton. Shipping x3-lang compiler = parse→IR→emit→verify only (regalloc.rs dead_code placeholder v0.2; no optimizer passes). Optimizer passes (LICM/CSE/strength-reduction/superopt-SMT) live only in experimental crates/x3-opt/x3-compiler as scaffolding (76 TODO/placeholder hits). BTC PoW header-chain validation NOT implemented (btc_adapter.rs:143), feature-gated bitcoin-adapter fails closed. Groth16/PLONK zk cross-chain proofs NOT implemented (x3-bridge cross_chain_proofs.rs `ZkProofNotImplemented`, zk-proofs feature enabled nowhere). Wormhole VAA verify = structural counts only, real ECDSA-vs-guardian-key is a TODO comment. Real BTC mainnet signer quorum NOT active (x3-bitcoin-vault has lib+17 tests but unconsumed). GPU kernels = CPU-sim behind EMPTY `cuda=[]`/`opencl=[]`/`metal=[]`/`vulkan=[]` features (matching GPU_VALIDATOR_HONEST_AUDIT); cross-chain-gpu-validator crate::cuda module absent so cfg-cuda path can't even compile. x3-desktop get_app_registry() hard-returns `vec![]` (apps/x3-desktop/src-tauri/src/main.rs:1324) → app registry not wired to FEATURE_REGISTRY (matches tauri_desktop_gap_report still-open).
- **Registry required-tests aspirational**: e.g. [x3_reactor] required_tests benchmark_job_submits/benchmark_result_publishes do NOT exist anywhere in code (x3-bench crate is real but names differ); testnet NoOp validator "accepts all proofs" is testnet-only (runtime/Cargo.toml:167, cross-vm-bridge NoOpDispatcher) — must not be read as real verification.
- **Readiness number reconciliation**: CURRENT_MAINNET_STATUS avg ~54%/60%, but RELEASE_GATES.md + several docs cite ~36% as current authority; FEATURE_REGISTRY per-feature = 10–88%. Book presents both honestly. Pick: RELEASE_GATES.md ~36% framing + registry is the coherent "current", scoreboard rows 100%-external claims already corrected by LAUNCH_SCOPE.
- **x3-lang pytest suite NOT all green**: `.venv/bin/python -m pytest x3-lang/tests/` = 8 failed/6 passed (fails in runner dry-run + emitters needing verifier-proof bundle — several fail-closed by design). Typechecker/parser core pass. Format-and-inject doc formatter warnings on emitter files too.

## olddocs move side-effects to flag
Moving obsolete docs OUT of repo breaks doc-graph links pointing at them (e.g. README docs list → MAINNET_RC1_SCOPE.md, doc index references). Not fixed (out of audit scope) — logged as follow-up. Being a git repo (init'd by me Sep 3, ~37-39 commits), moves appear as deletions; reversible. NOTE current commit history was REWRITTEN earlier today for SEC-v1 purge — pre-rewrite hashes invalid; tip a44fd79a-era (post-purge hashes: 207fffeb/a063faeb/20459eaa/43e96630/1f980377).

## Verdicts on top docs (quick recon for future)
README, LAUNCH_SCOPE(v1.1, truly authoritative), FEATURE_REGISTRY.toml, RELEASE_GATES.md, GPU_VALIDATOR_HONEST_AUDIT.md, TESTNET_GAP_LEDGER.md, TESTNET_VERIFICATION.md, FAILURES_AND_TODOS.md = trustworthy/current (FAILURES_AND_TODOS overclaims the all-5-readiness-fix only). GAP_REPORT.md ~90-item taxonomy is PARTIAL (many items fixed later) — reconcile against FAILURES_AND_TODOS + registry before trusting. docs/x3-lang/README.md is high quality & candid (crate map + placeholder warnings). MAINNET framings in root are superseded by LAUNCH_SCOPE.

## Execution-pattern notes (reusable)
- Deep fast audit = 5 parallel read-only research subagents each writing ONE evidence file in Desktop/xxxstar/evidence/, then parent synthesized into book.md. For a repo this big, DO NOT try to read all claim docs in one context; partition by domain and delegate, always re-key facts yourself.
- When a task asserts "check the checked-in spec has wrong authorities", FIRST decode the actual file (jq on genesis.raw.top for RAW/Live specs) — do not trust the premise.
- Existing memory file confirmed to contain the SEC-v1 purge chain + P0 consensus resolution (run-mesh.py 8579b34d deterministic full-mesh) + USB reconcile + x3-lang/atomic-swap merges. All prior standing threads: readiness fixes largely landed; cross-host mesh proof still loopback-bound (blocked, needs real networked hosts).

## Standing next-work options (unchanged, ask operator)
1. Cross-host mesh re-proof (loopback-only P0 evidence) — blocked on single box.
2. Fix doc-graph dangling pointers from olddocs move + FEATURE_REGISTRY dead-entry path honesty pass (populate/remap x3_sentinel/broadcast/grantsmith/reactor rows) — NEW follow-up born from this session.
3. Full cargo test/clippy + forge/anchor EVM-SVM execution (tools not installed on host) to close the UNVERIFIABLE gaps the book flagged in ch7.
4. Roam memory-search embed-index mismatch; reading files directly works.
5. Readiness lib marketing/grant stub — flag task_53e5c32e raised (port registry-driven bodies from main.rs into lib.rs).

## CORRECTION appended later same session — olddocs retirement is now REAL & committed `9322e41f`
IMPORTANT state fix. An earlier pass only COPIED the flagged obsolete docs into Desktop/xxxstar/olddocs/ and
had NOT deleted them from the repo; the working tree was later auto-restored (git commit `35c9f99a` "restore
unexplained working-tree deletions", memorialized in memory), so the repo stayed 100%% intact while git showed
clean and book.md wrongly said "removed". Re-checked and surfaced to operator; user chose "Actually remove from
repo". Result: **52 obsolete docs deleted from xxxstar-main and committed `9322e41f`** (52 files, 33,909
deletions). Every deletion pre-verified tracked-in-git AND byte-identical (`cmp`) to its olddocs twin
(13 root + 19 crates/x3-relayer PHASE_13/MAINNET suite + 14 reports/ stubs + 5 docs/runbooks/deployment
TIER5/production overclaims + 1 docs/archive leftover = 52/52 matched, 0 problems). Legit docs preserved
(docs/runbooks/deployment/DEPLOYMENT_CHECKLIST.md+DEPLOYMENT_GUIDE.md+TIER5_DEPLOYMENT_GUIDE.md+
TIER5_PRE_DEPLOYMENT_CHECKLIST.md; reports/ kept 52 of 66). Live-authority docs left untouched (LAUNCH_SCOPE,
FEATURE_REGISTRY.toml, RELEASE_GATES, TESTNET_GAP_LEDGER, TESTNET_VERIFICATION, GPU_VALIDATOR_HONEST_AUDIT).
book.md Ch6 updated to state REAL committed removal + integrity history; header updated; Ch4 item #2 enriched with
exact source-quoted stub bodies; new evidence file evidence/WORKED_EXAMPLE_readiness_lib_vs_claim.md. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## LESSON applied to own output (worth keeping): never report a doc "moved/retired" without verifying the
REPO end-state (git status clean + `[ -e origin ]` + cmp to archive). A copy is not a move. The audit caught
this in its own deliverable on the same day — the doc-vs-reality gap is real even for the audit book. Always
confirm file ownership/count at the end (Desktop/xxxstar/olddocs now = 53 files incl README index; source repo
tree clean). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Ready worked-example finding (real, precise — good template for future doc-claims audits)
FAILURES_AND_TODOS.md 2026-06-18 claims it "replaced 5 placeholder String::from(...) functions in
crates/x3-readiness/src/lib.rs:509-541" with registry-driven reports. FALSE for 2 of them in the CURRENT
lib.rs: generate_marketing_claims_audit() (~:518) still returns literal "No automated claims verification
configured." and generate_grant_pipeline_report() (~:622) literal "Grant tracking integration not yet wired.";
btc_gateway/service_health/swarm_health/reactor (4) ARE registry-driven. main.rs (CLI) DOES have real
registry-driven versions (generate_marketing_audit :320, generate_grant_pipeline_report :337 wired to
subcommands :78-79). So the CLI is honest but the LIBRARY API two generators are no-op placeholders (anti-
AGENTS.md). Template value: FAILURES_AND_TODOS is the repo's honest-repair ledger; even it overstates — verify
every "fixed N placeholders" claim by reading the exact fn. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## MEMORY-SEARCH FIXED (2026-09-04) — embeddings moved to LOCAL ollama (no OpenAI credits)
- Root cause: x3 agent memory embeddings defaulted to openai/text-embedding-3-small; OpenAI account had exhausted credits (429 credit_balance_exhausted). Index had degraded to fts-only fallback ("built for fts-only") and vector search was paused.
- Fix: added per-agent override in ~/.openclaw/openclaw.json agents.entries.x3.memory.search = {provider: ollama, model: nomic-embed-text}; pulled nomic-embed-text (~274MB) via ollama (local, free).
- Blockers hit & cleared: (1) stale "built for fts-only" identity + 203 stub 2-byte embeddings persisted through index --force (reindex did NOT replace stubs or reconcile meta); (2) fixed via `openclaw memory reset --agent x3 --yes` (clears derived index only, keeps sessions/memories), then `openclaw memory index --force`; (3) stale zero-byte reindex-lock.sqlite (dated 2026-09-03) blocked rebuild — removed manually (no live holder; only gateway process runs).
- VERIFIED: meta now model=nomic-embed-text provider=ollama vectorDims=768; 1785/1785 chunks with real vectors (0 stubs); 15/15 files (6 memory + 9 sessions); embedding cache 1785 entries; functional `memory search` returns ranked relevance. No OpenAI billing dependency.
- LESSON: when embedding provider is unavailable, OpenClaw commits an fts-only index + stub vectors that index --force will NOT overwrite; use `memory reset` then `memory index --force`. Stale *.reindex-lock.sqlite files survive failed runs and must be removed (reset/reindex don't always clear them).
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## X3 EVM (forge) CONTRACT SUITE UNBLOCKED + PROVEN GREEN (2026-09-04)
- The audit-ready X3 EVM contracts under X3-contracts/evm/ were UNRUNNABLE: forge not installed, and
  the lib deps forge-std + openzeppelin-contracts were EMPTY (phantom .gitmodules entries, no gitlinks
  in index, no committed copies). Root cause of audit "task 1" blocker (verify X3ExternalGateway).
- Fix (commit 2e6efe03): installed foundry (forge 1.8.1); removed 2 phantom .gitmodules entries for
  X3-contracts/evm/lib/{forge-std,openzeppelin-contracts}; wired lib/forge-std -> committed root
  forge-std (1.16); vendored OpenZeppelin v4.9.6 locally (matches security/ReentrancyGuard layout the
  contracts use — v5 removed that dir, would NOT compile) gitignored; untracked+ignored regenerable
  forge artifacts (out/, cache/). NOTE: contracts target OZ v4 layout, NOT v5.
- PROOF: `forge test` over X3-contracts/evm -> 169 passed / 0 failed across 12 suites (incl. 4096-run
  fuzz + invariant). X3ExternalGateway 10/10, CrossChainGovernance 20/20 (fuzz), AtlasSphereX3 19/19,
  StakingPool, Treasury, Flashloan, KernelBridge. EVM surface no longer "unproven".
- Each repo foundry project needs its deps present to run; check lib/ emptiness (phantom submodules)
  first, and match OZ major to the security/ layout the contracts import.
- Reinstall note: foundry lives ~/.foundry/bin; add to PATH (`export PATH=$PATH:$HOME/.foundry/bin`).

## Agent memory-sharing fix (2026-09-04 ~14:52) — points memory embeddings at local Ollama
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->
User: "im using deepseek" + "how do i make all agents share info" + "run it".
All 5 agents (main,x3,x3-research,x3inventory,custodian) run DeepSeek brain (custom-api-deepseek-com/deepseek-v4-flash @ api.deepseek.com). Only agent `x3` had memory.search pointed at local Ollama nomic-embed-text; the other 4 fell back to OpenAI text-embedding-3-small => Embedding 429 "no credits" => cross-agent vector recall broken (only file direct-read worked).
FIXED: edited ~/.openclaw/openclaw.json (backup openclaw.json.bak.ollama-*) — set agents.entries.{main,x3-research,x3inventory,custodian}.memory.search = {provider:ollama, model:nomic-embed-text} (matches working x3). Confirmed `openclaw memory status --agent main` now Provider: ollama/nomic-embed-text (no more OpenAI).
Ollama server up @127.0.0.1:11434 with nomic-embed-text:latest. Reindex in progress per-agent via `openclaw memory index --agent <a> --force` (Ollama llama-server pegged ~300-615% CPU). main was Dirty+paused; forcing full reindex bg (log /tmp/mem-reindex-main.log). Move leftover memory-reindex-* orphan temp files to /home/lojak/.cache/openclaw/orphan-reindex-backup-* (x3 had 12 files/six 22MB abort sets from OpenAI-429 era; main had one).
CLEANUP-REQUIRED after each agent reindex: `openclaw memory status --agent <a>` should show vector search NOT paused + embeddings ready. Pending agents to verify/reindex: main, x3-research, x3inventory, custodian (x3 already ready).
SECURITY NOTE (self-flagged): reading config surfaced raw API keys (DeepSeek sk-..., GitHub PAT, ollama) into an earlier transcript — advise user to rotate. Avoid echoing secret config values.

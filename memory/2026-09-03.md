# MEMORY — 2026-09-03

Session identity: agent `x3` (xxxstar-main repo agent). Host x3star1.
Workspace: `/home/lojak/Desktop/xxxstar-main` (X3 blockchain / cross-VM atomic execution).
NOTE: **No VCS (git) repo at workspace root.** Cannot git-checkpoint; report "no VCS root".

## User request being worked (active)
"xxxstar-main on my Desktop is the folder" → user asked agent to work on the repo.
Guiding repo brief (AGENTS.md): finish production-grade GPU-accelerated validator raising finalized TPS.
Brief's own rules: never report GPU acceleration unless real GPU path executed & verified; STOP + report clearly if required hardware/drivers/etc unavailable. User (via operator UI) chose CPU-only finalized-TPS optimization path and later a micro-benchmark track.

## DECISIVE HARDWARE FACT (verified)
Host has NO GPU compute hardware / no driver / no toolkit:
- Only PCI GPU-like device = Matrox G200eR2 (PCI 102b:0534, `14:00.0`) BMC display — NOT a compute accelerator.
- No nvidia-smi, no nvcc, no CUDA, no OpenCL, no /dev/nvidia*.
- /dev/dri only card1, owner root:video; user lojak NOT in video group → even Intel/software path closed.
- Vulkan ICDs present only for lavapipe/virtio (software).
- CPU = Intel Xeon E5-2620 v4, 32 logical cores.
- wgpu runtime probe passed only via fails-closed: stderr "pci id for fd 4: 102b:0534, driver (null)".

Consequence: GPU-acceleration phases (real GPU run, GPU tests, GPU benchmark) BLOCKED on hardware. No GPU claim can be made on this host. Assignment deliverable written to `GPU_VALIDATOR_HONEST_AUDIT.md` at workspace root.

## Verified builds/tests this session
- `cargo build -p x3-accel` OK. `cargo build -p x3-accel-wgpu` OK (real WGSL SHA256 wgpu backend, `Finished in 29.67s`). `cargo build -p x3-gpu-validator-swarm` OK (~1m09s).
- `cargo test -p x3-accel` 7/7 pass. `cargo test -p x3-accel --features wgpu` 7/7 pass but only via fails-closed branch.
- `cargo test -p x3-gpu-validator-swarm` all green (4 test binaries + benches incl. stress test sustained 30s, 10k tps soak, 1k/5k/10k real-time-tps, tps_sliding_window). These measure CPU in-memory orchestration, NOT on-chain finalized TPS.
- Toolchain pin 1.90.0 (rust-toolchain.toml). Prebuilt node binary exists in `target/release/` (79MB). Workspace default-members = ["node"].
- `cargo check -p parallel-proposer` clean (heavier crate, links x3-chain-runtime).

## Key architecture truths (important)
GPU stack = 2 generations:
1. `cross-chain-gpu-validator` (older): repo's OWN GAP_REPORT admits kernels are CPU simulations, empty validation loop, ~18% score, gated off RC-1. Docs claim memory pooling but `gpu_memory_pool.rs` "VRAM Slab Allocator" = host-side HashMap/RwLock bookkeeping only, no real device buffers.
2. `x3-gpu-validator-swarm` + `x3-accel` + `x3-accel-wgpu` (live): CPU is consensus truth; backends fail closed (`AccelError::BackendUnavailable`); parity recheck every batch; divergence→Divergent+quarantine; replay mode. `cuda/opencl/metal/vulkan` features on x3-gpu-validator-swarm are EMPTY labels — no CUDA FFI anywhere; crypto (ed25519-dalek verify_strict etc.) runs on CPU.

CPU finalized-TPS track finding:
- Aura authoring uses real `ParallelProposerFactory` (crates/parallel-proposer) wired in node/src/service.rs + cli. Builds blocks via real Substrate `BlockBuilderBuilder` + real tx pool + serial `apply_extrinsic`.
- Crates: `x3-parallel-executor` (standalone parallel exec, NOT wired into node execution); `parallel-proposer` (the actually-wired proposer).
- `apply_extrinsics_parallel` (parallel-proposer/src/substrate.rs) still pushes serially; rayon only partitions shards. Per-tx overhead: data.clone + encoded_size + BlakeTwo256 hash_of + extract_tx_metadata (full UncheckedExtrinsic re-encode→decode per tx every proposal).
- `contention predictor` + `extract_tx_metadata` add overhead stock proposers don't pay.
- Verdict: finalized TPS structurally gated by runtime block-weight/proof-size limits + Aura slot cadence + GRANDPA finality latency, NOT proposer signature checks. CPU-only proposer change cannot honestly push finalized TPS past consensus bounds.
- NO reproducible finalized-TPS baseline exists yet (needs local authority chain run: scripts/testnet/run-7-validators-local.sh, scripts/setup-multi-node-testnet.sh, submit-remark.js/load-remarks-tps.js).
- Signer/account pattern: `node/src/rpc.rs` builds signed extrinsics via `<Signature as Verify>::Signer::from(public).into_account()` → AccountId; SignedPayload::from_raw with 10-component SignedExtra incl. pallet_x3_invariants::InvariantCheck + pallet_x3_agent_law::AgentLawCheck.

## Micro-benchmark work in progress (last action)
- Track = prove whether proposer per-tx metadata overhead (decode path) is worth removing.
- Wrote `crates/parallel-proposer/benches/authoring_overhead.rs` (real runtime extrinsics, real extract_tx_metadata import, criterion-free std Instant timing). Known issue: file references nonexistent `parallel_proposer::TxMetadata0` / `TxMetadata` (real type lives in `contention_predictor` crate, not re-exported). Needs import fix when resuming.
- Earlier flawed synthetic bench (`benches/_draft.rs` etc.) DELETED — wrong API guesses (from_hex), would not faithfully measure real decode. Cargo.toml dev-deps for parallel-proposer were being added: frame-system, pallet-transaction-payment, pallet-x3-invariants, pallet-x3-agent-law, sp-core, parity-scale-codec, etc. Verify Cargo.toml edits are complete/valid on resume.

## Files changed this session
- Created: `GPU_VALIDATOR_HONEST_AUDIT.md` (workspace root, the honest audit + CPU-track analysis).
- Benches (in-progress, may need repair): crates/parallel-proposer/benches/authoring_overhead.rs; Cargo.toml dev-deps additions in crates/parallel-proposer/Cargo.toml.
- No git commit possible (no VCS root).

## RESULT (benchmark COMPLETE, measured)
Faithful micro-benchmark DONE and evidence appended.
- Final repro run (cargo bench -p parallel-proposer --bench authoring_overhead), release: pool-walk (the real apply_extrinsics_parallel loop model: clone+encoded_size+BlakeTwo256::hash_of+extract_tx_metadata) is ~1.13–1.22× hash-only ⇒ metadata decode adds ~+100–180 ns/tx at 512B; single-tx overhead ~90–460 ns/tx. Sub-µs/tx end to end.
- Runtime BlockWeights caps normal txs at ~135 ms/200 ms Aura slot (Weight::from_parts(1.5e11 ref-time,5MB), ~90% normal ratio). Authoring-side µs/tx overhead is NOT the finalized-TPS bottleneck.
- VERDICT: proposer-overhead removal ruled OUT by data (saves only µs/tx vs ms weight+slot budget). No production source modified.
- Audit doc updated: `GPU_VALIDATOR_HONEST_AUDIT.md` §6.1 measured results + §6.2 updated recommendation (removed stale "Not yet executed" line).

## How the bench was built (repair notes for future)
- `crates/parallel-proposer/benches/authoring_overhead.rs` compiles clean (harness=false, std Instant, no criterion).
- Real signed extrinsics built by MIRRORING exactly `node/src/rpc.rs` lines ~1012-1049: 10-elem `SignedExtra` tuple; the from_raw additional tuple is exactly 10 elements `((), spec, tx_version, gen_hash, gen_hash, (), (), (), (), ())`; signature via `Signature::from(pair.sign(payload))`.
- Gotchas fixed: `sp_runtime::H256` is private→ use `sp_core::H256`; `AgentLawCheck::<Runtime>::new()` does NOT exist → `decode(&mut &[][..]).expect(...)` (decode from empty bytes) as rpc.rs does via decode_agent_law_check(). `TxMetadata` type lives in `contention_predictor` crate, NOT re-exported by parallel_proposer (bench accesses only its fields sender/nonce through extract_tx_metadata return, no import needed). clippy needless_range_loop on sink helper → use iterator. Don't add sp-keyring (not a workspace key); use sp_core sr25519 Pair directly.
- Cargo.toml now has dev-deps + [[bench name=authoring_overhead harness=false]]. Verify these remain present on resume if re-running.

## Next steps (repo state at end)
1. (Recommended) Stop here unless user wants a real multi-node finalized-TPS baseline: scripts/testnet/*.sh + submit-remark.js/load-remarks-tps.js; that is the only way to measure FINALIZED TPS and needs a running authority chain.
2. No production code was changed; benchmark + audit doc are the deliverables. No VCS repo at root → no commit.
3. Completion report was delivered to user this session (Files changed / Commands run / Proof result / Remaining blockers / Next 10 tasks / Completion %).

# Memory 2026-09-05

## User request this session: "scan full repo and finish up any stubs and make sure everything is wired in tight"
Comprehensive full-repo stub sweep + wiring verification. Both Rust AGENTS gates now proven GREEN and 2 genuine live gaps closed. Durable findings & decisions below. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Gate proofs (run 2026-09-05, do not re-derive)
- `cargo check --workspace` → EXIT 0
- `cargo clippy --workspace --all-targets -- -D warnings` → EXIT 0 (was 101 BEFORE this session's fix)
- Zero `todo!()`/`unimplemented!()` macros remain in executable production (non-comment) lines of core live dirs (runtime/, pallets/, crates/, node/src). Prior hardening sessions really cleared them.
- `scripts/no_stub_guard.py` full-repo scan exits 0 (it only BLOCKS on staged new lines; whole-repo run is informational ~604 markers, mostly docs/tooling/seed — expected).

## Fix #1 — x3-chain-health-daemon clippy-gate breach (commit 422a9fb9)
`crates/x3-chain-health-daemon/src/main.rs` had top-level `#[cfg(test)] mod tests` BEFORE `#[tokio::main] async fn main()` → `clippy::items-after-test-module` FAILED the exact AGENTS gate. Fixed by relocating the tests module to be the LAST top-level item (tests run at EOF, after main). Crate clippy `-D warnings` EXIT 0; 4/4 bin tests pass. This crate came from the USB merge (`14fe3a78`) and had never been gate-checked — a latent breach. LESSON: any newly-merged crate must immediately run the strict workspace clippy, not just cargo check. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Fix #2 — token_getSupply RPC fabricated supply (commit 1f27cff1)
`node/src/rpc.rs` `token_getSupply` hardcoded `locked: u128 = 0` ("placeholder — wire to treasury_reserved") → desktop Supply Dashboard (SupplyDashboardPanel.tsx via rpc-client.ts + Tauri main.rs) showed locked=0 / circulating=total forever = FALSE data, not a true zero.
Fix: added `AtlasKernelRuntimeApi::native_locked_supply() -> u128` (pallets/x3-kernel trait decl after get_total_issuance; runtime/src/lib.rs impl after get_total_issuance) that returns the REAL free+reserved native balance held by the protocol treasury account (`TreasuryAccountId`). Node RPC now computes circulating = total_issuance − locked.
KEY implementation facts (do not re-derive):
- This runtime has NO pallet-staking (no `Staking:` in construct_runtime, no pallet-staking dep in runtime Cargo.toml) → there is NO on-chain staking-ledger lock to sum. "Locked" native = balances reserved on protocol/treasury accounts only. Treasury-held is the only clean, real definition.
- `TreasuryAccountId` (parameter_types!, `frame_support::PalletId(*b"py/trsry")`) is module-scope reachable from the runtime impl.
- In the runtime impl, do NOT add a local `use frame_support::traits::{Currency,...}` — `free_balance` already resolves from file-level `Currency` import (line ~57) and `reserved_balance` from file scope too; a local import is UNUSED → fails under `-D warnings`. Just call `pallet_balances::Pallet::<Runtime>::free_balance(&acct)` / `reserved_balance(&acct)` with NO local trait import.
- Runtime API trait method addition is safe vs test mocks: the only other implementor is `crates/x3-bridge-adapters/src/lib.rs:754` `RuntimeApiHarness` inside `#[cfg(test)]` via `mock_impl_runtime_apis!` which auto-provides new method bodies → no mock breakage; full-workspace `--all-targets` clippy EXIT 0 confirms.
- `pallet_staking` split note: upstream staking crates are a dependency only at repo-root/node level for validator tooling, NOT in the runtime. Don't try to sum ledger locks.

## Reachability audit disposition (scanned stubs LEFT as-is, each verified NOT on a live path)
- `cross-vm-bridge/src/connector.rs` `LiveNodeDispatcher::get_evm_bridge_escrow/get_svm_bridge_escrow` return zeroed address + warn ("RPC plumbing not yet wired"): ZERO references anywhere except its own module. Production x3-kernel commit path uses `KernelCrossVmDispatcher`; real escrow fetch lives in x3-bridge-adapters' typed-client adapter (`api.get_evm_bridge_escrow(at)`). Orphaned connector — no live caller → safe to leave as documented zero-fallback. Do NOT fabricate RPC plumbing in it.
- `crates/x3-gpu-validator-swarm x3_kernel_versioning::execute_kernel` canned matmul/conv2d bytes: referenced only by its own test (versioning.rs:530); no validator/finality/cpu-fallback path calls it. Dead mock scaffolding in unused public API.
- `flash-finality sign_with_keystore` None + `[0u8;64]` fallback: opt-in only via `enable_flash_finality` flag (default OFF); unit test enforces "flash finality must stay off"; GRANDPA is authoritative finality. Shadow gadget.
- `x3-vm universal_escrow.rs:161` InMemoryEscrowProvider: self-labeled test/development in-memory impl of an escrow trait; real chain-backed provider needs a live external chain (out of scope/un-wireable here).
- `node/src/command.rs:461` comit-status CLI prints "Full Comit query endpoint not yet implemented": operator debugging tool that self-documents its limit (not silent). Leave.
- `runtime/src/lib.rs` various `return 0`: length/validation guards, benign.

## Reminder re user pre-existing work
At session start the working tree had the user's in-progress content: apps/x3-desktop SceneManager.tsx/DesktopIcons.tsx edits, untracked apps/x3-desktop CenterEye.tsx, IDENTITY.md/USER.md edits, an e2e test change, plus a parallel doc commit 5d411dc0. I deliberately did NOT touch/commit the user's in-progress files. Parallel-session commits (5d411dc0) may appear in the log; don't attribute them to this session. My commits: 422a9fb9 (health-daemon), 1f27cff1 (supply RPC).

## Standing next-work (unchanged from prior, resume whichever)
1. P0 cross-host consensus-mesh re-proof — loopback-bound only; run-mesh.py + deterministic PeerIds commit 8579b34d; needs real networked hosts.
2. Full `cargo test --workspace` needs an idle host re-run (env-dependent e2e SIGKILL `cross_vm_real_chain_test`).
3. pnpm / python -m pytest gates not runnable on this host (tooling absent).
4. Memory-search index mismatch (fts-only vs text-embedding-3-small) — reading files directly works.

## MASTER CHECKLIST + GRANTS DB (2026-09-05 ~00:00) — commits c81363e5 + spawns 1c3fa8bc/4d10e5c9/55193b33
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->
User: "send me this back with everything checked off that is complete...
and make agents to start finding grants for these costly items...
hardware, isp, datacenter, you know what i need".

DELIVERABLES (committed c81363e5):
- docs/current/MASTER_CHECKLIST_STATUS.md (16.3KB):
  Maps external v1.0.0 checklist paths (/runtime, /vm, /daemon, /ai,
  /sdk, /cli, /ui, /tests/econ, /ops) to ACTUAL repo layout
  (runtime/, crates/, apps/, X3-contracts/evm/, tests/e2e/). Uses
  live verification against HEAD 796d5361. ~25 ✅, ~15 ⚠️, ~10 ❌,
  ~5 ❓. ADDS 16 items the external checklist MISSED.

- grants/GRANTS_DATABASE.md (17.6KB):
  Pre-populated 32 prospects across 12 categories:
  C1 hyperscaler (AWS/GCP/Azure/DO/Hetzner/OVH/Linode/Vultr),
  C2 Web3-native (Akash/Filecoin/Aleph/Fleek), C3 DePIN
  (Render/io.net/Spheron), C4 Polkadot/Substrate (W3F Grants,
  Substrate Builder, Moonbeam, Astar, Pioneers), C5 cross-chain
  (ETH ESP, Arbitrum, Optimism RPGF, Polygon, Base), C6 EVM
  (Gitcoin, Octant, Giveth), C7 SVM (Solana Foundation, Metaplex),
  C8 audit (W3F Audit, OZ, ToB, Cantina, C4rena), C9 bug bounty
  (Immunefi, HackerOne, Cantina), C10 legal (LexDAO, a16z, local),
  C11 hardware/ISP/datacenter (Equinix, Hetzner dedicated, OVH bare
  metal, home ISP, Helium/World Mobile Token), C12 OSS foundations
  (Linux Foundation, Apache, Open Source Collective, GitHub Sponsors).

RESEARCH AGENTS SPAWNED (waiting on completion events):
- grants-cloud (runId 1c3fa8bc, child 9db9c12b-...): C1/C2/C3 deep-dive
- grants-ecosystem (runId 4d10e5c9, child 9cbef91d-...): C4/C5/C6/C7
- grants-audit-hardware (runId 55193b33, child b792fab1-...): C8/C9/C10/C11/C12
  -- HEAVY PRIORITY on C11 (hardware/ISP/datacenter per user's specific ask)

❓ ITEMS RESOLVED in this session via verification:
- Graceful shutdown: ✅ Real (node/src/service.rs:111 shutdown_tx via tokio)
- Storage migrations: ✅ Real (10 pallets have migrations.rs with OnRuntimeUpgrade:
  agent-accounts, agent-memory, atomic-trade-engine, evolution-core,
  governance, meme-overlord, svm-runtime, treasury, x3-kernel, x3-verifier)
- Backup/restore: ✅ Real (scripts/snapshot-restore.sh with backup/restore/list modes)
- Daemon main: ✅ Real (crates/x3-chain-health-daemon/src/main.rs: 389 LOC async fn main)
- TypeScript tests: ✅ 964 files in apps/
- Reward model: ⚠️ Partial (x3-court has reward_challenger, x3-fees has reward logic;
  full staking/inflation/nominate not implemented — deferred to M3 per LAUNCH_SCOPE)
- Atlas Kernel tests: ✅ 36 in pallets/x3-atomic-kernel/tests.rs + AtlasHTLC + AtlasSphereX3 in forge
- ABI verifier: ❌ Not found in standard search; external ABI check is in
  X3-contracts/evm/test/ via Solidity function selectors, not Rust-side verifier

## native_locked_supply tests added (commit 6a24d8cf, mine) 2026-09-05
Context: task #2 from the suggested-next-work list. Confirmed both gates still green for prior work (`cargo check -p x3-chain-node` EXIT 0 after token_getSupply wiring; strict `cargo clippy --workspace --all-targets -- -D warnings` EXIT 0 in the earlier stub-sweep session). Added `mod native_supply_contract_tests` at END of runtime/src/lib.rs `#[cfg(all(test, feature = "std"))]`.
3 tests, ALL PASS (`cargo test -p x3-chain-runtime --lib native_supply_contract` → 3 passed, 30 filtered): empty-treasury=0; treasury deposit counted as protocol-locked not circulating; user balance NOT counted as locked. Strict clippy on runtime `--lib --tests -- -D warnings` EXIT 0 (module is lint-clean incl. `let _ = deposit_creating` and bare `free_balance`/`reserved_balance`).
KEY gotcha (do not re-derive): real runtime `TestExternalities::default()` has NO genesis funds; to seed an account via `pallet_balances::Pallet::<Runtime>::deposit_creating` the amount MUST exceed ExistentialDeposit (= 100 * MICRO_ATLAS = 100_000 base units; MICRO_ATLAS=1_000 base, X3=1e9 base). A `user_seed = 7_777` silently did NOT credit the account (free_balance stayed 0) → sub-ED mint dropped; use >= ~5_000_000 base units (e.g. 5 mATLAS) for funded-user fixtures. `reserved_balance`/`free_balance` resolve fine in the child test module via `use super::*` (root `use frame_support::traits::Currency` line 57); NO local trait import needed.
DESIGN FACT (this is why tests are at the storage-accessor level, not method level): `native_locked_supply()` is declared in pallet-x3-kernel `AtlasKernelRuntimeApi` and IMPLEMENTED ONLY in runtime/src/lib.rs `impl_runtime_apis!`. It is a runtime-API method invocable ONLY through a client-side RuntimeApi executor (node/src/rpc.rs token_getSupply). The kernel-pallet mock (mock.rs) does NOT implement the RuntimeApi and has no treasury pallet, so it cannot test it. The runtime crate test can't call the method body without a full sp_api client harness (not vendored). So the faithful unit seam = assert the exact `pallet_balances` accessors the method body reads (guards the RPC's circulating=total-locked contract); true method-level coverage belongs to the node/e2e suite (tests/e2e). Do NOT try to invent an executor harness — out of pattern, brittle.
Note: working tree during this run had LOTS of concurrent-session modifications (Cargo.lock, FEATURE_REGISTRY.toml, x3-verification-router/, x3-crosschain-gateway/, docs, scripts, tests/e2e/Cargo.toml, etc. + commits like 5ae43f96). Those are NOT mine; I staged/committed ONLY runtime/src/lib.rs (6a24d8cf). Never `git add -A` here. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
# Memory 2026-09-05

## User request this session: "scan full repo and finish up any stubs and make sure everything is wired in tight"
Comprehensive full-repo stub sweep + wiring verification. Both Rust AGENTS gates now proven GREEN and 2 genuine live gaps closed. Durable findings & decisions below. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Gate proofs (run 2026-09-05, do not re-derive)
- `cargo check --workspace` → EXIT 0
- `cargo clippy --workspace --all-targets -- -D warnings` → EXIT 0 (was 101 BEFORE this session's fix)
- Zero `todo!()`/`unimplemented!()` macros remain in executable production (non-comment) lines of core live dirs (runtime/, pallets/, crates/, node/src). Prior hardening sessions really cleared them.
- `scripts/no_stub_guard.py` full-repo scan exits 0 (it only BLOCKS on staged new lines; whole-repo run is informational ~604 markers, mostly docs/tooling/seed — expected).

## Fix #1 — x3-chain-health-daemon clippy-gate breach (commit 422a9fb9)
`crates/x3-chain-health-daemon/src/main.rs` had top-level `#[cfg(test)] mod tests` BEFORE `#[tokio::main] async fn main()` → `clippy::items-after-test-module` FAILED the exact AGENTS gate. Fixed by relocating the tests module to be the LAST top-level item (tests run at EOF, after main). Crate clippy `-D warnings` EXIT 0; 4/4 bin tests pass. This crate came from the USB merge (`14fe3a78`) and had never been gate-checked — a latent breach. LESSON: any newly-merged crate must immediately run the strict workspace clippy, not just cargo check. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Fix #2 — token_getSupply RPC fabricated supply (commit 1f27cff1)
`node/src/rpc.rs` `token_getSupply` hardcoded `locked: u128 = 0` ("placeholder — wire to treasury_reserved") → desktop Supply Dashboard (SupplyDashboardPanel.tsx via rpc-client.ts + Tauri main.rs) showed locked=0 / circulating=total forever = FALSE data, not a true zero.
Fix: added `AtlasKernelRuntimeApi::native_locked_supply() -> u128` (pallets/x3-kernel trait decl after get_total_issuance; runtime/src/lib.rs impl after get_total_issuance) that returns the REAL free+reserved native balance held by the protocol treasury account (`TreasuryAccountId`). Node RPC now computes circulating = total_issuance − locked.
KEY implementation facts (do not re-derive):
- This runtime has NO pallet-staking (no `Staking:` in construct_runtime, no pallet-staking dep in runtime Cargo.toml) → there is NO on-chain staking-ledger lock to sum. "Locked" native = balances reserved on protocol/treasury accounts only. Treasury-held is the only clean, real definition.
- `TreasuryAccountId` (parameter_types!, `frame_support::PalletId(*b"py/trsry")`) is module-scope reachable from the runtime impl.
- In the runtime impl, do NOT add a local `use frame_support::traits::{Currency,...}` — `free_balance` already resolves from file-level `Currency` import (line ~57) and `reserved_balance` from file scope too; a local import is UNUSED → fails under `-D warnings`. Just call `pallet_balances::Pallet::<Runtime>::free_balance(&acct)` / `reserved_balance(&acct)` with NO local trait import.
- Runtime API trait method addition is safe vs test mocks: the only other implementor is `crates/x3-bridge-adapters/src/lib.rs:754` `RuntimeApiHarness` inside `#[cfg(test)]` via `mock_impl_runtime_apis!` which auto-provides new method bodies → no mock breakage; full-workspace `--all-targets` clippy EXIT 0 confirms.
- `pallet_staking` split note: upstream staking crates are a dependency only at repo-root/node level for validator tooling, NOT in the runtime. Don't try to sum ledger locks.

## Reachability audit disposition (scanned stubs LEFT as-is, each verified NOT on a live path)
- `cross-vm-bridge/src/connector.rs` `LiveNodeDispatcher::get_evm_bridge_escrow/get_svm_bridge_escrow` return zeroed address + warn ("RPC plumbing not yet wired"): ZERO references anywhere except its own module. Production x3-kernel commit path uses `KernelCrossVmDispatcher`; real escrow fetch lives in x3-bridge-adapters' typed-client adapter (`api.get_evm_bridge_escrow(at)`). Orphaned connector — no live caller → safe to leave as documented zero-fallback. Do NOT fabricate RPC plumbing in it.
- `crates/x3-gpu-validator-swarm x3_kernel_versioning::execute_kernel` canned matmul/conv2d bytes: referenced only by its own test (versioning.rs:530); no validator/finality/cpu-fallback path calls it. Dead mock scaffolding in unused public API.
- `flash-finality sign_with_keystore` None + `[0u8;64]` fallback: opt-in only via `enable_flash_finality` flag (default OFF); unit test enforces "flash finality must stay off"; GRANDPA is authoritative finality. Shadow gadget.
- `x3-vm universal_escrow.rs:161` InMemoryEscrowProvider: self-labeled test/development in-memory impl of an escrow trait; real chain-backed provider needs a live external chain (out of scope/un-wireable here).
- `node/src/command.rs:461` comit-status CLI prints "Full Comit query endpoint not yet implemented": operator debugging tool that self-documents its limit (not silent). Leave.
- `runtime/src/lib.rs` various `return 0`: length/validation guards, benign.

## Reminder re user pre-existing work
At session start the working tree had the user's in-progress content: apps/x3-desktop SceneManager.tsx/DesktopIcons.tsx edits, untracked apps/x3-desktop CenterEye.tsx, IDENTITY.md/USER.md edits, an e2e test change, plus a parallel doc commit 5d411dc0. I deliberately did NOT touch/commit the user's in-progress files. Parallel-session commits (5d411dc0) may appear in the log; don't attribute them to this session. My commits: 422a9fb9 (health-daemon), 1f27cff1 (supply RPC).

## Standing next-work (unchanged from prior, resume whichever)
1. P0 cross-host consensus-mesh re-proof — loopback-bound only; run-mesh.py + deterministic PeerIds commit 8579b34d; needs real networked hosts.
2. Full `cargo test --workspace` needs an idle host re-run (env-dependent e2e SIGKILL `cross_vm_real_chain_test`).
3. pnpm / python -m pytest gates not runnable on this host (tooling absent).
4. Memory-search index mismatch (fts-only vs text-embedding-3-small) — reading files directly works.

## MASTER CHECKLIST + GRANTS DB (2026-09-05 ~00:00) — commits c81363e5 + spawns 1c3fa8bc/4d10e5c9/55193b33
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->
User: "send me this back with everything checked off that is complete...
and make agents to start finding grants for these costly items...
hardware, isp, datacenter, you know what i need".

DELIVERABLES (committed c81363e5):
- docs/current/MASTER_CHECKLIST_STATUS.md (16.3KB):
  Maps external v1.0.0 checklist paths (/runtime, /vm, /daemon, /ai,
  /sdk, /cli, /ui, /tests/econ, /ops) to ACTUAL repo layout
  (runtime/, crates/, apps/, X3-contracts/evm/, tests/e2e/). Uses
  live verification against HEAD 796d5361. ~25 ✅, ~15 ⚠️, ~10 ❌,
  ~5 ❓. ADDS 16 items the external checklist MISSED.

- grants/GRANTS_DATABASE.md (17.6KB):
  Pre-populated 32 prospects across 12 categories:
  C1 hyperscaler (AWS/GCP/Azure/DO/Hetzner/OVH/Linode/Vultr),
  C2 Web3-native (Akash/Filecoin/Aleph/Fleek), C3 DePIN
  (Render/io.net/Spheron), C4 Polkadot/Substrate (W3F Grants,
  Substrate Builder, Moonbeam, Astar, Pioneers), C5 cross-chain
  (ETH ESP, Arbitrum, Optimism RPGF, Polygon, Base), C6 EVM
  (Gitcoin, Octant, Giveth), C7 SVM (Solana Foundation, Metaplex),
  C8 audit (W3F Audit, OZ, ToB, Cantina, C4rena), C9 bug bounty
  (Immunefi, HackerOne, Cantina), C10 legal (LexDAO, a16z, local),
  C11 hardware/ISP/datacenter (Equinix, Hetzner dedicated, OVH bare
  metal, home ISP, Helium/World Mobile Token), C12 OSS foundations
  (Linux Foundation, Apache, Open Source Collective, GitHub Sponsors).

RESEARCH AGENTS SPAWNED (waiting on completion events):
- grants-cloud (runId 1c3fa8bc, child 9db9c12b-...): C1/C2/C3 deep-dive
- grants-ecosystem (runId 4d10e5c9, child 9cbef91d-...): C4/C5/C6/C7
- grants-audit-hardware (runId 55193b33, child b792fab1-...): C8/C9/C10/C11/C12
  -- HEAVY PRIORITY on C11 (hardware/ISP/datacenter per user's specific ask)

❓ ITEMS RESOLVED in this session via verification:
- Graceful shutdown: ✅ Real (node/src/service.rs:111 shutdown_tx via tokio)
- Storage migrations: ✅ Real (10 pallets have migrations.rs with OnRuntimeUpgrade:
  agent-accounts, agent-memory, atomic-trade-engine, evolution-core,
  governance, meme-overlord, svm-runtime, treasury, x3-kernel, x3-verifier)
- Backup/restore: ✅ Real (scripts/snapshot-restore.sh with backup/restore/list modes)
- Daemon main: ✅ Real (crates/x3-chain-health-daemon/src/main.rs: 389 LOC async fn main)
- TypeScript tests: ✅ 964 files in apps/
- Reward model: ⚠️ Partial (x3-court has reward_challenger, x3-fees has reward logic;
  full staking/inflation/nominate not implemented — deferred to M3 per LAUNCH_SCOPE)
- Atlas Kernel tests: ✅ 36 in pallets/x3-atomic-kernel/tests.rs + AtlasHTLC + AtlasSphereX3 in forge
- ABI verifier: ❌ Not found in standard search; external ABI check is in
  X3-contracts/evm/test/ via Solidity function selectors, not Rust-side verifier

## GRANTS-DB C4-C7 PASS (subagent depth 1/1, 2026-09-05 ~00:23)
Parent: grants-ecosystem (runId 4d10e5c9, child 9cbef91d-...).
Task: verify rows C4.1-C7.2 and add any missing programs with working
URL, specific eligibility, current award range, currently-accepting
applications, and a 1-5 confidence rating. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

OUTCOME (committed in single edit to grants/GRANTS_DATABASE.md):
- 16 existing C4-C7 rows verified by `web_fetch` against the live URL
  on 2026-09-05. Each row's fit-notes field updated with a verified
  2026-09-05 timestamp and the actual current status.
- 4 new rows added: C6.4 Starknet Seed Grants, C6.5 Starknet Growth
  Grants, C6.6 Prezenti Celo Grants, C7.3 Superteam Earn Grants.

KEY HEADLINE CORRECTION (must propagate to user / master checklist):
- W3F Grants Program general intake is **DISCONTINUED** as of 2026.
  Old URL `grants.web3.foundation` redirects to W3F homepage; the
  canonical GitHub repo (`w3f/Grants-Program`) explicitly states no
  new intake and no open RFPs. The pre-populated C4.1 row's
  "$5k-$100k" figure is stale. The "P0 Apply to W3F Grants" action
  in the recommended-next-actions table is now WRONG; it must be
  replaced by either (a) Polkadot OpenGov Treasury referendum
  (C4.2, active, public-goods eligible), (b) W3F strategic/treasury
  relationship work, or (c) C8.1 W3F Audit track (requires an
  earlier W3F grant milestone, which is now blocked). This change
  ripples into the doc's P0 priority and the grant-application plan.

VERIFIED DEFUNCT/STALE (status now NOT-A-GRANT or replaced):
- C4.3 Substrate Builder Program → 404; no replacement.
- C4.4 Moonbeam/Moonriver Grants → `moonbeam.foundation` redirected
  to AI-agent network marketing page; old /grants/ 404s.
- C4.5 Astar Foundation → foundation page 404 shell, no grants.
- C4.6 Polkadot Pioneers Prize → DNS does not resolve.
- C5.3 Optimism RetroPGF old quest URL → only an OP Mainnet
  Gateway shell; no current application URL was verifiable. Keep
  polling the current Optimism docs index.
- C5.4 Polygon Village / Grants → URL redirects to `hadronfc.com`
  (Hadron Founders Club, not a Polygon program).
- C5.5 Base Ecosystem Fund → explicitly a Coinbase Ventures
  preseed/seed investment, NOT a cash grant.
- C6.1 Gitcoin Grants Stack → `grants.gitcoin.co` DNS failure, old
  `gitcoin.co/grants/` 404; current homepage is a funding
  knowledge base, not an application.
- C6.2 Octant → main site 403 from fetcher; no current published
  award, intake, or deadline. Re-check on next pass.
- C7.2 Metaplex Foundation → live protocol docs site, but no
  grant application, award range, or deadline.

VERIFIED ACTIVE (and notable mechanics):
- C4.2 Polkadot OpenGov Treasury → live. Anyone can submit a
  preimage + refundable decision deposit (~42 DOT guide). Runtime,
  tooling, and public-goods scopes are all in scope, not just dApps.
  This is now the strongest P0 candidate for a Substrate-project
  treasury ask in 2026.
- C5.1 Ethereum Foundation ESP → active but **Wishlist/RFP-based**,
  not a fixed $5k-$500k schedule. Office Hours open. The 2025
  Academic Grants round has closed; watch for the 2026 round.
- C5.2 Arbitrum Audit Program → currently **Active**, $10M total
  pool over 12 months. ArbiFuel also active but no published cash
  award. Generic Foundation/DAO programs inactive.
- C6.3 Giveth GIVbacks → live biweekly raffle, 1M GIV per round,
  eligible donations $5+, up to 500k GIV win, 50-80% donation-value
  factor; projects only benefit indirectly (donor-side rewards).
- C7.1 Solana Foundation Funding → rolling applications, ~1 wk
  initial review, ~3 wk target decision, milestone-based standard
  + convertible grants + RFPs.
- C7.3 Superteam Earn → $10k microgrants, but **only for builders
  in India, Southeast Asia, Eastern Europe, Africa**. US applicant
  not eligible. Confidence 1 unless a qualifying regional team
  applies.

INVESTIGATED BUT DELIBERATELY NOT ADDED (no qualifying public
award/eligibility/intake evidence):
- zkSync, Linea/Lineth, Scroll, Blast, Mode — checked each
  ecosystem's official grants page; current pages do not publish a
  specific award range + specific eligibility + live application
  simultaneously, so they failed the strict prospect criteria.
- Hyperliquid grants — no public grants program page located.
- Berachain grants — no public grants program page located.
- Octant current epoch — site inaccessible; no published
  award/deadline.
- Solana Hyperdrive — no resolvable official program page.
- Filecoin Pods Funding — current page lacks public award range.
- Starkware/Starknet Foundation direct — page redirects users to
  Starknet Seed/Growth forms; the foundation itself does not
  publish a separate open grant. Use the Seed/Growth Airtable
  forms (C6.4, C6.5) instead.

METHODOLOGY NOTES (for the next grants pass):
- In 2026, foundation grants increasingly use **Airtable forms** as
  the application surface (Starknet Seed, Starknet Growth). The
  `tinyurl.com/starknet-*` shortlinks resolve to those Airtable
  app URLs. Follow the same Airtable pattern for other ecosystems.
- Several "grants" are actually **VC investments** (Base Ecosystem
  Fund). Distinguish carefully: the row's `Covers` and `Award`
  fields must reflect cash grants, not equity.
- Many historical `$X-$Y award` ranges in pre-existing rows are
  STALE because programs have moved to RFP/Wishlist mechanics with
  no published range. Mark such rows with `No standard public range
  (RFP/Wishlist)` instead of fabricating a number.
- Regional programs (Superteam, some Indian/SEA/Africa/EE grants)
  are real but **invisible to a US-based applicant**. Tag
  confidence 1 unless a regional team qualifies.
- Some ecosystem "grants" pages exist for SEO/marketing but contain
  no application form (Metaplex, Moonbeam AI-agent rebrand). Status
  must be NOT-A-GRANT, not RESEARCH.
- Prezenti's URL is a real page, but its title showed 2024 H2
  content while the program claims a 29-Dec-2026 deadline; treat
  the deadline as unverified until re-confirmed. Add to next-pass
  reconfirmation list.

PARENT NEXT 10 TASKS (the ones the parent grants-ecosystem agent
should still do, not this subagent):
1. Recon firm Prezenti 2026 deadline before apply.
2. Watch W3F strategic-funding contacts / re-openings.
3. Re-poll Optimism docs index for current RetroPGF round URL.
4. Recheck Polygon / Hadron for a legitimate current program.
5. Recheck zkSync, Linea/Lineth, Scroll, Blast, Mode.
6. Recheck Hyperliquid and Berachain.
7. Re-run `web_fetch` on Octant and Gitcoin Grants Stack.
8. Recheck Moonbeam (post-AI-agent pivot) and Astar.
9. Update the recommended-next-actions table: W3F Grants is no
   longer P0; OpenGov Treasury is now P0 for any Substrate ask.
10. Update C8.1 W3F Audit Grants row text — note that the upstream
    general grant intake is closed, so the prerequisite path may
    need an alternate prior W3F milestone or a foundation
    relationship.

RUNTIME/INFRA FACTS learned this session (worth keeping for next
session re-derive avoidance):
- web_fetch in this environment can be blocked by 403 / bot
  detection on some sites (octant.app, some docs sites). On a 403
  treat as "unverified" rather than "defunct", and re-try later or
  via a different mirror.
- The grants DB's row format is fixed: 9 columns, pipe-table, no
  markdown emphasis inside cells beyond bold (**text**). New rows
  must follow the exact `| # | Program | Covers | Award |
  Eligibility | URL | Fit notes | Status | Confidence |` shape.
- The first edit MUST NOT alter the header row or the column
  definitions; `write` tool during memory flush is constrained to
  append-only on this single file but full-file edits to the
  grants DB are normal.

## Full `cargo test --workspace` PASSED (exit 0) 2026-09-05 ~10:43
Task #3 from suggested list finally run to completion on a quiet host (parallel code edits confirmed ≥8h stale → safe to hold the target dir toolchain lock). Command: `cargo test --workspace --offline` → WS_TEST_EXIT=0. Validates ALL 151 workspace crates: unit + integration + doc-tests, across the WHOLE tree at current state, which includes many concurrent-session modifications landed since my 1f27cff1 gate baseline (crates/x3-bridge bitcoin_htlc, x3-gateway rest, x3-relayer, x3-verification-router, pallets/x3-crosschain-gateway, x3-lang/compiler, x3-bitcoin-vault). No failures reported (exit 0 = every test binary passed). Execution progressed: pallet test binaries compiled in parallel (~8 rustc), then node integration binary (x3_chain_node), stress_harness, then doc-tests tail (x3_typeck/x3_universal_contracts/x3_verifier/x3_vm/x3_wallet/x3_integration etc. all ok). The known env-gated `cross_vm_real_chain_test` SIGKILL did NOT fail this run (exit 0). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
Meaning: the standing "full workspace test needs idle host" gap (from LAUNCH_SCOPE/readiness tracking) is now CLOSED at this state. Alongside earlier provable gates (cargo check --workspace EXIT 0, strict clippy --workspace --all-targets -D warnings EXIT 0 from the stub-sweep session), the Rust test+lint+build surface is green at HEAD ~6a24d8cf.
# Memory 2026-09-05

## User request this session: "scan full repo and finish up any stubs and make sure everything is wired in tight"
Comprehensive full-repo stub sweep + wiring verification. Both Rust AGENTS gates now proven GREEN and 2 genuine live gaps closed. Durable findings & decisions below. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Gate proofs (run 2026-09-05, do not re-derive)
- `cargo check --workspace` → EXIT 0
- `cargo clippy --workspace --all-targets -- -D warnings` → EXIT 0 (was 101 BEFORE this session's fix)
- Zero `todo!()`/`unimplemented!()` macros remain in executable production (non-comment) lines of core live dirs (runtime/, pallets/, crates/, node/src). Prior hardening sessions really cleared them.
- `scripts/no_stub_guard.py` full-repo scan exits 0 (it only BLOCKS on staged new lines; whole-repo run is informational ~604 markers, mostly docs/tooling/seed — expected).

## Fix #1 — x3-chain-health-daemon clippy-gate breach (commit 422a9fb9)
`crates/x3-chain-health-daemon/src/main.rs` had top-level `#[cfg(test)] mod tests` BEFORE `#[tokio::main] async fn main()` → `clippy::items-after-test-module` FAILED the exact AGENTS gate. Fixed by relocating the tests module to be the LAST top-level item (tests run at EOF, after main). Crate clippy `-D warnings` EXIT 0; 4/4 bin tests pass. This crate came from the USB merge (`14fe3a78`) and had never been gate-checked — a latent breach. LESSON: any newly-merged crate must immediately run the strict workspace clippy, not just cargo check. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Fix #2 — token_getSupply RPC fabricated supply (commit 1f27cff1)
`node/src/rpc.rs` `token_getSupply` hardcoded `locked: u128 = 0` ("placeholder — wire to treasury_reserved") → desktop Supply Dashboard (SupplyDashboardPanel.tsx via rpc-client.ts + Tauri main.rs) showed locked=0 / circulating=total forever = FALSE data, not a true zero.
Fix: added `AtlasKernelRuntimeApi::native_locked_supply() -> u128` (pallets/x3-kernel trait decl after get_total_issuance; runtime/src/lib.rs impl after get_total_issuance) that returns the REAL free+reserved native balance held by the protocol treasury account (`TreasuryAccountId`). Node RPC now computes circulating = total_issuance − locked.
KEY implementation facts (do not re-derive):
- This runtime has NO pallet-staking (no `Staking:` in construct_runtime, no pallet-staking dep in runtime Cargo.toml) → there is NO on-chain staking-ledger lock to sum. "Locked" native = balances reserved on protocol/treasury accounts only. Treasury-held is the only clean, real definition.
- `TreasuryAccountId` (parameter_types!, `frame_support::PalletId(*b"py/trsry")`) is module-scope reachable from the runtime impl.
- In the runtime impl, do NOT add a local `use frame_support::traits::{Currency,...}` — `free_balance` already resolves from file-level `Currency` import (line ~57) and `reserved_balance` from file scope too; a local import is UNUSED → fails under `-D warnings`. Just call `pallet_balances::Pallet::<Runtime>::free_balance(&acct)` / `reserved_balance(&acct)` with NO local trait import.
- Runtime API trait method addition is safe vs test mocks: the only other implementor is `crates/x3-bridge-adapters/src/lib.rs:754` `RuntimeApiHarness` inside `#[cfg(test)]` via `mock_impl_runtime_apis!` which auto-provides new method bodies → no mock breakage; full-workspace `--all-targets` clippy EXIT 0 confirms.
- `pallet_staking` split note: upstream staking crates are a dependency only at repo-root/node level for validator tooling, NOT in the runtime. Don't try to sum ledger locks.

## Reachability audit disposition (scanned stubs LEFT as-is, each verified NOT on a live path)
- `cross-vm-bridge/src/connector.rs` `LiveNodeDispatcher::get_evm_bridge_escrow/get_svm_bridge_escrow` return zeroed address + warn ("RPC plumbing not yet wired"): ZERO references anywhere except its own module. Production x3-kernel commit path uses `KernelCrossVmDispatcher`; real escrow fetch lives in x3-bridge-adapters' typed-client adapter (`api.get_evm_bridge_escrow(at)`). Orphaned connector — no live caller → safe to leave as documented zero-fallback. Do NOT fabricate RPC plumbing in it.
- `crates/x3-gpu-validator-swarm x3_kernel_versioning::execute_kernel` canned matmul/conv2d bytes: referenced only by its own test (versioning.rs:530); no validator/finality/cpu-fallback path calls it. Dead mock scaffolding in unused public API.
- `flash-finality sign_with_keystore` None + `[0u8;64]` fallback: opt-in only via `enable_flash_finality` flag (default OFF); unit test enforces "flash finality must stay off"; GRANDPA is authoritative finality. Shadow gadget.
- `x3-vm universal_escrow.rs:161` InMemoryEscrowProvider: self-labeled test/development in-memory impl of an escrow trait; real chain-backed provider needs a live external chain (out of scope/un-wireable here).
- `node/src/command.rs:461` comit-status CLI prints "Full Comit query endpoint not yet implemented": operator debugging tool that self-documents its limit (not silent). Leave.
- `runtime/src/lib.rs` various `return 0`: length/validation guards, benign.

## Reminder re user pre-existing work
At session start the working tree had the user's in-progress content: apps/x3-desktop SceneManager.tsx/DesktopIcons.tsx edits, untracked apps/x3-desktop CenterEye.tsx, IDENTITY.md/USER.md edits, an e2e test change, plus a parallel doc commit 5d411dc0. I deliberately did NOT touch/commit the user's in-progress files. Parallel-session commits (5d411dc0) may appear in the log; don't attribute them to this session. My commits: 422a9fb9 (health-daemon), 1f27cff1 (supply RPC).

## Standing next-work (unchanged from prior, resume whichever)
1. P0 cross-host consensus-mesh re-proof — loopback-bound only; run-mesh.py + deterministic PeerIds commit 8579b34d; needs real networked hosts.
2. Full `cargo test --workspace` needs an idle host re-run (env-dependent e2e SIGKILL `cross_vm_real_chain_test`).
3. pnpm / python -m pytest gates not runnable on this host (tooling absent).
4. Memory-search index mismatch (fts-only vs text-embedding-3-small) — reading files directly works.

## MASTER CHECKLIST + GRANTS DB (2026-09-05 ~00:00) — commits c81363e5 + spawns 1c3fa8bc/4d10e5c9/55193b33
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->
User: "send me this back with everything checked off that is complete...
and make agents to start finding grants for these costly items...
hardware, isp, datacenter, you know what i need".

DELIVERABLES (committed c81363e5):
- docs/current/MASTER_CHECKLIST_STATUS.md (16.3KB):
  Maps external v1.0.0 checklist paths (/runtime, /vm, /daemon, /ai,
  /sdk, /cli, /ui, /tests/econ, /ops) to ACTUAL repo layout
  (runtime/, crates/, apps/, X3-contracts/evm/, tests/e2e/). Uses
  live verification against HEAD 796d5361. ~25 ✅, ~15 ⚠️, ~10 ❌,
  ~5 ❓. ADDS 16 items the external checklist MISSED.

- grants/GRANTS_DATABASE.md (17.6KB):
  Pre-populated 32 prospects across 12 categories:
  C1 hyperscaler (AWS/GCP/Azure/DO/Hetzner/OVH/Linode/Vultr),
  C2 Web3-native (Akash/Filecoin/Aleph/Fleek), C3 DePIN
  (Render/io.net/Spheron), C4 Polkadot/Substrate (W3F Grants,
  Substrate Builder, Moonbeam, Astar, Pioneers), C5 cross-chain
  (ETH ESP, Arbitrum, Optimism RPGF, Polygon, Base), C6 EVM
  (Gitcoin, Octant, Giveth), C7 SVM (Solana Foundation, Metaplex),
  C8 audit (W3F Audit, OZ, ToB, Cantina, C4rena), C9 bug bounty
  (Immunefi, HackerOne, Cantina), C10 legal (LexDAO, a16z, local),
  C11 hardware/ISP/datacenter (Equinix, Hetzner dedicated, OVH bare
  metal, home ISP, Helium/World Mobile Token), C12 OSS foundations
  (Linux Foundation, Apache, Open Source Collective, GitHub Sponsors).

RESEARCH AGENTS SPAWNED (waiting on completion events):
- grants-cloud (runId 1c3fa8bc, child 9db9c12b-...): C1/C2/C3 deep-dive
- grants-ecosystem (runId 4d10e5c9, child 9cbef91d-...): C4/C5/C6/C7
- grants-audit-hardware (runId 55193b33, child b792fab1-...): C8/C9/C10/C11/C12
  -- HEAVY PRIORITY on C11 (hardware/ISP/datacenter per user's specific ask)

❓ ITEMS RESOLVED in this session via verification:
- Graceful shutdown: ✅ Real (node/src/service.rs:111 shutdown_tx via tokio)
- Storage migrations: ✅ Real (10 pallets have migrations.rs with OnRuntimeUpgrade:
  agent-accounts, agent-memory, atomic-trade-engine, evolution-core,
  governance, meme-overlord, svm-runtime, treasury, x3-kernel, x3-verifier)
- Backup/restore: ✅ Real (scripts/snapshot-restore.sh with backup/restore/list modes)
- Daemon main: ✅ Real (crates/x3-chain-health-daemon/src/main.rs: 389 LOC async fn main)
- TypeScript tests: ✅ 964 files in apps/
- Reward model: ⚠️ Partial (x3-court has reward_challenger, x3-fees has reward logic;
  full staking/inflation/nominate not implemented — deferred to M3 per LAUNCH_SCOPE)
- Atlas Kernel tests: ✅ 36 in pallets/x3-atomic-kernel/tests.rs + AtlasHTLC + AtlasSphereX3 in forge
- ABI verifier: ❌ Not found in standard search; external ABI check is in
  X3-contracts/evm/test/ via Solidity function selectors, not Rust-side verifier

## native_locked_supply tests added (commit 6a24d8cf, mine) 2026-09-05
Context: task #2 from the suggested-next-work list. Confirmed both gates still green for prior work (`cargo check -p x3-chain-node` EXIT 0 after token_getSupply wiring; strict `cargo clippy --workspace --all-targets -- -D warnings` EXIT 0 in the earlier stub-sweep session). Added `mod native_supply_contract_tests` at END of runtime/src/lib.rs `#[cfg(all(test, feature = "std"))]`.
3 tests, ALL PASS (`cargo test -p x3-chain-runtime --lib native_supply_contract` → 3 passed, 30 filtered): empty-treasury=0; treasury deposit counted as protocol-locked not circulating; user balance NOT counted as locked. Strict clippy on runtime `--lib --tests -- -D warnings` EXIT 0 (module is lint-clean incl. `let _ = deposit_creating` and bare `free_balance`/`reserved_balance`).
KEY gotcha (do not re-derive): real runtime `TestExternalities::default()` has NO genesis funds; to seed an account via `pallet_balances::Pallet::<Runtime>::deposit_creating` the amount MUST exceed ExistentialDeposit (= 100 * MICRO_ATLAS = 100_000 base units; MICRO_ATLAS=1_000 base, X3=1e9 base). A `user_seed = 7_777` silently did NOT credit the account (free_balance stayed 0) → sub-ED mint dropped; use >= ~5_000_000 base units (e.g. 5 mATLAS) for funded-user fixtures. `reserved_balance`/`free_balance` resolve fine in the child test module via `use super::*` (root `use frame_support::traits::Currency` line 57); NO local trait import needed.
DESIGN FACT (this is why tests are at the storage-accessor level, not method level): `native_locked_supply()` is declared in pallet-x3-kernel `AtlasKernelRuntimeApi` and IMPLEMENTED ONLY in runtime/src/lib.rs `impl_runtime_apis!`. It is a runtime-API method invocable ONLY through a client-side RuntimeApi executor (node/src/rpc.rs token_getSupply). The kernel-pallet mock (mock.rs) does NOT implement the RuntimeApi and has no treasury pallet, so it cannot test it. The runtime crate test can't call the method body without a full sp_api client harness (not vendored). So the faithful unit seam = assert the exact `pallet_balances` accessors the method body reads (guards the RPC's circulating=total-locked contract); true method-level coverage belongs to the node/e2e suite (tests/e2e). Do NOT try to invent an executor harness — out of pattern, brittle.
Note: working tree during this run had LOTS of concurrent-session modifications (Cargo.lock, FEATURE_REGISTRY.toml, x3-verification-router/, x3-crosschain-gateway/, docs, scripts, tests/e2e/Cargo.toml, etc. + commits like 5ae43f96). Those are NOT mine; I staged/committed ONLY runtime/src/lib.rs (6a24d8cf). Never `git add -A` here. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
# Memory 2026-09-05

## User request this session: "scan full repo and finish up any stubs and make sure everything is wired in tight"
Comprehensive full-repo stub sweep + wiring verification. Both Rust AGENTS gates now proven GREEN and 2 genuine live gaps closed. Durable findings & decisions below. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Gate proofs (run 2026-09-05, do not re-derive)
- `cargo check --workspace` → EXIT 0
- `cargo clippy --workspace --all-targets -- -D warnings` → EXIT 0 (was 101 BEFORE this session's fix)
- Zero `todo!()`/`unimplemented!()` macros remain in executable production (non-comment) lines of core live dirs (runtime/, pallets/, crates/, node/src). Prior hardening sessions really cleared them.
- `scripts/no_stub_guard.py` full-repo scan exits 0 (it only BLOCKS on staged new lines; whole-repo run is informational ~604 markers, mostly docs/tooling/seed — expected).

## Fix #1 — x3-chain-health-daemon clippy-gate breach (commit 422a9fb9)
`crates/x3-chain-health-daemon/src/main.rs` had top-level `#[cfg(test)] mod tests` BEFORE `#[tokio::main] async fn main()` → `clippy::items-after-test-module` FAILED the exact AGENTS gate. Fixed by relocating the tests module to be the LAST top-level item (tests run at EOF, after main). Crate clippy `-D warnings` EXIT 0; 4/4 bin tests pass. This crate came from the USB merge (`14fe3a78`) and had never been gate-checked — a latent breach. LESSON: any newly-merged crate must immediately run the strict workspace clippy, not just cargo check. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

## Fix #2 — token_getSupply RPC fabricated supply (commit 1f27cff1)
`node/src/rpc.rs` `token_getSupply` hardcoded `locked: u128 = 0` ("placeholder — wire to treasury_reserved") → desktop Supply Dashboard (SupplyDashboardPanel.tsx via rpc-client.ts + Tauri main.rs) showed locked=0 / circulating=total forever = FALSE data, not a true zero.
Fix: added `AtlasKernelRuntimeApi::native_locked_supply() -> u128` (pallets/x3-kernel trait decl after get_total_issuance; runtime/src/lib.rs impl after get_total_issuance) that returns the REAL free+reserved native balance held by the protocol treasury account (`TreasuryAccountId`). Node RPC now computes circulating = total_issuance − locked.
KEY implementation facts (do not re-derive):
- This runtime has NO pallet-staking (no `Staking:` in construct_runtime, no pallet-staking dep in runtime Cargo.toml) → there is NO on-chain staking-ledger lock to sum. "Locked" native = balances reserved on protocol/treasury accounts only. Treasury-held is the only clean, real definition.
- `TreasuryAccountId` (parameter_types!, `frame_support::PalletId(*b"py/trsry")`) is module-scope reachable from the runtime impl.
- In the runtime impl, do NOT add a local `use frame_support::traits::{Currency,...}` — `free_balance` already resolves from file-level `Currency` import (line ~57) and `reserved_balance` from file scope too; a local import is UNUSED → fails under `-D warnings`. Just call `pallet_balances::Pallet::<Runtime>::free_balance(&acct)` / `reserved_balance(&acct)` with NO local trait import.
- Runtime API trait method addition is safe vs test mocks: the only other implementor is `crates/x3-bridge-adapters/src/lib.rs:754` `RuntimeApiHarness` inside `#[cfg(test)]` via `mock_impl_runtime_apis!` which auto-provides new method bodies → no mock breakage; full-workspace `--all-targets` clippy EXIT 0 confirms.
- `pallet_staking` split note: upstream staking crates are a dependency only at repo-root/node level for validator tooling, NOT in the runtime. Don't try to sum ledger locks.

## Reachability audit disposition (scanned stubs LEFT as-is, each verified NOT on a live path)
- `cross-vm-bridge/src/connector.rs` `LiveNodeDispatcher::get_evm_bridge_escrow/get_svm_bridge_escrow` return zeroed address + warn ("RPC plumbing not yet wired"): ZERO references anywhere except its own module. Production x3-kernel commit path uses `KernelCrossVmDispatcher`; real escrow fetch lives in x3-bridge-adapters' typed-client adapter (`api.get_evm_bridge_escrow(at)`). Orphaned connector — no live caller → safe to leave as documented zero-fallback. Do NOT fabricate RPC plumbing in it.
- `crates/x3-gpu-validator-swarm x3_kernel_versioning::execute_kernel` canned matmul/conv2d bytes: referenced only by its own test (versioning.rs:530); no validator/finality/cpu-fallback path calls it. Dead mock scaffolding in unused public API.
- `flash-finality sign_with_keystore` None + `[0u8;64]` fallback: opt-in only via `enable_flash_finality` flag (default OFF); unit test enforces "flash finality must stay off"; GRANDPA is authoritative finality. Shadow gadget.
- `x3-vm universal_escrow.rs:161` InMemoryEscrowProvider: self-labeled test/development in-memory impl of an escrow trait; real chain-backed provider needs a live external chain (out of scope/un-wireable here).
- `node/src/command.rs:461` comit-status CLI prints "Full Comit query endpoint not yet implemented": operator debugging tool that self-documents its limit (not silent). Leave.
- `runtime/src/lib.rs` various `return 0`: length/validation guards, benign.

## Reminder re user pre-existing work
At session start the working tree had the user's in-progress content: apps/x3-desktop SceneManager.tsx/DesktopIcons.tsx edits, untracked apps/x3-desktop CenterEye.tsx, IDENTITY.md/USER.md edits, an e2e test change, plus a parallel doc commit 5d411dc0. I deliberately did NOT touch/commit the user's in-progress files. Parallel-session commits (5d411dc0) may appear in the log; don't attribute them to this session. My commits: 422a9fb9 (health-daemon), 1f27cff1 (supply RPC).

## Standing next-work (unchanged from prior, resume whichever)
1. P0 cross-host consensus-mesh re-proof — loopback-bound only; run-mesh.py + deterministic PeerIds commit 8579b34d; needs real networked hosts.
2. Full `cargo test --workspace` needs an idle host re-run (env-dependent e2e SIGKILL `cross_vm_real_chain_test`).
3. pnpm / python -m pytest gates not runnable on this host (tooling absent).
4. Memory-search index mismatch (fts-only vs text-embedding-3-small) — reading files directly works.

## MASTER CHECKLIST + GRANTS DB (2026-09-05 ~00:00) — commits c81363e5 + spawns 1c3fa8bc/4d10e5c9/55193b33
<!-- project: path:/home/lojak/Desktop/xxxstar-main -->
User: "send me this back with everything checked off that is complete...
and make agents to start finding grants for these costly items...
hardware, isp, datacenter, you know what i need".

DELIVERABLES (committed c81363e5):
- docs/current/MASTER_CHECKLIST_STATUS.md (16.3KB):
  Maps external v1.0.0 checklist paths (/runtime, /vm, /daemon, /ai,
  /sdk, /cli, /ui, /tests/econ, /ops) to ACTUAL repo layout
  (runtime/, crates/, apps/, X3-contracts/evm/, tests/e2e/). Uses
  live verification against HEAD 796d5361. ~25 ✅, ~15 ⚠️, ~10 ❌,
  ~5 ❓. ADDS 16 items the external checklist MISSED.

- grants/GRANTS_DATABASE.md (17.6KB):
  Pre-populated 32 prospects across 12 categories:
  C1 hyperscaler (AWS/GCP/Azure/DO/Hetzner/OVH/Linode/Vultr),
  C2 Web3-native (Akash/Filecoin/Aleph/Fleek), C3 DePIN
  (Render/io.net/Spheron), C4 Polkadot/Substrate (W3F Grants,
  Substrate Builder, Moonbeam, Astar, Pioneers), C5 cross-chain
  (ETH ESP, Arbitrum, Optimism RPGF, Polygon, Base), C6 EVM
  (Gitcoin, Octant, Giveth), C7 SVM (Solana Foundation, Metaplex),
  C8 audit (W3F Audit, OZ, ToB, Cantina, C4rena), C9 bug bounty
  (Immunefi, HackerOne, Cantina), C10 legal (LexDAO, a16z, local),
  C11 hardware/ISP/datacenter (Equinix, Hetzner dedicated, OVH bare
  metal, home ISP, Helium/World Mobile Token), C12 OSS foundations
  (Linux Foundation, Apache, Open Source Collective, GitHub Sponsors).

RESEARCH AGENTS SPAWNED (waiting on completion events):
- grants-cloud (runId 1c3fa8bc, child 9db9c12b-...): C1/C2/C3 deep-dive
- grants-ecosystem (runId 4d10e5c9, child 9cbef91d-...): C4/C5/C6/C7
- grants-audit-hardware (runId 55193b33, child b792fab1-...): C8/C9/C10/C11/C12
  -- HEAVY PRIORITY on C11 (hardware/ISP/datacenter per user's specific ask)

❓ ITEMS RESOLVED in this session via verification:
- Graceful shutdown: ✅ Real (node/src/service.rs:111 shutdown_tx via tokio)
- Storage migrations: ✅ Real (10 pallets have migrations.rs with OnRuntimeUpgrade:
  agent-accounts, agent-memory, atomic-trade-engine, evolution-core,
  governance, meme-overlord, svm-runtime, treasury, x3-kernel, x3-verifier)
- Backup/restore: ✅ Real (scripts/snapshot-restore.sh with backup/restore/list modes)
- Daemon main: ✅ Real (crates/x3-chain-health-daemon/src/main.rs: 389 LOC async fn main)
- TypeScript tests: ✅ 964 files in apps/
- Reward model: ⚠️ Partial (x3-court has reward_challenger, x3-fees has reward logic;
  full staking/inflation/nominate not implemented — deferred to M3 per LAUNCH_SCOPE)
- Atlas Kernel tests: ✅ 36 in pallets/x3-atomic-kernel/tests.rs + AtlasHTLC + AtlasSphereX3 in forge
- ABI verifier: ❌ Not found in standard search; external ABI check is in
  X3-contracts/evm/test/ via Solidity function selectors, not Rust-side verifier

## GRANTS-DB C4-C7 PASS (subagent depth 1/1, 2026-09-05 ~00:23)
Parent: grants-ecosystem (runId 4d10e5c9, child 9cbef91d-...).
Task: verify rows C4.1-C7.2 and add any missing programs with working
URL, specific eligibility, current award range, currently-accepting
applications, and a 1-5 confidence rating. <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

OUTCOME (committed in single edit to grants/GRANTS_DATABASE.md):
- 16 existing C4-C7 rows verified by `web_fetch` against the live URL
  on 2026-09-05. Each row's fit-notes field updated with a verified
  2026-09-05 timestamp and the actual current status.
- 4 new rows added: C6.4 Starknet Seed Grants, C6.5 Starknet Growth
  Grants, C6.6 Prezenti Celo Grants, C7.3 Superteam Earn Grants.

KEY HEADLINE CORRECTION (must propagate to user / master checklist):
- W3F Grants Program general intake is **DISCONTINUED** as of 2026.
  Old URL `grants.web3.foundation` redirects to W3F homepage; the
  canonical GitHub repo (`w3f/Grants-Program`) explicitly states no
  new intake and no open RFPs. The pre-populated C4.1 row's
  "$5k-$100k" figure is stale. The "P0 Apply to W3F Grants" action
  in the recommended-next-actions table is now WRONG; it must be
  replaced by either (a) Polkadot OpenGov Treasury referendum
  (C4.2, active, public-goods eligible), (b) W3F strategic/treasury
  relationship work, or (c) C8.1 W3F Audit track (requires an
  earlier W3F grant milestone, which is now blocked). This change
  ripples into the doc's P0 priority and the grant-application plan.

VERIFIED DEFUNCT/STALE (status now NOT-A-GRANT or replaced):
- C4.3 Substrate Builder Program → 404; no replacement.
- C4.4 Moonbeam/Moonriver Grants → `moonbeam.foundation` redirected
  to AI-agent network marketing page; old /grants/ 404s.
- C4.5 Astar Foundation → foundation page 404 shell, no grants.
- C4.6 Polkadot Pioneers Prize → DNS does not resolve.
- C5.3 Optimism RetroPGF old quest URL → only an OP Mainnet
  Gateway shell; no current application URL was verifiable. Keep
  polling the current Optimism docs index.
- C5.4 Polygon Village / Grants → URL redirects to `hadronfc.com`
  (Hadron Founders Club, not a Polygon program).
- C5.5 Base Ecosystem Fund → explicitly a Coinbase Ventures
  preseed/seed investment, NOT a cash grant.
- C6.1 Gitcoin Grants Stack → `grants.gitcoin.co` DNS failure, old
  `gitcoin.co/grants/` 404; current homepage is a funding
  knowledge base, not an application.
- C6.2 Octant → main site 403 from fetcher; no current published
  award, intake, or deadline. Re-check on next pass.
- C7.2 Metaplex Foundation → live protocol docs site, but no
  grant application, award range, or deadline.

VERIFIED ACTIVE (and notable mechanics):
- C4.2 Polkadot OpenGov Treasury → live. Anyone can submit a
  preimage + refundable decision deposit (~42 DOT guide). Runtime,
  tooling, and public-goods scopes are all in scope, not just dApps.
  This is now the strongest P0 candidate for a Substrate-project
  treasury ask in 2026.
- C5.1 Ethereum Foundation ESP → active but **Wishlist/RFP-based**,
  not a fixed $5k-$500k schedule. Office Hours open. The 2025
  Academic Grants round has closed; watch for the 2026 round.
- C5.2 Arbitrum Audit Program → currently **Active**, $10M total
  pool over 12 months. ArbiFuel also active but no published cash
  award. Generic Foundation/DAO programs inactive.
- C6.3 Giveth GIVbacks → live biweekly raffle, 1M GIV per round,
  eligible donations $5+, up to 500k GIV win, 50-80% donation-value
  factor; projects only benefit indirectly (donor-side rewards).
- C7.1 Solana Foundation Funding → rolling applications, ~1 wk
  initial review, ~3 wk target decision, milestone-based standard
  + convertible grants + RFPs.
- C7.3 Superteam Earn → $10k microgrants, but **only for builders
  in India, Southeast Asia, Eastern Europe, Africa**. US applicant
  not eligible. Confidence 1 unless a qualifying regional team
  applies.

INVESTIGATED BUT DELIBERATELY NOT ADDED (no qualifying public
award/eligibility/intake evidence):
- zkSync, Linea/Lineth, Scroll, Blast, Mode — checked each
  ecosystem's official grants page; current pages do not publish a
  specific award range + specific eligibility + live application
  simultaneously, so they failed the strict prospect criteria.
- Hyperliquid grants — no public grants program page located.
- Berachain grants — no public grants program page located.
- Octant current epoch — site inaccessible; no published
  award/deadline.
- Solana Hyperdrive — no resolvable official program page.
- Filecoin Pods Funding — current page lacks public award range.
- Starkware/Starknet Foundation direct — page redirects users to
  Starknet Seed/Growth forms; the foundation itself does not
  publish a separate open grant. Use the Seed/Growth Airtable
  forms (C6.4, C6.5) instead.

METHODOLOGY NOTES (for the next grants pass):
- In 2026, foundation grants increasingly use **Airtable forms** as
  the application surface (Starknet Seed, Starknet Growth). The
  `tinyurl.com/starknet-*` shortlinks resolve to those Airtable
  app URLs. Follow the same Airtable pattern for other ecosystems.
- Several "grants" are actually **VC investments** (Base Ecosystem
  Fund). Distinguish carefully: the row's `Covers` and `Award`
  fields must reflect cash grants, not equity.
- Many historical `$X-$Y award` ranges in pre-existing rows are
  STALE because programs have moved to RFP/Wishlist mechanics with
  no published range. Mark such rows with `No standard public range
  (RFP/Wishlist)` instead of fabricating a number.
- Regional programs (Superteam, some Indian/SEA/Africa/EE grants)
  are real but **invisible to a US-based applicant**. Tag
  confidence 1 unless a regional team qualifies.
- Some ecosystem "grants" pages exist for SEO/marketing but contain
  no application form (Metaplex, Moonbeam AI-agent rebrand). Status
  must be NOT-A-GRANT, not RESEARCH.
- Prezenti's URL is a real page, but its title showed 2024 H2
  content while the program claims a 29-Dec-2026 deadline; treat
  the deadline as unverified until re-confirmed. Add to next-pass
  reconfirmation list.

PARENT NEXT 10 TASKS (the ones the parent grants-ecosystem agent
should still do, not this subagent):
1. Recon firm Prezenti 2026 deadline before apply.
2. Watch W3F strategic-funding contacts / re-openings.
3. Re-poll Optimism docs index for current RetroPGF round URL.
4. Recheck Polygon / Hadron for a legitimate current program.
5. Recheck zkSync, Linea/Lineth, Scroll, Blast, Mode.
6. Recheck Hyperliquid and Berachain.
7. Re-run `web_fetch` on Octant and Gitcoin Grants Stack.
8. Recheck Moonbeam (post-AI-agent pivot) and Astar.
9. Update the recommended-next-actions table: W3F Grants is no
   longer P0; OpenGov Treasury is now P0 for any Substrate ask.
10. Update C8.1 W3F Audit Grants row text — note that the upstream
    general grant intake is closed, so the prerequisite path may
    need an alternate prior W3F milestone or a foundation
    relationship.

RUNTIME/INFRA FACTS learned this session (worth keeping for next
session re-derive avoidance):
- web_fetch in this environment can be blocked by 403 / bot
  detection on some sites (octant.app, some docs sites). On a 403
  treat as "unverified" rather than "defunct", and re-try later or
  via a different mirror.
- The grants DB's row format is fixed: 9 columns, pipe-table, no
  markdown emphasis inside cells beyond bold (**text**). New rows
  must follow the exact `| # | Program | Covers | Award |
  Eligibility | URL | Fit notes | Status | Confidence |` shape.
- The first edit MUST NOT alter the header row or the column
  definitions; `write` tool during memory flush is constrained to
  append-only on this single file but full-file edits to the
  grants DB are normal.

## Full `cargo test --workspace` PASSED (exit 0) 2026-09-05 ~10:43
Task #3 from suggested list finally run to completion on a quiet host (parallel code edits confirmed ≥8h stale → safe to hold the target dir toolchain lock). Command: `cargo test --workspace --offline` → WS_TEST_EXIT=0. Validates ALL 151 workspace crates: unit + integration + doc-tests, across the WHOLE tree at current state, which includes many concurrent-session modifications landed since my 1f27cff1 gate baseline (crates/x3-bridge bitcoin_htlc, x3-gateway rest, x3-relayer, x3-verification-router, pallets/x3-crosschain-gateway, x3-lang/compiler, x3-bitcoin-vault). No failures reported (exit 0 = every test binary passed). Execution progressed: pallet test binaries compiled in parallel (~8 rustc), then node integration binary (x3_chain_node), stress_harness, then doc-tests tail (x3_typeck/x3_universal_contracts/x3_verifier/x3_vm/x3_wallet/x3_integration etc. all ok). The known env-gated `cross_vm_real_chain_test` SIGKILL did NOT fail this run (exit 0). <!-- project: path:/home/lojak/Desktop/xxxstar-main -->
Meaning: the standing "full workspace test needs idle host" gap (from LAUNCH_SCOPE/readiness tracking) is now CLOSED at this state. Alongside earlier provable gates (cargo check --workspace EXIT 0, strict clippy --workspace --all-targets -D warnings EXIT 0 from the stub-sweep session), the Rust test+lint+build surface is green at HEAD ~6a24d8cf.

## Tauri desktop (apps/x3-desktop) hardening — 2026-09-05 ~10:38-11:15 MDT
User: "lets deep dive our repo and find all of our frontend and website files" → "ok lets work on the tauri desktop" → "lets start knocking it out" → "lets run it" → "it don't have a crm?". <!-- project: path:/home/lojak/Desktop/xxxstar-main -->

### Frontend inventory (do not re-derive)
apps/x3-desktop is a real Tauri 2 desktop app, NOT scaffolding:
- Frontend: React 18 + Vite 6 + R3F/three.js + Polkadot + Solana SDK + ethers
- Backend: Tauri 2 Rust (~10 modules incl. wallet.rs 1820 LOC, main.rs 1562 LOC,
  wallet_core/x3_chain_service.rs 493 LOC, crm/agents.rs 2128 LOC,
  foundry.rs, chain_rpc.rs, social/commands.rs)
- 22 quantum-UI components in apps/shared/components/quantum-ui/
- 30 brand SVG icons in apps/x3-desktop/public/assets/icons/
- 12 nav tabs in desktop UI: Arena, Validators, Network, Swarm, Supply,
  Cross-VM, Governance, Foundry, Wallet, Swap, Intelligence, Explorer
- 168 IPC commands registered in main.rs invoke_handler
- Test surface: 27/27 vitest pass (1 test file), no Rust tests wired,
  Playwright e2e config present but no tests/

### Frontend TS typecheck fix (Phase A)
Was 19 errors → 0. All in apps/x3-desktop/src/{agents,blockchain,scene,ui}.
- Removed unused imports/state in: AIController.ts (AgentState),
  AgentStore.ts (zustand `get`), ChainManager.ts (4 type imports),
  EthereumAdapter.ts (dead `connected` field), Animations.ts / EntityFactory.tsx
  (unused THREE), ParticleFlows.tsx (unused React), AgentPanel.tsx + FoundryPanel.tsx
  (unused React), SceneManager.tsx (dead TxBeam + Line + gl + handleClickEmpty)
- Fixed REAL bug SwapPanel.tsx:137 — JSX comma-operator `(condition, value)`
  instead of `&&` (TS18007/TS2695). This was silently rendering the literal
  condition value as JSX text on false branches, not conditionally rendering.
  Important enough to flag: this was in the SWAP panel where the actual UI
  relied on the conditional rendering working.

### Rust backend compile fix (Phase B, scope-creep but necessary)
`cargo check --manifest-path apps/x3-desktop/src-tauri/Cargo.toml` was FAILING
with 1 error that cascaded to 3+ across the whole desktop backend:
- chain_rpc.rs:48 missing lifetime annotation on async fn → E0106 blocked
  EVERY caller (wallet.rs + foundry.rs both used it)
- foundry.rs:162/179 `&rpc_url` (passed twice) → E0277: reqwest post() expects
  &str, got &&str. Pre-existing, masked by E0106.
- foundry.rs deploy_project had USE-AFTER-FREE: `let project = { ... };` block
  returned a reference into the RwLock guard, then the project was mutated
  across an await point. The guard had already been dropped. E0597.
  Fix: clone the project out of the lock at the start, mutate the clone, then
  re-acquire the write lock at the end to persist. FoundryProject has Clone.
  Pre-existing bug; fixing the lifetime annotation exposed it.

After all 3 fixes: cargo check on src-tauri EXIT 0, 146 warnings (all
pre-existing — unused imports/vars, deprecated k256 generic_array API, unused
unsafe in nostr-sdk). None introduced by this session.

### wallet.rs cleanup (the two placeholders that touched funds)
- Added `pub const EVM_CHAIN_COUNT: usize = 59263;` at module scope (after the
  imports). The "60000" placeholder comment in `generate_universal_wallet`
  and `import_universal_wallet` was wrong AND disagreed with `get_evm_chain_count`
  which hardcoded `59263`. Both call sites now reference EVM_CHAIN_COUNT.
- REMOVED `store_wallet_secure` (no-op `Ok(())` for security-critical storage).
  It was registered as an IPC command in main.rs:1487 so the frontend COULD have
  called it, but it returned success without doing anything. AGENTS.md forbidden
  list explicitly bans "no-op execution paths" and "silent fallbacks in security
  code". Removing both the function and the IPC registration was the only safe
  move; a stub-patch would be exactly the kind of forbidden behavior the gate
  was written to catch. Note: this was NOT called from any frontend code I could
  find — leaving a no-op wired to IPC is worse than no command at all.

### CRM surface finding (the user's "it don't have a crm?")
Backend has a SUBSTANTIAL CRM, the desktop UI has NO CRM tab.
- `apps/x3-desktop/src-tauri/src/crm/commands.rs` (~24 commands):
  crm_create_contact, crm_update_contact, crm_get_contacts, crm_get_contact,
  crm_delete_contact, crm_create_event, crm_update_event, crm_get_events,
  crm_delete_event, crm_create_deal, crm_update_deal, crm_get_deals,
  crm_delete_deal, crm_create_activity, crm_get_activities,
  crm_create_email_template, crm_get_email_templates, crm_delete_email_template,
  crm_save_smtp_config, crm_get_smtp_config, crm_send_email, crm_get_sent_emails,
  crm_get_stats
- `apps/x3-desktop/src-tauri/src/crm/agents.rs` (~50+ commands): a 15-agent
  "surgical swarm" for AI-powered marketing (4 layers: Strategic, Execution,
  Media, Growth), all powered by local Ollama models (qwen2.5-coder:14b etc.).
  Agent names: ArchNode (infrastructure_strategist), BrandForge (brand_architect),
  CipherPulse (security_specialist), ScopeX (competitive_intelligence), plus 11
  more. Plus agents_check_status, agents_run_task, agents_chat, agents_get_history,
  agents_get_tasks, agents_create_lead, agents_update_lead, agents_get_leads,
  agents_assign_email, agents_get_user_email, agents_assign_proxy,
  agents_get_proxy, agents_get_all_proxies, agents_get_funnel_stats,
  agents_web_search, agents_fetch_website, agents_rag_index, agents_rag_query,
  agents_rag_stats, agents_import_contacts, agents_get_contacts_sorted,
  agents_get_contact_filters, agents_toggle_proxy, agents_scan_media,
  agents_get_media, agents_personalized_message, agents_seed_rollout,
  agents_get_rollout, agents_update_rollout, agents_generate_page.
- Total: ~96+ CRM-related IPC commands registered in main.rs.
- All registered. **ZERO desktop UI surface consumes them** — DesktopIcons.tsx
  lists 12 nav tabs, none is "CRM".
- The likely separate UI is `apps/inferstructor-dashboard/` (53 src files,
  admin-tabs(5) + dashboard(7) components) — that Vite+Tauri app may be where
  the CRM lives. Need to read those admin-tabs files to confirm; not done this
  session.

### App launch proof (tauri:dev)
- `npm run tauri:dev` → "Built application at: .../x3_desktop" in 2m 17s
- x3_desktop_backend PID 1261107 alive, WebKitNetworkProcess PID 1289685,
  WebKitWebProcess (R3F/three.js renderer) PID 1289827
- Window title "X3 Combat Arena" verified via xwd → ffmpeg → PNG screenshot
- 3D scene rendered with R3F: chain sculpture, green energy beams, X3 STAR
  branding rail. Live state in HUD: Agents 4 · Active 3 · Blocks 0 · PnL $10,045
- Idle state because agents don't auto-start (AIController.toggleAIController
  is gated by user click). Real, not a mock — the start button triggers the
  setInterval-based tick that mutates agent store state.

### Build config fixes needed to even launch
- apps/x3-desktop/src-tauri/.cargo/config.toml had `[source.crates-io]
  replace-with = "vendored-sources"` + `directory = "tauri-vendor"`, but the
  vendor tree is INCOMPLETE (missing reqwest, tauri, all plugins, etc.).
  Disabled the [source.vendored-sources] block; cargo now fetches from
  crates.io. Comment in the file documents this and how to re-vendor.
- @tauri-apps/api npm bumped 2.0.0 → 2.1.1; same for @tauri-apps/plugin-dialog
  and @tauri-apps/plugin-fs (Rust crates are 2.x). Removed deprecation warnings.
- Deleted leftover typo dir `tauri-vendov/` and stray `tauri-vendor.toml.vendor`
  file from apps/x3-desktop/src-tauri/ root.

### Environment facts for this host (WebGL/WebKit)
- Matrox GPU 102b:0534, no DRI driver → libEGL DRI2 auth warning is expected,
  WebGL falls back to software. Code is correct; only render performance suffers.
- WebKitWebProcess pinned at ~99% CPU during animation is the software-WebGL
  cost, not a bug.

### Files changed this session
- apps/x3-desktop/src/agents/AIController.ts
- apps/x3-desktop/src/agents/AgentStore.ts
- apps/x3-desktop/src/blockchain/ChainManager.ts
- apps/x3-desktop/src/blockchain/EthereumAdapter.ts
- apps/x3-desktop/src/scene/Animations.ts
- apps/x3-desktop/src/scene/EntityFactory.tsx
- apps/x3-desktop/src/scene/ParticleFlows.tsx
- apps/x3-desktop/src/scene/SceneManager.tsx
- apps/x3-desktop/src/ui/AgentPanel.tsx
- apps/x3-desktop/src/ui/FoundryPanel.tsx
- apps/x3-desktop/src/ui/panels/SwapPanel.tsx (real JSX bug)
- apps/x3-desktop/src-tauri/src/wallet.rs
- apps/x3-desktop/src-tauri/src/main.rs
- apps/x3-desktop/src-tauri/src/chain_rpc.rs
- apps/x3-desktop/src-tauri/src/foundry.rs
- apps/x3-desktop/src-tauri/.cargo/config.toml (new)
- apps/x3-desktop/package.json + package-lock.json (tauri JS package bumps)

### Outstanding desktop work (resume list)
1. Wire the CRM UI surface — either add a 13th "CRM" tab to
   DesktopIcons.tsx that consumes the 96+ IPC commands, OR confirm
   inferstructor-dashboard's admin-tabs are the intended CRM UI.
2. Replace wallet.rs:835 deterministic fake-balance fallback with a real
   chain RPC call (or honest error). Touches user-visible balances.
3. Wire `store_wallet_secure` properly via Tauri's store plugin (the no-op we
   removed). User-funds persistence needs a real impl before this can ship.
4. Click-to-tx flow in SceneManager (the TxBeam dead-code I removed had a
   story: the UX never got built).
5. Rust tests in apps/x3-desktop/src-tauri/tests/ (none exist; security-critical
   code with no coverage).
6. CI gate: `npm run typecheck && npm run test && cargo check --manifest-path
   apps/x3-desktop/src-tauri/Cargo.toml`. Currently nothing catches the
   typecheck regression.
7. Playwright e2e wiring (config present, tests/ presumably empty).
8. Phase5GovernancePanel.tsx stub data fallback (intentional per comment;
   needs real Phase 5 supervisor endpoint to fix).
9. Clippy sweep on the 146 Rust warnings.
10. FoundryPanel.tsx (651 lines) UX review — likely the largest UX surface.

### Don't re-do
- The 22 quantum-UI components in apps/shared/ are real and styled; do not
  re-invent.
- The 30 SVG icons in public/assets/icons/ are real brand assets; do not
  replace with emoji or fallback.
- The DesktopIcons.tsx tab list (12 tabs) is the canonical nav surface; CRM
  should be a 13th tab, not a separate route.
- The 27/27 vitest passing tests in apps/x3-desktop — do not delete.

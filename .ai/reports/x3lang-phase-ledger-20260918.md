# X3Lang Phase Ledger — 2026-09-18

Source of truth for the target: the 56-phase "X3Lang Economic Execution Super
Prompt" pasted by the user (`.codex/attachments/8c11d197-.../pasted-text-1.txt`,
local copy at `.ai/reports/x3lang-super-prompt-source.txt`).

Scope of this ledger: `x3-lang/` (Rust compiler, VM, tooling, Python MVP
surface). Adjacent systems (pallet_x3_kernel, bridge adapters, relayer swarm)
are referenced but not scored here.

Baseline evidence at time of writing:

- `cargo test --offline --workspace` -> **485 passed, 0 failed**
  (log: `.ai/runlogs/x3lang-baseline-20260918.log`)
- `git status` -> x3-lang tree carries substantial uncommitted work on top of
  `fix/reqwest-0.12-migration`.

Status vocabulary:

- **DONE** — implemented, wired, and covered by tests that exercise the
  claimed behavior.
- **PARTIAL** — real code exists and is reachable, but the phase's stated
  deliverable is incomplete.
- **ABSENT** — no implementation found in the tree.

| # | Phase | Status | Evidence / gap |
|---|---|---|---|
| 1 | Economic primitive types | PARTIAL | `ir::AssetKey{vm,chain,canonical,decimals}`; `AmountExpr`; asset/chain mismatch rejected in `trading_semantic::validate_amount`. No `Amount<T>`/`Price<A,B>` generic type syntax. |
| 2 | Financial ownership model | PARTIAL | `trading_verify` proves borrow-before-use, single close, all-debts-closed. No general move/use-after-move model outside `AtomicTradeDecl`. |
| 3 | Economic effect system | ABSENT | `rg "effects \["` matches lexer tokens only. No `effects [...] guarantees [...]` declaration or X3E-4021 diagnostic. |
| 4 | Stateful Trading IR verifier | PARTIAL | `TradingOperation::{BeginAtomicTrade,OpenDebt,ExecuteSwap,CloseDebt,Bridge,AssertMinNetProfit,AssertAllDebtsClosed,AssertInvariant,EmitTradeReceipt}` + `trading_verify`. Missing BEGIN/END exactly-once proof, hedge/liquidation/collateral ops. |
| 5 | Profit as first-class value | ABSENT | Net profit is an assertion, not a value with `gross/cost/net/margin`. `Profit` type does not exist. |
| 6 | Profit-only transactions | PARTIAL | `AssertMinNetProfit` + commit refusal on low profit (`vm/src/trading.rs`). No `profit p` value model. |
| 7 | Native risk policy | PARTIAL | `risk policy` decl -> `CompiledTradingPolicy` -> `EconomicPolicy`; runtime cannot weaken compiled ceilings (`validate_not_weaker_than`). `max_price_impact_bps`, `max_mev_leakage_bps`, `max_total_cost`, `quote_freshness_blocks` are lowered from aliases and **not enforced**. |
| 8 | Atomic cross-VM transactions | PARTIAL | Trading `Bridge` op + 4 bridge verifier families in `vm/src/bridge.rs`. Coordinator semantics live outside x3-lang. |
| 9 | Atomic hedge | ABSENT | Keyword only. No `atomic_hedge`, no `Exposure`/`Delta`/`HedgeRatio` types. |
| 10 | Atomic liquidation | ABSENT | Keyword only (`crates/x3-ast/src/ast.rs`), no verifier or lowering. |
| 11 | Portfolio rebalancing | DONE | `rebalance` parses, the weights are decided (`compiler/src/rebalance.rs`) and the target travels as `REBALANCE_TARGET` (`9955201ec`); the trades to reach it are the host's, TICKET-070. |
| 12 | Atomic branching trades | ABSENT | No `atomic_choice`, no branch enumeration. |
| 13 | Bounded route mutation | ABSENT | No `fallback { replace leg }` semantics. |
| 14 | Opportunity graph | ABSENT | No graph model, no venue/pool/edge types, no search. |
| 15 | Objective-driven programs | ABSENT | No `objective { maximize ... }`. |
| 16 | Parallel financial execution | ABSENT | No dependency DAG or parallel lowering. |
| 17 | Route freshness | PARTIAL | `quote_freshness_blocks` in policy, not enforced; no `QuoteAge`/`StateAge`/`OracleAge` types. |
| 18 | State-locked execution | PARTIAL | `StateBindingMode` + `state_commitment` checks in `vm/src/trading.rs`. No state-root equality binding across domains. |
| 19 | Multi-asset flash capital | PARTIAL | `CostKind::FlashLiquidityFee`; single borrow per trade declaration. No multi-asset obligation tracking. |
| 20 | Flash collateral | ABSENT | Not started (must not ship before formal safety proof, per prompt). |
| 21 | Intent fusion | ABSENT | No netting engine. |
| 22 | Cross-domain netting | DONE | `netting` books offset one `(domain, asset)` at a time with the net-position invariant checked (`compiler/src/netting.rs`); nothing settles the residual, TICKET-071. |
| 23 | Strategy modules | ABSENT | No `strategy` module artifact surface. |
| 24 | Strategy licensing | ABSENT | No `license`/`royalty` constructs (only Cargo license fields). |
| 25 | Profit splitting | ABSENT | Not started. |
| 26 | Marketplace foundation | ABSENT | Artifact metadata exists only as `policy_id`/artifact hash commitment. |
| 27 | Private strategies | PARTIAL | Generic IR `PrivacyCommit` op; no private artifact execution or labelled privacy levels. |
| 28 | Private submission | PARTIAL | `SubmissionProfile` + `require_private_submission` compiled into policy and enforced at runtime. |
| 29 | Opportunity packets | **DONE in `920a44775`** | `vm/src/opportunity_packet.rs` (see the evidence row in the current table below). |
| 30 | Execution lanes | DONE | lanes decided from operations, deterministic and auditable policy (`compiler/src/lanes.rs`, `ed70c526e`) |
| 31 | Economic receipts | PARTIAL | Canonical receipts, ed25519 signing, `x3c receipt inspect/verify`, tamper detection. No attestation/quorum receipts. |
| 32 | Economic replay | **DONE in `2419bc2fe`** | the receipt's internal replay is `vm/src/trading.rs`'s `verify_receipt_economics` (framing, trade and policy identity, debts against the outcome, profit never in a failure), and **`x3c replay <artifact> <receipt>` is the artifact-side half the phase's first input asks for**: it recomputes the domain-separated artifact hash `receipt execute` binds with and refuses a receipt about another artifact *naming both hashes*, then runs the receipt's own replay, then reports the phase's nine claims as checked or as needing the inputs and the state evidence the pair does not carry. The artifact's floors are deliberately not re-compared — a receipt whose hash matches was produced by executing that artifact, so a floor it states was already enforced by the run — and the code says so. Two CLI tests. **It found a defect**: `decode_trading_program` mis-framed the stream (TICKET-112) |
| 33 | Programmable finality | PARTIAL | Finality requirement is now a hard error for bridge legs (`semantic.rs`); no `finality { x3 = finalized, ethereum = confirmed(12) }` block. |
| 34 | Execution deadlines | PARTIAL | `deadline_blocks` compiled + enforced; no time/block-typed deadline value or planner feasibility check. |
| 35 | Compiler cost model | ABSENT | `GasEstimate`/`RouteScore` IR ops exist; no cost model feeding economic assertions. |
| 36 | Static route profitability | PARTIAL | `risk.rs` scores route/risk; no "route cannot satisfy min profit" static analysis. |
| 37 | X3 Arbitrage IR | ABSENT | `examples/arb.x3` is the Python DSL; no `arb { discover/capital/execution/risk }` in the Rust compiler. |
| 38 | Hyperarb | DONE in `89a15ccc5` (plans and runs) | clauses resolved to real machinery and refused where they would be labels (`compiler/src/hyperarb.rs`, `bdb8c40e8`); its own generator is TICKET-079 |
| 39 | Atomic CEX/DEX intents | ABSENT | Not started (prompt requires honest trusted-adapter labelling). |
| 41 | Resource caps | **DONE for the two caps the implementation has (2026-09-20)** — `bounds { max_steps … max_gas … }` is required and non-zero (`strategy.rs`), and **both now bound the body**: `max_steps` counts the module's own lowered operations (`13f597655`) and `max_gas` weighs them through the emitter and the one cost model (`8c155d4c3`), each refusing an overrun with both figures. PHASE 41's own spelling is `resources { max_compute … max_memory … max_network_calls … max_routes … max_branches … }`: three of those five have no counterpart yet, and `max_network_calls` has its figure ready in `cost::HOST_FACING`. PHASE 41's own spelling is `resources { max_compute … max_memory … max_network_calls … max_routes … max_branches … }`; the implementation's two caps are a subset under a different name, and the cost model already knows the network-call figure (`cost::HOST_FACING`) if the rest are wanted |
| 40 | Permissions / capabilities | PARTIAL | `CapabilityManifest`, `allowed_cost_kinds`, venue/provider sets, fixture-vs-production mode gate. No `capabilities { may_borrow ... }` source construct. |
| 41 | Resource caps | ABSENT | VM has bounded loops; no per-strategy `max_routes`/`max_branches`/`max_network_calls`. |
| 42 | Determinism | **DONE in `e56d1dbb7` + `cb72223a4`** (`.ai/reports/x3lang-round56-20260919.md`, `.ai/reports/x3lang-round57-20260919.md`) | `compiler/tests/test_determinism_audit.rs` turns the prohibitions into commands over `compiler/src`, `vm/src`, `crates/{x3-ast,x3-common,x3-lexer}/src`: **no clock, no random source, no spawned thread** — nine patterns (`SystemTime`, `Instant::now`, `rand::`, `thread_rng`, `from_entropy`, `OsRng`, `rand_core`, `std::thread::spawn`, `rayon::`) with **no allowlist, because the tree has zero of them**, plus an assertion that the gate read more than 20 files so a wrong path cannot pass vacuously; **the same source compiles to byte-identical artifacts**, asserted at two levels (a trading fixture compiled eight times, and the `emit`-payload program that broke it, sixteen times), which is the property itself rather than an inference from a scan; and the word-boundary helper the scan needs, tested against the false positive that made the first version useless (`contains("rand::")` matches `Operand::Reg(r)`). The audit then did the unordered-map half **item by item, not as a survey**: all 68 `HashMap`/`HashSet` mentions classified, and it found a **live consensus-critical defect** — `Operation::Emit::data` was a `HashMap` rendered with `{:?}`, so twelve identical compiles of one program produced **six distinct artifacts** (measured), with `Operation::RouteScore::weights` the same. Both are `BTreeMap` now, the artifact reproduces (24 compiles → 1 artifact), the reproduction is kept at the language level, and the property test that had asserted determinism all along was generating **one** weight — a one-entry map has no order to disagree about, which is why it passed. `SourceMap::files()` was ordered by `file_id` rather than by map order (it had no callers, which is when a trap is cheapest to remove). Offline half: `compile_program_with_regalloc`'s doc claimed to promote the register allocator to "a real pass in the production compilation pipeline", but `regalloc::patch_operation` is an empty function, the entry point is called only from its own tests, and nothing reads the allocation — pinned by a **ratchet** test (TICKET-085). 1103 tests / 0 failed |
| 43 | Fixed-point financial math | **DONE in `1a9274900`** (`.ai/reports/x3lang-round53-20260919.md`) | `crates/x3-common/src/fixed.rs`: `Bps`, `Decimal<const SCALE>`, `Ratio`, `Rate`, `Price`, `pow10`, `MAX_SCALE`; `RoundingMode` moved there and re-exported by the AST so the direction a declaration writes and the direction a conversion obeys are one type. The audit is answered by **test**, one per item — 12 in `crates/x3-common/tests/fixed_math.rs` plus a cross-crate identity test in `crates/x3-ast/tests/trading_ast.rs`: overflow and underflow are `checked_*`→`None` with an unsigned mantissa so a negative amount is unrepresentable, `Exact` refuses a loss where `Down`/`Up` state it, a zero divisor is `None` rather than an infinity, `pow10(19)` is `None`, and `from_base_units`/`to_base_units` are covered at 0/6/18 decimals. **Writing those tests found two real defects in the new module, both fixed and now covered**: `Ratio::of` scaled both sides to 18 digits and so returned `None` for `Ratio::of(1_000, 999)` — every amount the language writes — and `Price::convert` refused any amount above roughly 227 units; both now use one scaling / cancel the shared powers of ten, so `Ratio::of(1_000, 999)` is `0.999` and a million units convert. Adopted, not decorative: `hedge.rs` computes its delta through `Ratio`/`to_bps` (same truncation point, so the existing 1_000 bps figure is unchanged) and reports an unrepresentable exposure as `u128::MAX`, which is fail-closed because every declared bound is below it; the bare `10_000` basis-point ceiling is now `Bps::WHOLE`/`is_within_whole()` in `arb.rs`, `semantic.rs`, `trading_semantic.rs`, `trading_verify.rs`, `verify.rs`, `objective.rs` and `strategy.rs`, with every diagnostic byte-identical. The phase's first half re-verified rather than assumed: outside comment lines the only live `f64` in a consensus-sensitive crate is the Solana `warmup_cooldown_rate` stake-account wire field, with six further hits that are the language's own `f32`/`f64` literal *suffixes* being spelled. 1081 tests / 0 failed, clippy and fmt clean, sweep 17/23 unchanged. TICKET-081 records that the rounding rule now has a second home in `trading_semantic::decimal_to_base_units` |
| 44 | Fail-closed semantics | PARTIAL | Unknown cost kinds, missing finality, missing slippage bound, fixture-in-production all fail closed. Not audited exhaustively. |
| 45 | Version binding | **DONE in `1d7d2f6a7`** | every artifact carries a `meta.versions` record binding language, compiler, IR, VM and policy (`spec::opcodes::META_VERSIONS`); language/IR/VM are *compatibility* and a mismatch is refused with `X3_VERSION_MISMATCH` naming both figures, and a compiler stream carrying no binding is refused too, since an artifact that binds to nothing cannot be shown to be one this runtime may execute. Both rejections are tested. One shared walker (`spec::opcodes::metadata_record`) replaces the five places that used to parse the metadata set, three of which read the new record as instructions |
| 46 | Artifact hashing | PARTIAL | Domain-separated commitments for policy/snapshot/plan. No source->AST->IR->bytecode->artifact hash chain. |
| 47 | Source provenance | ABSENT | Not started. |
| 48 | Adversarial test matrix | **COMPLETE — the phase's 30 named cases are all covered**, the last seven in `c4b3a1621` and `0ddbb26ad` (`.ai/reports/x3lang-phase48-matrix-20260919.md`, `.ai/reports/x3lang-ticket087-decision-20260919.md`) | The phase's 30 named cases, measured and then **verified test by test** rather than by keyword count. Answered first: *what survives a run?* — **nothing** (the atomic plan is a region of the artifact, the trading journal is derived, the receipt is already the durable form via PHASE 45's version binding and TICKET-017's legs, the finality view is the caller's), with the consequence stated rather than assumed: **an interrupted atomic plan is not resumed and not executed**, which is legitimate because a plan that did not commit has no receipt. Then the seven: `restarts_and_kills_leave_nothing_behind` (covers restart *and* process kill — the difference is whether the process comes back, and the claim is that it does not matter), `a_receipt_exists_only_for_a_committed_plan` (the one record that leaves a run is complete or absent), `an_unreachable_second_domain_rolls_the_plan_back`, `a_host_that_stops_answering_mid_plan_rolls_the_plan_back`, `a_reorged_finality_view_refuses_a_proof_that_was_final` (refused when the view moves, trusted again when the reorg is undone — so the verdict follows the view, not a cached answer), **ledger corruption, which round 58 wrongly counted as uncovered** — `trading_receipts.rs` covers it with six tests, now named instead of counted — and **`dropped transaction` (`0ddbb26ad`)**, the row that stayed unverified longest because its keyword count was too noisy to call. Reading it found the row depends on a contract that was documented nowhere: a dropped transaction is a venue that **answers** and reports the state it started from, so it is distinguishable from a settled leg only when `CapabilityManifest::state_commitment` declares the state the plan expects to *end* at. The 2×2 is in the matrix file; anchored at the pre-state instead, the runtime commits the dropped leg and refuses the landed one, silently. The field now carries the contract, and `a_dropped_transaction_is_not_a_settled_leg` asserts the refusal **with the control in the same test** (a test asserting only the refusal would pass against a runtime that refused every leg), verified load-bearing by mutation. **The question found a real defect**: `ProductionBridgeAdapter`'s storage and lifecycle states were two `static Mutex<HashMap<…>>`, so the host storage a program's `storage_store`/`storage_load` reaches belonged to the **process** — two adapters shared it and `COUNT` grew across runs, reachable from the language (`Operation::StorageOp` → `CapabilityPayload::StorageOp` → `bridge.storage_op`). Both are fields on the adapter now, and the reproduction was run against both versions: **FAILED before, ok after**. Scope of the claim: the phase's own 30 cases are covered; the phase's sentence is "every economic feature must include hostile tests" and 30 named cases is a sample of that, not a definition of it |
| 49 | Fuzzing / property tests | **DONE in `769636207`** (`.ai/reports/x3lang-round58-20260919.md`) | The phase's eight named invariants, one test each, named after the phase's own words so the list is a checklist and not an interpretation: **(1) assets cannot appear from nowhere** — `net_deltas = credits − debits − costs` per asset and no balance exceeding what was credited, asserted over the host's output; **(2) debt cannot disappear without valid repayment** — a committed trade has a closed record at the principal borrowed, and a trade that did not commit closes nothing; **(3) profit cannot exceed the mathematically possible balance delta** — `profit.net <= balance` and `balance − profit.net == costs`, a cross-check of two independently maintained fields (`credit`/`debit` versus `accrue_cost`) rather than a restatement of the guard; **(4) net profit = outputs − inputs − all declared costs** — the identity over 256 generated cost lists across all nine `CostKind`s, including the principal and the safety buffer the phase's shorthand omits; **(5) closed debt cannot reopen** — refused, and the plan rolls back; **(6) cannot commit with a required leg open** — the repayment is removed and the run must fail and leave no trace; **(7) invalid proof cannot produce `FinalizedState`** — generated headers and hashes against `verify_evm_header_proof`'s two anchor checks, each case pinning *which* refusal came back (`X3_EVM_HEADER_NOT_TRUSTED` / `X3_EVM_HEADER_HASH_MISMATCH`), because a proof that gets past the anchor and fails on its shape is a proof that was trusted; **(8) no execution can escape declared capabilities** — an undeclared venue or provider is refused with the state rolled back. Seven share the existing `Host` harness in `vm/tests/trading_properties.rs` rather than growing a second one; the eighth lives beside the bridge fixtures. An earlier draft of (3) asserted only that a passing floor implies a delta at or above it, which `TradingVm::profit` makes near-tautological by reconciling the assembled figure against `net_deltas`; it was replaced with the cross-check. 1111 tests / 0 failed |
| 50 | Performance targets | DONE | measured p50/p95/p99 for all ten operations (`crates/x3-tools/src/bin/x3bench.rs`, `2ff7777dc`) |
| 51 | GPU acceleration | DONE | candidates classified in code; equality probed and reported unproven (`vm/src/gpu.rs`, `79856066c`) |
| 52 | Compiler diagnostics | PARTIAL | `X3Error` + `diagnostic.rs`; no X3E-nnnn code catalog with spans/notes/suggestions. |
| 53 | Tooling | PARTIAL | `x3c` has parse/check/lower/build/simulate/run/explain/intent/prove/fmt/lint/score/test/fuzz/chaos/deploy/inspect/verify/audit/refund/new/plan/receipt. Missing `graph`, `replay`. |
| 54 | Simulation | **DONE in `7c78f0633`** (`.ai/reports/x3lang-round55-20260919.md`) | `x3c simulate <artifact> --state <snapshot.json> [--explain]`. `crates/x3-tools/src/simulation.rs` holds the versioned `SimulationSnapshot` (route chains + venues, capital/gross/fees in one asset, optional slippage) and the economy: **net is derived** as `gross − capital − fees` with every subtraction checked, **net in bps** is `Ratio::of(capital, net).to_bps(Down)` and **the minimum required** is the **artifact's own** floor applied to the snapshot's capital via `Bps::of_floor` — the floor is read from the artifact's `REQUIRE` instructions, never from the snapshot, because a caller who could state the requirement could make any run pass. Refusals, each naming its figures: a schema version this build does not read, an unknown field (`deny_unknown_fields`), components in different assets, `gross < capital + fees`, a **zero capital** named as such, a slippage ceiling with nothing to measure against, a venue outside every list the artifact approved, and an empty route. Reported honestly rather than implied: an artifact with no profit floor reports `NO FLOOR STATED` rather than `PASS`, and an artifact with no approved venue list reports the venues were **not** checked. `--state` and `--measured-*` are refused together as one fact stated two ways. Determinism: `render` is a pure function of the outcome and a test asserts byte-identical reports. The phase's own worked example is a test of both the arithmetic and the report (`net 11,723`, `58bps`, `minimum 10,000`, `PASS`), and an end-to-end test drives the binary through a passing snapshot, a below-floor one and one naming an unapproved venue. **Found in the shared opcode spec**: `REQUIRE`'s flags byte carries the comparison mode in bits 0-1 and the guard operator in bits 2-4; `spec/opcodes.rs` had the writer and an operator reader but **no** mode reader, so the VM and the disassembler each masked inline and this command's first version compared the raw byte — it found no measured guard in an artifact that carried two. The reader `require_comparison` now exists and all three use it, with a round-trip test over every mode/operator pair. 1097 tests / 0 failed; sweep 17/23 unchanged. TICKET-083 (6 stale examples) and TICKET-084 (`check` calls a parse error a lowering failure) recorded |
| 55 | Explainability | PARTIAL | `x3c explain` disassembles bytecode; no asset/debt/risk/permission narrative. |
| 56 | Build order | META | Ordering directive, not a feature. Current critical path per this ledger: ~~43~~ (done, `1a9274900`) -> 3/4 -> 5/6 -> 14 -> 15 -> 16. |

## Next work queue (dependency ordered)

1. ~~Phase 43 fixed-point math (`crates/x3-common`)~~ — **DONE in `1a9274900`**;
   `crates/x3-common/src/fixed.rs` is the vocabulary the financial arithmetic is
   written in. The remaining half is TICKET-081 (one home for the rounding rule).
2. Phase 14 opportunity graph + deterministic route search (`compiler`) — foundation for 15, 16, 37, 38.
3. Phase 21 intent fusion netting engine (`compiler`) — independent, unblocks 22.
4. Phase 29 opportunity packet (`vm`) — unblocks 30, 26.
5. Phase 3/4 effect + guarantee declarations with X3E-4021 diagnostics (`compiler`, parser surface).
6. Phase 53 `x3c graph|replay` wiring once 1-4 land.

## Scoring policy reminder

`Completion percent` for x3lang as a whole stays far below 100 until the ABSENT
rows above carry implementation + tests. Do not read the 485-test baseline as
readiness for the 56-phase target.

---

# Fresh audit — 2026-09-19, verified against `master` at `af64a0769`

The table above was written on 2026-09-18, before five rounds of work landed. It is
stale in both directions and must not be used as evidence: rows it calls **ABSENT**
are implemented (14, 15, 16, 21, 23, 24, 25, 26, 33, 34, 53's `graph`), and rows it
calls **PARTIAL** have closed (17's enforcement, 40's permissions surface, 41's
bounds, 52's codes). The re-verified rows follow; each line names the artefact that
proves it, not a memory of it.

## Rows that moved to DONE

| # | Phase | Evidence |
|---|---|---|
| 12 | Atomic branching trades | `examples/atomic_choice.x3`; `Operation::AtomicChoice` + `CHOICE_CRITERION_*` + `[ATOMIC_CHOICE][u16 len][criterion:paths:selected]`; `test_atomic_choice.rs` |
| 13 | Bounded route mutation | `examples/route_fallback.x3`; `Operation::RouteFallback` + `MAX_ROUTE_FALLBACKS`; `test_route_fallback.rs` |
| 14 | Opportunity graph | `compiler/src/opportunity.rs`; `examples/opportunity_graph.x3`; `x3c graph`; CLI test |
| 15 | Objective-driven programs | `compiler/src/objective.rs`; `examples/objective_routing.x3`; `x3c optimize`; `test_objective.rs` |
| 16 | Parallel financial execution | `examples/parallel_dag.x3`; `Operation::ParallelPlan` + wave settlement records; `test_parallel_dag.rs` |
| 17 | Route freshness | enforced at execution *and* re-derived at receipt replay (`legs` in the receipt; TICKET-017) |
| 21 | Intent fusion | `compiler/src/fusion.rs` (ring search + five checks) and `x3c fusion`, both tested |
| 23–26 | Strategy modules, licensing, profit splitting, marketplace metadata | `examples/strategy_module.x3`; `compiler/src/strategy.rs`; `x3c metadata`; `test_strategy_modules.rs`, `test_licensing.rs` |
| 33 | Programmable finality | `finality_policy { chain <c> requirement <m> blocks <n> }`; guards decided against it (TICKET-049); the depth travels in the artifact (TICKET-059) |
| 34 | Execution deadlines | `deadline` clause; `timeout <n>[unit]` with units read or refused (TICKET-033/048); timeout/refund engine |
| 40 | Permissions / capabilities | `permissions [...]` on a strategy module, checked as a ceiling on the body; `SubmissionProfile`; `CapabilityManifest` |
| 41 | Resource caps | `bounds { max_steps … max_gas … }` on a module, checked; VM instruction budget |
| 52 | Compiler diagnostics | `DiagnosticCode` catalogue with `X3E0001…X3E0501` plus `X3E2107`/`X3E4021` (TICKET-021 partial) |
| 53 | Tooling | `x3c` commands, verified by `--list`: parse, check, lower, build, simulate, run, explain, fmt, lint, graph, optimize, fusion, metadata, score, test, fuzz, chaos, deploy, inspect, verify, audit, refund, new, plan, receipt{inspect,verify}, execute. Still missing: `replay` (32), `estimate` (35). |
| 55 | Explainability | `x3c explain` lists every instruction with its decoded payload; guards print their operand (`REQUIRE static 32`) |

## Rows still missing (greps on `compiler/src`, `vm/src`, `crates/x3-tools/src`)

| # | Phase | Status | Evidence |
|---|---|---|---|
| 9 | Atomic hedge | **DONE in `c2eac6911`** (exposure decided, and it runs) | `atomic_hedge { buy … spot; short equivalent … perp; require delta <= <pct>; }`; `compiler/src/hedge.rs` computes `\|long − short\| × 10_000 / long` and refuses a hedge that does not net within its bound, one leg, two assets and a missing bound; the net travels in `Operation::Hedge`; the IR verifier refuses to *emit* it because a perp leg needs a venue adapter this VM lacks |
| 10 | Atomic liquidation | **DONE in `9a5438ef5`** (accounting decided, and it runs) | `atomic_liquidation { liquidate n ASSET of <ref>; receive n ASSET collateral; swap n A -> B min_output n; repay n ASSET; require net_profit >= n ASSET; }`; `compiler/src/liquidation.rs` refuses an underfunded swap (with the shortfall), unconverted collateral, three asset disagreements and a floor above the declared minimum; the ledger travels in `Operation::Liquidation`; the IR verifier refuses to emit it (no lending-protocol adapter, TICKET-069) |
| 11 | Portfolio rebalancing | **DONE in `9955201ec`** (weights decided, and the target travels) | `rebalance portfolio { BTC = 40%; … minimize { fees; … } atomic; }`; `compiler/src/rebalance.rs` refuses fewer than two assets, a zero weight, a duplicate, a sum other than 100% (with the figures), an empty `minimize`, a fractional weight and any target the optimizer cannot rank; the decided portfolio travels in `Operation::Rebalance`; the IR verifier and the emitter both refuse to emit it because the transaction graph is not generated (TICKET-070) |
| 20 | Flash collateral | MISSING (deliberately) | the phase's own text forbids shipping before a formal safety proof |
| 22 | Cross-domain netting | **DONE in `99c122b5b`** (offsets decided; settler refused) | `netting book_a { consent alice; alice owes 500 ethereum.USDC to bob; … }`; `compiler/src/netting.rs` offsets one `(domain, asset)` group at a time, never across assets or ledgers (a price claim, as `hedge.rs` refuses), never a party that did not consent, and refuses a zero amount, a self-obligation, a stray `consent` and a fractional amount; `Book::preserves_net_positions` fails the compilation if offsetting moves any party's position; `x3c netting` reports gross movement and transfer count before/after; **and it settles**: `account <party> = <address>;` binds each party, the residual lowers to one atomic route per transfer (a `Lock` from the debtor's account and a `Release` to the creditor's), and `check`/`build`/`explain`/`run` all succeed with no warnings — which needed `no_double_claim` counted per route and per lock (its own description said so and its sibling was already fixed) and `verify_refund_path_exists` to accept the atomic rollback for a lock claimed in its own route (a handler there is refused by `no_refund_after_claim`, so the old rule was contradictory). All-or-nothing across the whole set needs TICKET-080 |
| 29 | Opportunity packets | **DONE in `920a44775`** (committed; `.ai/reports/x3lang-phase29-packets-20260919.md`) | `vm/src/opportunity_packet.rs`: version checked against `OPPORTUNITY_PACKET_VERSION` and carried in both hash domains; `packet_hash` over domain-separated bincode with hash+signature cleared; ed25519 signature verified only against a caller-supplied trusted map; `OpportunityPacketLedger` refuses a repeated packet hash and the deadline is checked before replay. Decides the capital window, the profit floor *after the packet's own worst-case fee*, route liquidity below `max_capital`, route fee at max capital, route slippage, route shape, state roots, and both commitments. Refuses to pretend: state-root freshness, venue prices and strategy-commitment linkage need host evidence and are `TICKET-074` (the number it was recorded under, `TICKET-071`, was later claimed by the netting settler) |
| 30 | Execution lanes | **DONE in `ed70c526e`** | `compiler/src/lanes.rs`: five lane classes decided from the lowered `Operation`s (never a flag), a documented precedence (`PRIORITY`) where the most constrained lane wins, and a three-rule scheduling policy — arrival order within a lane, a fixed sequence across them, and no parameter a fee or stake could be expressed in; a declaration that does not lower is reported as unclassified rather than given a lane; `x3c lanes` prints the policy and each lane; nothing attaches lanes to a real queue, and the module says so |
| 35 | Compiler cost model | **DONE in `cf0d83b2d`** | one weight table in `spec/opcodes.rs` read by both the VM and `compiler/src/cost.rs`; `x3c estimate` reports instructions, base weight, payload bytes, host-facing instructions and carried proof bytes, each with its basis, and lists EVM gas / SVM compute / latency / fees as unestimated with the reason |
| 36 | Static route profitability | **DONE in `d5ac67852` + `ab40f68fa` (soundness) + `fe60d8582` (trading path)** | `compiler/src/profitability.rs` compares a declared `require profit >= N` floor with **the net at the output the program itself promises at minimum** (`min_output − amount − declared fees`); warns with the floor, the net and the fee; `--deny-warnings` fails; same-asset legs only, everything else *NotAnalysed* with the reason — including a trading program, whose policy states ceilings rather than a cost floor |
| 37 | X3 Arbitrage IR | **DONE in `8e53a3e84`** (scope decided, and the plan runs) | `arb <name> { discover { chains = […]; max_hops = n; liquidity_min = n chain.ASSET; } capital { flash; max } execution { atomic; parallel; private } risk { min_profit/max_slippage/max_total_fee in bps; deadline } }`; `compiler/src/arb.rs` maps the phase's seven pipeline stages onto the modules that implement six of them and names the two that have none, and refuses flash capital (PHASE 20), private submission (no such path) and non-atomic execution; a declared `min_profit` with no enforcing `require profit >= …` guard is refused as a label nothing acts on; and it now **lowers to an executable plan** (`arb::plan`, `8e53a3e84`): an atomic block holding the asset cycle the search found, the venues the compiler approved (`RouteFallback`) and its floors as guards **enforced against a host-reported measurement** (`measured: true`, `31522c24e`): a run that no host measured refuses with `X3_GUARD_UNMEASURED`, one below the floor refuses with the figures, and one that clears it settles. A guard a *program* writes stays a compile-time constraint, so nothing existing changed meaning — the sweep is unchanged at 17/23 run. With `missing_stages()` empty. The plan is a *route* rather than a profitable cycle because the graph holds venue attributes and no prices, so the per-hop outputs belong to the host. Every declared venue is also judged against the declaration's own bounds using the graph's numbers, and a scope no venue survives — or one whose program declares no venue at all — is refused with every venue and the bound that removed it (`arb::venue_standings`, `fb426988e`) |
| 38 | Hyperarb | **DONE in `bdb8c40e8`** (clauses resolved; pipeline refused) | `hyperarb { capital = …; parallel { route_a = evaluate(<target>); … } choose <criterion>; hedge volatility; settle_across_domains; require net_profit >= <n>bps; }`; `compiler/src/hyperarb.rs` resolves every leg to a declared venue, chain or domain (a target naming none is refused with the list of what the program declares), maps the clauses onto `ParallelPlan`/`AtomicChoice`/PHASE 9's hedge, and refuses flash capital (PHASE 20), a hedge with no `atomic_hedge` to bound it, `settle_across_domains` over one domain, fewer than two legs, a repeated leg, zero capital and a zero floor; `Item::Hyperarb` is lowered **explicitly** because the catch-all would have dropped it from the artifact (TICKET-078); and it **lowers to the route its `choose` clause selects** (`hyperarb::plan`, `89a15ccc5`): `AtomicBegin, AtomicChoice, MultiHopSwap, RouteFallback, Require, AtomicEnd`, which `check`/`build`/`explain`/`run` all accept — with the net-profit floor enforced against a host-reported measurement, the same way PHASE 37's is (`31522c24e`). The legs are candidate routes (the phase's own pipeline says `Candidate Routes → Filter`); `choose highest_net_output` is refused because the graph holds no price, and `lowest_declared_fee`/`fewest_hops` are computable |
| 39 | Atomic CEX/DEX intents | **COMPLETE in `44a12ba94` + `1f976974a`** (guarantee enforced **and** carried into the artifact) | `VenueDecl.settlement: Option<SettlementGuarantee>` with the closed set `atomic / trusted_adapter / escrow / pre_funded / attested / compensating`; `semantic::verify_venue_decls` refuses an `orderbook` venue that states no guarantee and one that claims `settlement atomic` ("an off-chain venue does not expose enforceable settlement semantics"), accepts all five honest shapes, and accepts `atomic` on an on-chain venue where the VM really enforces it; `x3c fmt` round-trips the clause and an AST stored before it still loads (`#[serde(default)]`). **TICKET-075 closed in `1f976974a`**: the guarantee now travels — a venue lowers to `Operation::VenueSettlement { venue, guarantee }`, emitted as an inline record at opcode `0x58` (`[opcode][u16 len][venue:shape]`, padded, registered in the one `is_payload_opcode` table both the disassembler and the verifier walk by). `x3c inspect artifact.x3b` now prints `VENUE_SETTLEMENT venue <name> settles <shape>`, so a reader with no source can tell an `atomic` leg from a `compensating` one — the half of the phase's sentence the compile-time check could not deliver. `guarantee: Option<…>` is encoded as a present-but-empty shape field, so "states none" is a value the artifact states rather than a default it invents, and one test asserts that absence. `verifier::verify` is the single enforcement point (UTF-8, one separator, non-blank venue, a shape `SettlementGuarantee::parse` knows); the executor's arm advances the pc and does not re-validate, because `VM::execute` is `verify_and_execute` and no public path reaches the executor without it. Not carried, deliberately: a venue's fee/slippage/liquidity still reach the artifact through the plan a route produced, not as a separate record |
| 47 | Source provenance | **DONE in `00d6bbb8e`** | `x3c build --provenance <path>` writes a measured document (source/artifact/compiler-binary/lock hashes, the observed commit, the build environment), `unknown` + a note where it could not measure; four tests |
| 50 | Performance targets | **DONE in `2ff7777dc`** | `crates/x3-tools/src/bin/x3bench.rs` times all ten named operations against the functions that do them, at p50/p95/p99 by nearest rank over real samples; the verbatim run is `.ai/reports/x3lang-benchmarks-20260919.md`. The finding: parsing dominates by two orders of magnitude. A partial run exits non-zero, and a CLI test runs the harness so a stale report cannot go unnoticed |
| 51 | GPU acceleration | **DONE in `79856066c`** | `vm/src/gpu.rs` classifies the phase's six candidates in code with reasons (four consensus-critical — route scoring, signature verification, hash batches, graph scoring — and two not), probes the host path (`BridgeAdapter::gpu_dispatch`) instead of asserting a constant, and reports `Equality::Unproven` carrying the host's own `X3_BACKEND_REQUIRED` message; `Equality::Proven` is unrepresentable on one backend. The compared path is integer-only, so "bit for bit" is checkable. `x3c gpu` prints it. No kernel is faked and the GPU path is refused per candidate with the phase's own reason |

## Consequence for the objective

Every row of the phase list now has an implementation except 20, whose own text forbids
shipping it before a formal safety proof — that is the honest reading of the phase list, independent of how much
of the corpus works. Since the previous revision of this section, 11, 22, 29, 30, 37, 38, 39, 50 and 51
also closed (as decided-with-refused-execution, the way 9, 10 and 20 are), and 47 closed
in `00d6bbb8e`. PHASE 37's duplicate is settled: `compiler/src/arb.rs` is the one surface, and the
other implementation's graph-grounded validation was carried into it in `fb426988e`
(TICKET-076 and TICKET-077 closed). The two cheapest *real* features in
that set are the compiler cost model (35, with 36 as its check) and source
provenance (47): both are additive, both are testable without new host evidence,
and neither is a decision for the owner. 20 is deliberately out of scope; 9–11,
29–30 and 37–39 are each a project and are tracked as such.

---

# Fresh audit #2 — 2026-09-20, verified against `master` at `883078fbc`

The 2026-09-19 audit above was taken at `af64a0769`, which is now ~50 commits and a day behind. Its
**caveats are the part that goes stale first**, because a caveat is a promise that something is refused
until another ticket lands — and several of those tickets have landed. Re-verified today, by building and
running the construct rather than by reading the row:

| # | The 2026-09-19 caveat | Today's measurement |
|---|---|---|
| 9 | "the IR verifier refuses to *emit* it because a perp leg needs a venue adapter this VM lacks" | **emits and runs**: `x3c build v_hedge.x3` → `120 bytes (30 ops)`; `x3c run v_hedge.x3b --measured-delta-bps 1` → `ok` |
| 10 | "the IR verifier refuses to emit it (no lending-protocol adapter, TICKET-069)" | **emits**: `x3c build v_liq.x3` (an intent carrying the slippage ceiling + the liquidation) → `392 bytes (98 ops)`; `x3c run` → `X3_GUARD_UNMEASURED: the guard `profit >= 100bps` needs a profit the host measured` — the fail-closed measured guard, which is the designed behaviour, not a refusal to emit |
| 11 | "the IR verifier and the emitter both refuse to emit it because the transaction graph is not generated (TICKET-070)" | **emits and runs**: `x3c build v_reb.x3` → `132 bytes (33 ops)`; `x3c run` → `ok` |
| 22 | "All-or-nothing across the whole set needs TICKET-080" | **emits and settles the residual**: `x3c build v_net.x3` → `172 bytes (43 ops)`; `x3c run` → `ok — 4 asset ops` |
| 29 | "state-root freshness, venue prices and strategy-commitment linkage need host evidence and are TICKET-074" | **checked now** in `faf3115e8`: three requirements with a vocabulary, `PacketEvidence`, and `x3c packet verify --evidence` reporting what it checked; five VM tests and a CLI test |

**What this audit does *not* claim.** The rows the 2026-09-19 table marked DONE by naming a test file
(12–17, 21, 23–26, 30, 33–36, 37–43, 47–51, 53–55) are exercised by today's `cargo test --workspace`
(**1216 passed / 0 failed**) and by the 19-example sweep (19/19/19/18), but each row's *artifact* has not
been re-read against today's tree one line at a time. The five rows above are the ones whose caveats would
have been *misleading* if left as written, which is why they were the ones re-measured.

**PHASE 20** remains absent by its own text (flash collateral is forbidden before a formal safety proof).
That is the only phase row with no implementation, and it is a deliberate scope decision rather than
unfinished work.

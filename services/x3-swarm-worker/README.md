# x3-swarm-worker

Local worker service for the X3 swarm. This service polls the x3-swarm-api task queue and prints available tasks to stdout.

Use:

```bash
cargo run --manifest-path services/x3-swarm-worker/Cargo.toml
```

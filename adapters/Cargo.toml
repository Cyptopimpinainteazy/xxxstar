[package]
name = "x3-adapters"
version = "0.1.0"
edition = "2021"
description = "X3 Cross-VM HTLC Adapters — lock/claim/refund/verify/finality for EVM, SVM, Bitcoin, and more"

[lib]
path = "src/lib.rs"

[dependencies]
blake2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
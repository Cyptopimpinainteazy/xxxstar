# phase5_panel — DEPRECATED

Absorbed into `apps/x3-desktop/src/ui/panels/Phase5GovernancePanel.tsx`.

**WebSocket service** (`src/services/websocket.ts`) → replaced by direct WebSocket connection in Phase5GovernancePanel, connecting to `ws://localhost:8765` (md_supervisor).

**Date:** 2026-06-17

All new governance UI should be added as panels in `apps/x3-desktop/src/ui/panels/`.
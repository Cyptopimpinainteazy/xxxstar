[package]
name = "x3-readiness"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
authors = ["X3 Team"]
description = "X3 readiness and proof engine CLI"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.7"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1.0"

#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

# Use cargo run for immediate usability even when no binary is preinstalled.
exec cargo run --manifest-path "$ROOT_DIR/proof-forge/Cargo.toml" --bin x3-proof -- "$@"
